# Custom Commands Directory

Place your custom command definitions (`.toml` files) here.
These commands will be available in the Gemini CLI with the slash prefix corresponding to the filename.

Example:
`readme.toml` -> `/readme` command.

## Subdirectories (Namespacing)
You can use subdirectories to namespace commands.
`git/commit.toml` -> `/git:commit` command.
