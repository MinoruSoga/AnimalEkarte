# Custom Sandbox for Gemini CLI
# Used when running with `gemini --sandbox` or `BUILD_SANDBOX=1`

FROM gemini-cli-sandbox

# Install additional project dependencies here
# RUN apt-get update && apt-get install -y python3-pip
# RUN pip3 install numpy pandas
