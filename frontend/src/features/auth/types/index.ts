// Re-export from shared types layer.
// Implementation moved to @/types/auth.
export type {
  Resource,
  ResourceAction,
  ResourcePermission,
  ResourcePermissions,
  ClinicMembership,
  AuthClinic,
  AuthUser,
  AuthContextValue,
} from "@/types/auth";
