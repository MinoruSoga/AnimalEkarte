import { C } from "@/lib/design-tokens";

/**
 * DESIGN.md `button-primary` — SubmitButton/PrimaryButton 以外の汎用 Primary CTA 用 SSOT。
 */
export const PRIMARY_ACTION_BUTTON_CLASSES = `${C.bgActionPrimary} ${C.textOnActionPrimary} ${C.hoverBgActionPrimary} ${C.hoverTextOnActionPrimary} ${C.activeBgActionPrimary} ${C.activeTextOnActionPrimary} h-11 px-4 text-base rounded-full transition-colors shadow-none border-transparent`;
