# seed-old-db（アーカイブ済み）

2026-07-12 PO決定でアーカイブ。`cmd/stage-import` に置換済み
（BE-refactor.md G-1）。

`_` プレフィックスのディレクトリは Go ツールチェーンがビルド・テスト対象から
自動的に除外するため、削除ではなくアーカイブとして木に残している。

復帰する場合はこの dir を `cmd/` 直下へ `git mv` で戻すこと。
