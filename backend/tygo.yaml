packages:
  - path: "github.com/animal-ekarte/backend/internal/model"
    output_path: "/output/models.ts"
    # FE6-1: 生成定数の型 widen（typeof 定数 が string に潰れ「退化 union」化する問題）を解消。
    # union は const 宣言の値自体は維持したまま、型を `export type X = typeof A | typeof B | ...` の
    # 真の literal union として再出力する（値エクスポートに破壊的変更なし）。
    enum_style: "union"
    # FE6-1: lab_report.go の3型（LabExamReportSummary/Detail/ResultItem）は P7 準拠で
    # json タグが medicalrecord domain の非公開 response struct
    # （internal/medicalrecord/lab_report_response.go）へ移設済みのため、
    # model パッケージ自体の構造体には json タグが無い。tygo は無タグ時に Go フィールド名を
    # そのまま出力する（PascalCase）ため、ここに含めると実際の wire 形式（snake_case）と
    # 一致しない誤った型が生成される。現状 FE 側に消費者が 0 件であることを確認済み
    # （2026-07-11 grep 実測）。将来必要になった場合は FE6-5 と同じ手法
    # （domain-owned response struct を export してパッケージ追加生成）で対応すること。
    exclude_files: ["lab_report.go"]
    type_mappings:
      time.Time: "string"
      gorm.DeletedAt: "string | null"
      pq.StringArray: "string[]"
      # FE12-07: 未マップ型は tygo が `any` へ潰す（frontend/CLAUDE.md Type Safety MANDATORY 違反）。
      # JSON 系は `unknown` にして消費側へ境界検証を強制する。値の形は Go 側でも
      # 非構造化なので、`any` のまま素通しにすると型検査が嘘をつく。
      # UUID は wire 上ただの文字列であり `string` が実態。
      json.RawMessage: "unknown"
      datatypes.JSON: "unknown"
      uuid.UUID: "string"
      pq.Int64Array: "number[]"
      "uint64": "number"
      "uint": "number"
    frontmatter: |
      // This file is auto-generated by tygo from backend/internal/model/
      // DO NOT EDIT manually — run `make codegen` to regenerate
  - path: "github.com/animal-ekarte/backend/internal/auth"
    output_path: "/output/auth-responses.ts"
    # /me と login の wire DTO は auth domain の http_response.go が正本。
    # models.ts の M1（clinics omitempty）のような BE/FE drift を type-check で
    # 機械検知できるよう、HTTP response source だけを生成対象にする。
    include_files: ["http_response.go"]
    enum_style: "union"
    type_mappings:
      time.Time: "string"
      gorm.DeletedAt: "string | null"
      pq.StringArray: "string[]"
      # FE12-07: 未マップ型は tygo が `any` へ潰す（frontend/CLAUDE.md Type Safety MANDATORY 違反）。
      # JSON 系は `unknown` にして消費側へ境界検証を強制する。値の形は Go 側でも
      # 非構造化なので、`any` のまま素通しにすると型検査が嘘をつく。
      # UUID は wire 上ただの文字列であり `string` が実態。
      json.RawMessage: "unknown"
      datatypes.JSON: "unknown"
      uuid.UUID: "string"
      pq.Int64Array: "number[]"
      "uint64": "number"
      "uint": "number"
    frontmatter: |
      // This file is auto-generated by tygo from backend/internal/auth/http_response.go
      // DO NOT EDIT manually — run `make codegen` to regenerate
  - path: "github.com/animal-ekarte/backend/internal/trimming"
    output_path: "/output/trimming-responses.ts"
    # FE7-1/FE7-2: TrimmingResponse と summary DTO は trimming domain が正本。
    # Pet/Staff は非公開 summary 型を参照するため tstype:"-" で生成対象から除外し、
    # frontend/src/types/trimming.ts がこの2フィールドだけを補う。
    include_files: ["trimming_response.go"]
    enum_style: "union"
    type_mappings:
      time.Time: "string"
      gorm.DeletedAt: "string | null"
      pq.StringArray: "string[]"
      # FE12-07: 未マップ型は tygo が `any` へ潰す（frontend/CLAUDE.md Type Safety MANDATORY 違反）。
      # JSON 系は `unknown` にして消費側へ境界検証を強制する。値の形は Go 側でも
      # 非構造化なので、`any` のまま素通しにすると型検査が嘘をつく。
      # UUID は wire 上ただの文字列であり `string` が実態。
      json.RawMessage: "unknown"
      datatypes.JSON: "unknown"
      uuid.UUID: "string"
      pq.Int64Array: "number[]"
      "uint64": "number"
      "uint": "number"
    frontmatter: |
      // This file is auto-generated by tygo from backend/internal/trimming/trimming_response.go
      // DO NOT EDIT manually — run `make codegen` to regenerate
  - path: "github.com/animal-ekarte/backend/internal/pet"
    output_path: "/output/pet-responses.ts"
    # Pet detail の wire DTO は pet domain の pet_response.go が正本。
    # models.ts の model.Pet（name_kana 等）のような BE/FE drift を type-check で
    # 機械検知できるよう、HTTP response source だけを生成対象にする。
    include_files: ["pet_response.go"]
    enum_style: "union"
    type_mappings:
      time.Time: "string"
      gorm.DeletedAt: "string | null"
      pq.StringArray: "string[]"
      # FE12-07: 未マップ型は tygo が `any` へ潰す（frontend/CLAUDE.md Type Safety MANDATORY 違反）。
      # JSON 系は `unknown` にして消費側へ境界検証を強制する。値の形は Go 側でも
      # 非構造化なので、`any` のまま素通しにすると型検査が嘘をつく。
      # UUID は wire 上ただの文字列であり `string` が実態。
      json.RawMessage: "unknown"
      datatypes.JSON: "unknown"
      uuid.UUID: "string"
      pq.Int64Array: "number[]"
      "uint64": "number"
      "uint": "number"
    frontmatter: |
      // This file is auto-generated by tygo from backend/internal/pet/pet_response.go
      // DO NOT EDIT manually — run `make codegen` to regenerate
  - path: "github.com/animal-ekarte/backend/internal/owner"
    output_path: "/output/owner-responses.ts"
    # Owner detail の wire DTO は owner domain の http_response.go が正本。
    # models.ts の model.Owner（json:"name"）と OwnerResponse（json:"owner_name"）の
    # フィールド名 drift を type-check で機械検知できるよう、HTTP response source だけを生成対象にする。
    include_files: ["http_response.go"]
    enum_style: "union"
    type_mappings:
      time.Time: "string"
      gorm.DeletedAt: "string | null"
      pq.StringArray: "string[]"
      # FE12-07: 未マップ型は tygo が `any` へ潰す（frontend/CLAUDE.md Type Safety MANDATORY 違反）。
      # JSON 系は `unknown` にして消費側へ境界検証を強制する。値の形は Go 側でも
      # 非構造化なので、`any` のまま素通しにすると型検査が嘘をつく。
      # UUID は wire 上ただの文字列であり `string` が実態。
      json.RawMessage: "unknown"
      datatypes.JSON: "unknown"
      uuid.UUID: "string"
      pq.Int64Array: "number[]"
      "uint64": "number"
      "uint": "number"
    frontmatter: |
      // This file is auto-generated by tygo from backend/internal/owner/http_response.go
      // DO NOT EDIT manually — run `make codegen` to regenerate
  - path: "github.com/animal-ekarte/backend/internal/medicalrecord"
    output_path: "/output/medicalrecord-responses.ts"
    # MedicalRecord detail/list の wire DTO は medicalrecord domain の
    # medical_record_response.go + nested_summary_response.go が正本。
    # models.ts の model.MedicalRecord（version 必須・full relation graph）と
    # MedicalRecordResponse（実 wire: version 含む summary nest）の drift を
    # type-check で機械検知できるよう、HTTP response source だけを生成対象にする。
    # 本スライスは MedicalRecord detail 経路に限定（Examination 等は別スライス）。
    include_files:
      - "medical_record_response.go"
      - "nested_summary_response.go"
    enum_style: "union"
    type_mappings:
      time.Time: "string"
      gorm.DeletedAt: "string | null"
      pq.StringArray: "string[]"
      # FE12-07: 未マップ型は tygo が `any` へ潰す（frontend/CLAUDE.md Type Safety MANDATORY 違反）。
      # JSON 系は `unknown` にして消費側へ境界検証を強制する。値の形は Go 側でも
      # 非構造化なので、`any` のまま素通しにすると型検査が嘘をつく。
      # UUID は wire 上ただの文字列であり `string` が実態。
      json.RawMessage: "unknown"
      datatypes.JSON: "unknown"
      uuid.UUID: "string"
      pq.Int64Array: "number[]"
      "uint64": "number"
      "uint": "number"
    frontmatter: |
      // This file is auto-generated by tygo from backend/internal/medicalrecord/medical_record_response.go and nested_summary_response.go
      // DO NOT EDIT manually — run `make codegen` to regenerate
  - path: "github.com/animal-ekarte/backend/internal/medicalrecord"
    output_path: "/output/hospitalization-responses.ts"
    # Hospitalization detail/list と treatment-plan nested list の wire DTO は
    # medicalrecord domain の hospitalization_response.go + treatment_plan_response.go が正本。
    # models.ts の model.Hospitalization（treatment_plans 埋め込み・full relation graph）と
    # HospitalizationResponse（実 wire: summary nest のみ、treatment_plans なし）の drift を
    # type-check で機械検知できるよう、HTTP response source だけを生成対象にする。
    # nested_summary_response.go は Owner/Pet/Doctor nest 定義のため同梱（medicalrecord-responses と
    # 同一型を二重出力するが structural typing で共存可。別スライスで共通化する場合は削除可）。
    include_files:
      - "hospitalization_response.go"
      - "treatment_plan_response.go"
      - "nested_summary_response.go"
    enum_style: "union"
    type_mappings:
      time.Time: "string"
      gorm.DeletedAt: "string | null"
      pq.StringArray: "string[]"
      # FE12-07: 未マップ型は tygo が `any` へ潰す（frontend/CLAUDE.md Type Safety MANDATORY 違反）。
      # JSON 系は `unknown` にして消費側へ境界検証を強制する。値の形は Go 側でも
      # 非構造化なので、`any` のまま素通しにすると型検査が嘘をつく。
      # UUID は wire 上ただの文字列であり `string` が実態。
      json.RawMessage: "unknown"
      datatypes.JSON: "unknown"
      uuid.UUID: "string"
      pq.Int64Array: "number[]"
      "uint64": "number"
      "uint": "number"
    frontmatter: |
      // This file is auto-generated by tygo from backend/internal/medicalrecord/hospitalization_response.go, treatment_plan_response.go and nested_summary_response.go
      // DO NOT EDIT manually — run `make codegen` to regenerate
  - path: "github.com/animal-ekarte/backend/internal/identitylink"
    output_path: "/output/identitylink-responses.ts"
    # #239 identity-link wire DTOs. include response.go only (request/service stay domain-private).
    include_files: ["response.go"]
    enum_style: "union"
    type_mappings:
      time.Time: "string"
      gorm.DeletedAt: "string | null"
      pq.StringArray: "string[]"
      json.RawMessage: "unknown"
      datatypes.JSON: "unknown"
      uuid.UUID: "string"
      pq.Int64Array: "number[]"
      "uint64": "number"
      "uint": "number"
    frontmatter: |
      // This file is auto-generated by tygo from backend/internal/identitylink/response.go
      // DO NOT EDIT manually — run `make codegen` to regenerate
