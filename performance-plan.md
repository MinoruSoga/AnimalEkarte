# パフォーマンス最適化計画（2026-07-18 起案）

> **原則**: 計測 → 削除 → 最適化の順を厳守する（product-philosophy ③④）。**実測で特定していないものを最適化しない**。
> **前提資産**: ローカル/STG ともフルデータ（owners 10,370 / pets 15,654 / medical_records 425,544 / billing_items 1,542,422 / exam_results 1,322,503）— 本計画の全計測はこの規模で行う。小デモでの計測は無効。
> **優先順位**: 納品クリティカル（#266・UAT・Go-live）が先。本計画の Phase 0 は UAT と並行実施可能（計測は読み取りのみ）。

## 0. 目標メトリクス（④ — これで成否を判定する）

| # | メトリクス | 現状（実測/推定） | 目標 |
|---|---|---|---|
| M1 | 主要一覧ページの初期表示（フルデータ・STG 実 URL） | /owners は白画面∞（#266 対応中）。他一覧は未計測 | **全一覧 3 秒以内**（UAT ゲートと同一） |
| M2 | API p95（k6 持続負荷・フルデータ） | 897ms（7/05・ただし**空 DB での値** — フルデータで要再計測） | p95 < 500ms（一覧系）/ < 300ms（単件系） |
| M3 | 書き込み系 API（会計 PATCH 等） | **4.7 秒を実測**（7/17・混在精算 PATCH・warm） | < 1.5 秒 |
| M4 | コールドスタート（scale-to-zero 起床） | 2.2〜3.2 秒（正常時）/ 33 秒（CF 障害時） | 現状維持を確認（M4 は監視項目・最適化対象外） |
| M5 | 診察中の高頻度操作（カルテ検索・検歴表示） | 未計測 | 検索応答 < 1 秒 |

## 1. 既知の事実（このセッション群で実測済み — 仮説ではない）

- **全件取得クラスの構造問題**: #266（owners 104 リクエスト白画面）が代表。`fetch-limits` に集約された一括取得 6 箇所が同類の候補（棚卸しは #266 の受け入れ条件に含まれ進行中）
- **会計 PATCH 4.7 秒**: 混在精算（payment_splits）で実測。1.5M 行の billing_items 環境での書き込み+再計算経路に何かがある — **Phase 1 の最優先調査対象**
- **DB は直結・プーラー無し**（Cloudflare Containers の Hyperdrive 非対応）。プールは 10/5 に制限済み → **接続待ちがスループット上限になりうる**（負荷計測で顕在化するはず）
- **Container↔DB のレイテンシが未計測**: Container の配置リージョンは Cloudflare 任せ、DB は東京（ap-northeast）。1 リクエスト中のクエリ数×往復レイテンシが積算される構造 — N+1 の被害が AWS 時代より増幅される
- カルテ検索は 6 カラム ILIKE（トライグラム索引なし）が 425K 行を走る。#237 で EXISTS サブクエリが追加予定 → さらに重くなる方向
- 月次集計は 1.5M 行の billing_items を CTE で集計（`accounting_repository_reports_{monthly,close}.go`）
- 計測基盤は**既にある**: k6（`load-tests/`・`pnpm cf:load-smoke`）、Lighthouse CI（Frontend Lighthouse Audit workflow）、Workers Logs（sampling 100%）、`EXPLAIN ANALYZE` はフルデータのローカルで再現可能

## 2. Phase 0 — ベースライン計測（最初にやる・0.5 日・読み取りのみで UAT と並行可）

**成果物: `docs/ops/perf/BASELINE_20260718.md`（数字の表。以後の全 Phase はこの表の悪い順に着手する）**

1. **API レイテンシ表**: 主要エンドポイント（下表）を STG 実 URL に対し各 20 回、p50/p95 を記録
   - 一覧系: owners(#266 後) / pets / medical-records / reservations / examinations / accountings / trimming
   - 単件系: medical-record 詳細 / accounting 詳細
   - 書込系: 会計 PATCH（混在精算）/ カルテ保存 / 予約作成
   - 集計系: 月次レポート / レジ締め集計 / 集計・分析ページの API
2. **k6 フルデータ再計測**: `k6-cf-stg-sustained.js` を再実行（7/05 の 897ms は空 DB の値で比較基準にならない）。プール 10 制限下の同時実行数スイープ（VU 5/10/20）で**接続待ち飽和点**を特定
3. **DB スロークエリ**: ローカル（同一フルデータ）で `pg_stat_statements` を有効化し、UAT シナリオ一巡後の top10 を採取。上位に `EXPLAIN ANALYZE`
4. **Container↔DB レイテンシ実測**: `/health`（DB ping 有無を確認）と単純クエリ 1 本のエンドポイントの差分から往復コストを推定
5. **FE**: Lighthouse を主要 5 ページ（受付・カルテ・会計・一覧 2 種）に対しフルデータで実行。バンドルサイズは `vite build` の出力を記録
6. **M3 の 4.7 秒の内訳**: 会計 PATCH をローカルで再現し、slog タイミング（無ければ一時計測コード）で BE 内訳（クエリ数・tx 時間）を分解

## 3. Phase 1 — 実測 Top Offender の除去（Phase 0 の表が確定してから。想定 1〜2 日）

着手順は**ベースライン表の悪い順**。事前予想（Phase 0 で裏切られたら従わない）:

- **P1-a 会計 PATCH 4.7 秒の根治**: 予想容疑 = tx 内の不要な再集計 or 明細全件ロード or 監査ログの同期書込の直列化。内訳計測で確定してから修正
- **P1-b 全件取得クラスの残り**: #266 の fetch-limits 棚卸し結果で破綻判定されたページを owners-pets-list-plan.md と同型（サーバサイド化+limit clamp）で潰す
- **P1-c インデックス補強**: pg_stat_statements 上位の Seq Scan に対して。**必ず EXPLAIN ANALYZE で前後比較を記録**（migration は新規追記・採番は実ファイル最大+1）
- **P1-d N+1 の実測検出**: 一覧系 API 1 リクエストあたりのクエリ数を計測（GORM ログで機械集計）。閾値（例: 1 リクエスト 15 クエリ超）超過のエンドポイントを列挙 → Preload/JOIN 化。**直結+リージョン間レイテンシ環境では N+1 削減が最も効率の良い改善**

## 4. Phase 2 — 構造改善(実測が要求した場合のみ。想定 1〜2 日)

- **P2-a 検索の索引化**: カルテ検索（ILIKE 6 カラム+#237 の EXISTS）が M5 目標を外す場合、`pg_trgm` GIN を導入（拡張はプリインストール確認済み）。導入判断は Phase 0 の実測値、効果判定は同一クエリの前後比較
- **P2-b 集計の事前計算**: 月次/締め集計が目標を外す場合のみ。第一候補は「締め確定時に日次サマリを確定保存」（既に cash_register_closes がある — 二重管理にならない範囲で確定値のスナップショット化）。**リアルタイム集計テーブルや汎用キャッシュ層は作らない**（⑤の前倒し禁止）
- **P2-c 応答ペイロード削減**: 一覧 API が全カラム返却で肥大している場合、list 専用レスポンスの絞り込み（petListResponse の先例に倣う）

## 5. Phase 3 — インフラ調整(コード変更より後。想定 0.5 日+観測)

- **P3-a Smart Placement**: Worker の `placement: {mode: "smart"}` を検証 — Container/Worker を DB（東京）近くに寄せられれば全クエリの往復が一律短縮される。**1 設定で全 API に効くため費用対効果最大の候補**だが、Containers への適用可否は要実測
- **P3-b インスタンスタイプ**: CPU バウンドが実測された場合のみ `basic`(1/4 vCPU) → `standard` 等を検討（コスト増とセット判断・PO 承認）
- **P3-c プールと max_instances の再調整**: k6 飽和点の実測に基づき、PlanetScale の接続上限内で `DB_MAX_OPEN_CONNS × max_instances` を再配分
- **P3-d sleepAfter 調整**: 営業時間帯のコールドスタート遭遇率を Workers Logs で実測し、必要なら `10m` を延長（コスト増とセット判断）

## 6. Phase 4 — フロントエンド(Lighthouse が要求した場合のみ)

- バンドル: ルート単位 code-splitting の実態確認（React.lazy 適用状況）・重量依存（recharts 等）の遅延読み込み
- レンダリング: 一覧テーブルの再レンダリング計測（React DevTools Profiler）。仮想化（virtualizer）は**行数がサーバサイドページングで抑制された後に本当に必要か再評価**（#266 完了後は 1 ページ 50 行 — 仮想化不要のはず。YAGNI）
- 画像: R2 画像の Cache-Control / CF キャッシュルール（Phase 1-5 の WAF/キャッシュ残項目と同時に）

## 7. やらないこと（① — 要求される前に作らない）

- Redis 等の外部キャッシュ層の新設（実測が「同一クエリの反復」を示すまで）
- GraphQL / BFF の導入・API の全面再設計
- read replica（PlanetScale のスケールアップ含む）— 接続飽和・CPU 飽和の実測前
- 目標を既に満たしているエンドポイントへの改善（表の緑行は触らない）

## 8. 運用・ゲート

- 各修正は **Phase 0 の同一手順で前後比較**し、数字を BASELINE ドキュメントに追記して初めてクローズ（「速くなった気がする」の禁止）
- k6 の閾値（`thresholds`）に M2 目標値を設定し、`performance-tests.yml` を STG 実 URL 向けに更新 → **回帰を CI で機械検出**（④の恒久化）
- 実装は 1 改善 = 1 コミット（`perf(...)`）。Issue 化する場合は本書を正本に #260 方式で分割
- **実践ゲート**: ①責任者 = PO（起案 2026-07-18・業務目的 = 受付/診察中の操作待ち時間の削減） ②削除 = 全件取得クラス・不要クエリ（N+1）・未使用インデックスの削除を含む ④メトリクス = §0 の M1〜M5
