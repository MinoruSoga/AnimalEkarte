# 取扱説明書掲載用テキスト: トリミング — 前回のカルテのコピー

> **対象チケット**: EMR-168（スプシ No.32「取扱説明書には前回のカルテのコピーのやり方が載っていないが載っけた方がいいのでは？」）
> **用途**: システム内取扱説明書（`/manual` → 画面マニュアル「トリミング」）へ掲載する操作説明テキスト。
> **掲載状態**: 同一内容を `frontend/src/features/manual/content/screens/10-trimming.md` の「前回のカルテをコピーする（過去履歴の引用）」節へ反映済み。文言を修正する場合は同ファイルを更新し、本書は掲載用の原本として保持する。
> **動作の実装根拠**: `frontend/src/features/trimming/components/TrimmingRightColumn.tsx`（履歴カード → `onHistoryClick`）、`frontend/src/features/trimming/hooks/use-trimming-history.ts`（同一ペットの履歴取得・検索・並び替え・期間絞り込み）。

---

## 前回のカルテをコピーする

前回と同じコース・スタイルで施術する場合、過去のトリミング記録をワンクリックで現在のフォームへ取り込めます。

### 手順

1. トリミングの新規登録（または編集）画面を開きます。
2. 画面右側の「**過去の施術履歴**」パネルに、このペットの過去の施術がカードで一覧表示されます。
3. 必要に応じて絞り込みます。

   | 絞り込み | 内容 |
   | -------- | ---- |
   | 検索単語 | スタイル希望の文言で検索（ひらがな・カタカナ不問） |
   | 並び順   | 「新→古」/「古→新」 |
   | 実施日   | 開始日〜終了日で期間を指定 |
   | クリア   | すべての絞り込みを解除 |

4. 取り込みたい履歴のカードをクリックします。
5. フォームに内容が反映されます。当日の体重・担当などを入力・調整し、「**登録**」（または「**更新**」）で保存します。

### コピーされる項目 / されない項目

| 反映される                 | 反映されない（当日の入力が必要） |
| -------------------------- | -------------------------------- |
| コース                     | 体重（BW）・体温（BT）           |
| オプション                 | 画像（希望スタイル・完成）       |
| スタイル要望               | 日付・ステータス・次回予定日     |
| 使用シャンプー・使用リボン | 担当者の確定選択                 |
| 備考                       |                                  |
| 前回の担当者名（表示のみ） |                                  |

> ⚠️ **担当者名について**: 履歴をクリックすると前回の担当者名が「担当医」欄に表示されますが、担当者の選択自体はコピーされません。保存の前に必ず「担当医」欄から担当者を選択して確定してください（前回と同じ担当なら、選択画面でその名前が最初からハイライトされています）。

### 補足

- 履歴パネルに表示されるのは、いま開いているペットの過去記録だけです。他のペットの履歴は表示されません。
- コピーは「入力の下書き」を楽にする機能です。取り込んだ内容は保存前にいつでも書き換えられます。
- コース・オプションはマスタで無効化されていると選択肢に出ません（無効化後の再コピーには注意してください）。
