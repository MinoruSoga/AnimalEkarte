# 受け入れテスト再実行結果 2026-07-28 (local)

**実行者**: Claude（AI エージェント） / **対象**: S01〜S06 再実行（2026-07-17 報告分のバグ修正確認） / **環境**: ローカル（http://localhost:3003 、seed 003_demo） / **ログイン**: admin@noavet.jp（システム管理者、全医院アクセス可）

> 本レポートは `docs/ops/testing/scenarios/README.md` の記録ルールに従う。シナリオファイル本体は編集していない。
> 目的は前回（2026-07-17）報告した FAIL/バグ候補7件（BUG-401〜407）が修正されたかどうかの再検証であり、PASS 済みだった項目の網羅的な再実施は行っていない（S02 の異常値ハイライト等、前回 PASS の基本機能は本レポートでは深く再確認していない）。
> BUG-405 / BUG-2026-07-28-02 の判定は、再実行時の観察を保持したうえで、現行コード・テスト証跡に基づき後日訂正した。この訂正のためのブラウザ再実行は行っていない。
> BUG-403 / BUG-2026-07-28-01 は、再実行後に fetcher 境界を修正し、スコープ限定テスト17件と ESLint で確認した。以下の再実行時の観察は修正前の履歴として保持し、修正後のブラウザ/API再実行は行っていない。

## サマリー: 前回バグ候補の修正状況

| # | 内容 | 前回判定 | 今回判定 | 備考 |
|:--|:--|:--:|:--:|:--|
| 1 (BUG-401) | フィラリア予防薬の選択肢が2件しか出ない | FAIL | ✅ 修正確認 | ワクチン選択が実マスタを全件参照するよう修正された |
| 2 (BUG-402) | `/line-reserve/{clinicId}/` が503で空白ページ | FAIL(BLOCKED) | ✅ 修正確認 | LIFF予約アプリが正常起動、予約入力ステップまで進行確認 |
| 3 (BUG-403) | ケアプランtype=投薬/処置/持ち物が常に400 | FAIL | ✅ コード・テストで修正確認 | fetcher境界で作成・更新時の参照IDを数値化し、スコープ限定17テストがGREEN（詳細は 3-session-agent.html #ledger BUG-459） |
| 4 (BUG-404) | 入院デイリーカルテのケアログ登録・日次記録取得が全滅(500) | FAIL | ✅ 修正確認 | GET/POSTとも正常動作 |
| 5 (BUG-405) | 当日開始の入院が「入院中」タブに出ない | FAIL | 📝 記録訂正 | 当日の観察は保持するが、現行実装は DEC-2 の手動チェックイン仕様に一致し、コード欠陥ではない（詳細は 3-session-agent.html #ledger BUG-460） |
| 6 (BUG-406) | 主訴詳細が保存後リロードで空テンプレートに戻る | FAIL | ✅ 修正確認 | リロード後も正しく復元される |
| 7 (BUG-407) | 死亡記録ダイアログで外側「更新」を押さないと保存されないUX | UX注記 | ✅ 修正確認 | ダイアログ送信で即時DB保存・外側フォームも同期 |

**追加で判明した新規事項（今回のみ）**:

- 【重大・一時的】`GET /api/v1/pets`（ペット一覧/検索API）が常に400を返すリグレッションを再実行序盤で発見。`/owners` 画面のクラッシュとペット選択画面全滅の原因。ユーザーに報告し対応を依頼、対応後に再検証して修正を確認済み（経緯は git 履歴の bug.md 退役前リビジョンに保持）。

修正確認: **6件**（BUG-401, 402, 403, 404, 406, 407） / 現行コード・テスト証跡による記録訂正: **1件**（BUG-405） / 継続中のコード欠陥: **0件** / 一部修正・別原因で再発: **0件**

---

## 詳細

### S01: 死亡ペット誤操作の物理ブロック（対象: マメラ, ID 1000001）

BUG-407 の修正検証を中心に実施。

| # | 操作 | 判定 | 観察内容 |
|:--|:--|:--:|:--|
| 1 | ペット編集 → 死亡記録ダイアログで死亡日を入力し送信 | PASS | ダイアログの「死亡を記録する」ボタン押下時点で即時DB保存される。外側の「更新」ボタンを別途押さなくても、外側フォームの生死ステータス表示も即座に同期して「死亡記録を解除」ボタンに切り替わることを確認（BUG-407 修正確認） |
| 2 | 保存の永続化確認 | PASS | 外側フォームを保存せずダイアログを閉じても、API (`GET /v1/pets/1000001`) で `status: "deceased"` が確認できた |
| 3 | ペット選択画面での表示 | PASS | 死亡ペットは「死亡」バッジ＋「選択不可」（アクセシブルネームも「死亡・選択不可」）で表示され、物理的に選択できない |
| 4 | 飼主No検索での除外 | 変化なし | `include_deceased` 未指定（デフォルト）で検索すると死亡ペットは0件（検索結果に出てこない）。前回同様の挙動で仕様上の欠陥とは断定しない（要実測のまま） |
| 5 | 生存への復帰 | PASS | `DELETE /v1/pets/1000001/death` で復帰、`status: "alive"` に戻ったことを確認。テスト後、元の状態（生存）に復元済み |

### S02: 検査異常値ハイライトと確定ロック

前回 PASS 済みのため今回は深く再検証していない（境界値の要実測項目・権限ロール未整備の BLOCKED も前回から変化なしと推定）。

### S03: ワクチン接種→次回予定自動計算（対象: 豆助, ID 1000002）

| # | 操作 | 判定 | 観察内容 |
|:--|:--|:--:|:--|
| 1 | ワクチン選択プルダウンの選択肢確認 | ✅ 修正確認 | 以前はハードコードされた2択（実マスタと無関係）だったが、現在は実マスタ（`useGetAllVaccinesMaster`）を全件（12件）正しく描画する。フィラリア関連の項目（フィラリア予防薬/フィラリア予防注射/フィラリア予防(犬)/フィラリア予防(猫)の4件）もすべて選択肢に表示されることを確認 |

補足: マスタ側のフィラリア関連件数自体は前回報告の11件から4件に変わっているが、これはマスタデータの変更によるものであり、フォーム側の絞り込みバグではない。

### S04: LIFF飼い主予約ジャーニー通し

| # | 操作 | 判定 | 観察内容 |
|:--|:--|:--:|:--|
| 1 | `http://localhost:3003/line-reserve/1/` へアクセス | ✅ 修正確認 | 以前は空白ページ（Vite dev サーバーが `src/main.tsx` を503で返していた）だったが、現在は正常に「ノア動物病院 八王子」のトップ画面が表示される |
| 2 | 「新規予約」を押す | PASS | お客様情報入力ステップ（お名前・電話番号・ペット選択）まで正常に進行することを確認 |
| 3〜12 | コース選択〜キャンセル後の枠再表示等 | 未実施 | 手順1のブロッキングissueが解消されたことの確認が今回の主目的のため、以降の全ステップは網羅的には再実施していない |

### S05: 入院サイクル（対象: チビチビ ID 1（既存入院）, はな ID 1000005（新規テスト入院、検証後削除））

| # | 操作 | 判定 | 観察内容 |
|:--|:--|:--:|:--|
| 1 | ケアプラン type=食事/指示・その他 | PASS | 変化なし、正常動作 |
| 2 | ケアプラン type=投薬（UI経由） | 修正前の観察 | マスタ選択UI（薬剤選択セレクタ）が新たに追加され、以前は存在しなかった導線ができていた。一方、当時は薬剤を選択して送信すると「medicine_id: 正しい形式で入力してください」で400になり登録できなかった |
| 3 | API直接検証（type=medicine, medicine_id を文字列 vs 数値で送信） | 修正前の観察 | 文字列送信→400、数値送信→201。フロントの型定義（string）とバックエンドの型定義（uint64/数値）の不一致が原因と特定した。その後、fetcher境界の作成・更新処理で参照IDを数値化し、スコープ限定17テストがGREEN。修正後のブラウザ/API再実行は行っていない（3-session-agent.html #ledger BUG-459） |
| 4 | 日次記録のGET取得 | ✅ 修正確認 | `GET /v1/hospitalizations/{id}/daily-records/{date}` が正常に200を返す |
| 5 | ケアログ登録（type=food） | ✅ 修正確認 | `POST /v1/hospitalizations/{id}/daily-records/{date}/care-logs` が201で成功し、直後のGETにも正しく反映される |
| 6 | 当日開始の入院登録→「入院中」タブ表示 | 📝 記録訂正 | 再実行時、はな（開始日=本日）が `status: "reserved"` で作成され「予約」タブに残り、既存のチビチビ（`status: "admitted"`）が「入院中」に表示されたという観察、およびテスト後に当該レコードを削除した事実は保持する。一方、現行コード・テストでは、DEC-2 に従い日付から status を自動遷移させず、`reserved` かつ `canEdit` の詳細画面に「チェックイン」を表示し、操作時に `PATCH` で `status: "admitted"` を送る。退院処理は `admitted` かつ `canDelete` の場合だけ表示される。このため権限が主因の問題でもコード欠陥でもなく、旧判定が現行ツリーと不整合だった。この訂正ではブラウザを再実行していない |

### S06: カルテ確定Lockと監査証跡（対象: 豆助, レコード #1425548（新規作成、テスト用として残置））

| # | 操作 | 判定 | 観察内容 |
|:--|:--|:--:|:--|
| 1 | 新規カルテ作成→主訴詳細を入力→同タブのまま保存 | PASS | 「保存しました」トースト表示 |
| 2 | ページを完全リロードして問診タブを再度開く | ✅ 修正確認 | 入力した主訴詳細（「再検証テスト:嘔吐3回、元気消失」）が正しく復元されることを確認。以前は空のテンプレート表示に戻っていた |
| 3〜9 | 確定・編集ガード・追記・解除不在等 | 未実施 | 前回セッションで機能確認済みのため今回は再実施していない |

**後始末に関する注記**: 検証用に作成したカルテ #1425548 は、ツール側の破壊的操作ブロッカーにより DELETE API の実行が拒否されたため削除できず、seed データに残置されている。次回 DB リセット時に解消される。

---

## 新規発見事項（前回から変化・新規リグレッション）と記録訂正

1. **【一時的・対応済み】ペット一覧/検索API (`GET /v1/pets`) が常に400を返す重大リグレッション**。再実行序盤で発見し、`/owners` 画面クラッシュとペット選択機能全滅の原因になっていた。ユーザーに報告して対応を依頼し、対応後に `GET /v1/pets` が200で正常動作することを再検証済み。
2. **BUG-2026-07-28-01（後日修正確認）**: 再実行時は、マスタ選択UI追加後も `medicine_id` 等の型不一致（文字列 vs 数値）により400で登録できなかった。その後、作成・更新双方のfetcher境界で参照ID変換を実装し、スコープ限定17テストとESLintで修正を確認した。修正後のブラウザ/API再実行は行っていない。
3. **BUG-2026-07-28-02（記録訂正）**: 当日開始の入院が `reserved` で「予約」タブに表示された過去観察は保持するが、日付で自動遷移せず DEC-2 の手動チェックインで `admitted` にする現行仕様と一致する。現行コード・テスト証跡ではチェックイン導線と状態別の退院導線が存在するため、継続中のコード不具合という解釈を取り下げる。ブラウザ再実行による訂正ではない。

---

## 未実施・保留事項

- S02 手順4（境界値の判定挙動）、S02/S06 の権限ロール未整備によるBLOCKED、S01 A1（Lステップ配信除外）、S06 手順9（audit_logs DB確認）: 前回から状況の変化を確認しておらず、次回本格再実行時に要再確認
- S04 手順3〜12: LIFFジャーニーの残り手順は今回未実施（手順1のブロッカー解消確認が主目的だったため）
- S02 の基本機能（異常値ハイライト等、前回PASS分）: 今回は深く再検証していない
