# 動物病院管理システム 画面仕様書

本ドキュメントは、全画面（ルート）ごとの仕様を定義します。
各画面のルートパス、目的、構成コンポーネント、データフロー、ユーザー操作を網羅しています。

> **デザイン・UI共通仕様**:
> - 操作ボタンはNotion風に統一（メイン: `#038B94`, プライマリ: `#2383E2`）
> - 各行の操作アイコンは `MoreHorizontal` + `size-5` を使用
> - 内部ID（〇〇No, 〇〇ID等）はUI上表示する場合、等幅フォント（font-mono）を使用する

---

## 凡例

| 記号 | 意味 |
|------|------|
| `[R]` | ルートコンポーネント (`routes/` 配下) |
| `[C]` | 機能固有コンポーネント (`components/` 配下) |
| `[S]` | 共有コンポーネント (`/components/shared/`) |
| `[H]` | フック (`hooks/` 配下) |
| `[M]` | モーダル / ダイアログ |

---

## 1. ダッシュボード

### 1.1 ダッシュボード（カンバンボード）

| 項目 | 内容 |
|------|------|
| **ルート** | `/` |
| **コンポーネント** | `[R] Dashboard` |
| **目的** | 病院全体の来院フローをカンバンボード形式で俯瞰管理する |

**画面構成:**
- ヘッダー: タイトル（「当日の受付」）+ フィルターボタン + 新規予約ボタン
- フィルターバー（トグル表示）: 診察区分（初診/再診チェックボックス）、指名（担当医Select）、種類（トリミングのみCheckbox）
- カンバンボード（5カラム: 受付予約 → 受付済 → 診療中 → 会計待ち → 会計済）
- 各カラムに患者カード（`[C] AppointmentCard`）を配置
- カード間のドラッグ&ドロップによるステータス遷移（`dnd-kit`）
- カードクリックで詳細モーダル（`[C] DashboardDetailModal`）表示

**AppointmentCard 表示項目:**
| 項目 | 表示内容 |
|---|---|
| 時刻 | `appointment.time`（Clock アイコン付き、等幅フォント） |
| 次回予約 | `nextAppointment` バッジ（「次回予約済」=secondary / 「精算未確認」=destructive＋AlertCircle） |
| 飼い主名 | `ownerName`（太字） |
| ペット | `petType - petName`（Dog アイコン付き） |
| 初診/再診 | `visitType` バッジ（初診=青背景 / 再診=スレート背景） |
| 診療区分 | `serviceType` バッジ |
| 担当医 | `doctor` バッジ（指名時は「指」ラベル＋オレンジ背景） |
| ミニアクション | カード下部に配置（カルテ/施術、会計、入院へのクイックリンク） |

**DashboardDetailModal 表示項目:**
| セクション | 項目 |
|---|---|
| ヘッダー | 初診/再診アイコン（初/再）、診療区分名、ステータスバッジ（カラム別カラー） |
| 時間カード | 時刻（等幅・大文字）、nextAppointment バッジ |
| 患者情報 | ペット名、ペット種、飼主名 |
| 診療詳細 | 担当医（未定表示あり）、指名バッジ |
| 関連ページ | カルテ/施術、会計、入院へのリンクボタン群 |

**DashboardDetailModal ステータス別アクション:**
| ステータス | アクションボタン |
|---|---|
| 受付予約 | 取消、編集、飼主詳細、「受付済にする」 |
| 受付済 | 飼主詳細、「カルテ作成」（診療時、同時に診療中へ移動）/ 「診察を開始する」（非診療時） |
| 診療中 | 飼主詳細、「診察を終了する」、「カルテ入力」、「検査」（診療時）/ 「施術記録」（トリミング時） |
| 会計待ち | 飼主詳細、「会計へ進む」 |
| 会計済 | 飼主詳細、「完了/リストから削除」 |

---

## 2. 予約管理

### 2.1 予約カレンダー

| 項目 | 内容 |
|------|------|
| **ルート** | `/reservations` |
| **コンポーネント** | `[R] ReservationManagement` |
| **目的** | 診療・トリミング・手術の予約をカレンダー形式で一元管理する |

**画面構成:**
- ヘッダー: タイトル + 新規予約ボタン
- ツールバー: 日付ナビゲーション（前/次、今日）、現在年月表示、担当医フィルタ、表示切替（月/週）
- 凡例エリア: 予約種別ごとのカラードットと名称一覧
- カレンダービュー（`[C] MonthView` / `[C] WeekView` の切替）
- 予約クリックで詳細モーダル → 編集/削除/ステータス変更
- 新規作成で予約フォームモーダル

---

## 3. 飼主・ペット管理

### 3.1 飼主一覧

| 項目 | 内容 |
|------|------|
| **ルート** | `/owners` |
| **コンポーネント** | `[R] OwnersList` |
| **目的** | 飼主・ペット情報の検索・一覧表示 |

**画面構成:**
- ヘッダー: タイトル + 新規登録ボタン
- 検索バー（`[S] NotionFilter`）+ 件数表示
- データテーブル（`[S] DataTable`）: 飼主No、飼主名、ペット番号、ペット名、生死、種、生年月日、体重、環境、前回来院、操作

### 3.2 飼主登録/編集

| 項目 | 内容 |
|------|------|
| **ルート** | `/owners/new` / `/owners/:id` |
| **コンポーネント** | `[R] OwnerForm` |
| **目的** | 飼主情報の入力・更新、配下ペットの管理 |

**画面構成:**
- ヘッダー: タイトル + 保存ボタン
- 飼主情報セクション（4カラムグリッド）: 飼主No、郵便番号(会社/自宅)、住所1/2(会社/自宅)、会社名、会員区分、飼主名、危険人物、備考、生年月日、メール、電話番号、会社電話、割引率
- ペット一覧テーブル: ペット追加ボタン、各ペット行アクション（編集、予約、カルテ、等）
- ペット編集モーダル（`[C] PetEditModal`）: ペット名、CFBE(種別)、性別、生年月日、BREED(品種)、毛色、体重、避妊去勢、入手区分、危険度、保険、備考

---

## 4. 電子カルテ

### 4.1 カルテ一覧

| 項目 | 内容 |
|------|------|
| **ルート** | `/medical-records` |
| **コンポーネント** | `[R] MedicalRecords` |
| **目的** | 全カルテの検索・一覧表示 |

### 4.2 カルテ入力/編集

| 項目 | 内容 |
|------|------|
| **ルート** | `/medical-records/new` / `/medical-records/:id` |
| **コンポーネント** | `[R] MedicalRecordForm` |
| **目的** | 診療記録の全項目を9タブ構成で入力・編集する |

**タブ詳細:**
1. **問診**: 主訴、治療方針、カルテ履歴
2. **診察/治療プラン**: 身体所見・診断（DiagnosisHeader）、過去プラン、治療プランテーブル
3. **治療**: 完了処置の記録管理
4. **予防接種**: 予防接種記録（VaccinationForm）・履歴
5. **定期健診**: 定期健診記録・履歴
6. **検査**: 検査オーダー・結果履歴
7. **画像**: 画像アップロード・ギャラリー
8. **見積書**: 概算見積作成
9. **会計(医師確認)**: 算定チェック

---

## 5. 入院管理

### 5.1 入院一覧

| 項目 | 内容 |
|------|------|
| **ルート** | `/hospitalization` |
| **コンポーネント** | `[R] HospitalizationList` |
| **目的** | 入院・ホテル患者の一覧管理 |

### 5.2 入院登録/編集

| 項目 | 内容 |
|------|------|
| **ルート** | `/hospitalization/new` / `/hospitalization/:id/edit` |
| **コンポーネント** | `[R] HospitalizationForm` |
| **目的** | 入院予約・登録、初期治療プランの設定 |

**画面構成:**
- 患者情報カード（`[S] PatientInfoCard`）
- 3カラムグリッド: 基本情報、飼主からのリクエスト、スタッフへの連絡事項
- 治療プランテーブル
- 診療費計算サマリ（小計、税、全体値引き、合計）

### 5.3 入院詳細（デイリーカルテ）

| 項目 | 内容 |
|------|------|
| **ルート** | `/hospitalization/:id` |
| **コンポーネント** | `[R] HospitalizationDetail` |
| **目的** | 入院患者のケアプラン管理、デイリーログ記録 |

---

## 6. トリミング

### 6.1 トリミング一覧

| 項目 | 内容 |
|------|------|
| **ルート** | `/trimming` |
| **コンポーネント** | `[R] TrimmingList` |
| **目的** | トリミング予約の一覧管理 |

### 6.2 トリミング登録/編集

| 項目 | 内容 |
|------|------|
| **ルート** | `/trimming/new` / `/trimming/:id` |
| **コンポーネント** | `[R] TrimmingForm` |
| **目的** | トリミング記録の入力・管理 |

**画面構成:**
- 3カラムレイアウト:
  - 左: コース選択、スタイル希望、メモ、オプション、希望スタイル画像
  - 中: 体重(BW)、体温(BT)、使用シャンプー/リボン、備考、完成画像
  - 右: 施術履歴（フィルタ・検索・過去記録の参照）

---

## 7. 検査管理

### 7.1 検査一覧

| 項目 | 内容 |
|------|------|
| **ルート** | `/examinations` |
| **コンポーネント** | `[R] Examinations` |
| **目的** | 検査オーダー・結果の一覧管理 |

### 7.2 検査登録/編集

| 項目 | 内容 |
|------|------|
| **ルート** | `/examinations/new` / `/examinations/:id` |
| **コンポーネント** | `[R] ExaminationForm` |
| **目的** | 検査種別、ステータス、所見の入力 |

---

## 8. 会計

### 8.1 会計一覧

| 項目 | 内容 |
|------|------|
| **ルート** | `/accounting` |
| **コンポーネント** | `[R] Accounting` |
| **目的** | 会計レコードの一覧管理 |

### 8.2 会計詳細・精算

| 項目 | 内容 |
|------|------|
| **ルート** | `/accounting/new` / `/accounting/:id` |
| **コンポーネント** | `[R] AccountingDetail` |
| **目的** | 明細の確認・追加、保険適用、決済処理、帳票発行 |

**画面構成:**
- 左カラム: 明細一覧（カルテ連携明細 + 手動追加ボタン）、金額合計サマリ
- 右カラム:
  - ペット保険（窓口精算）: スイッチ、負担割合選択、負担額計算
  - 決済情報: 請求金額表示、支払方法選択（現金/カード/電子マネー）、お預かり・お釣り計算
- アクション: 会計確定、診療明細書発行、領収書発行

---

## 9. 予防接種管理

### 9.1 予防接種一覧

| 項目 | 内容 |
|------|------|
| **ルート** | `/vaccinations` |
| **コンポーネント** | `[R] VaccinationList` |
| **目的** | 予防接種記録の一覧管理 |

### 9.2 予防接種登録/編集

| 項目 | 内容 |
|------|------|
| **ルート** | `/vaccinations/new` / `/vaccinations/:id` |
| **コンポーネント** | `[R] VaccinationForm` |
| **目的** | 予防接種内容・次回予定の入力 |

**画面構成:**
- 左カラム: フォーム（名称、接種日、補助説明、LOT1-4、次回予定設定、備考）
- 右カラム: 過去の予防接種履歴一覧（フィルタ・検索・複製機能）

---

## 10. 見積書管理

### 10.1 見積書一覧

| 項目 | 内容 |
|------|------|
| **ルート** | `/estimates` |
| **コンポーネント** | `[R] EstimateList` |
| **目的** | 見積書の検索・一覧管理 |

### 10.2 見積書登録/編集

| 項目 | 内容 |
|------|------|
| **ルート** | `/estimates/new` / `/estimates/:id/edit` |
| **コンポーネント** | `[R] EstimateForm` |
| **目的** | 見積情報の入力・更新 |

---

## 11. 在庫管理

### 11.1 在庫一覧

| 項目 | 内容 |
|------|------|
| **ルート** | `/inventory` |
| **コンポーネント** | `[R] InventoryList` |
| **目的** | 在庫品目の一覧表示・検索 |

### 11.2 在庫登録/編集

| 項目 | 内容 |
|------|------|
| **ルート** | `/inventory/new` / `/inventory/:id` |
| **コンポーネント** | `[R] InventoryForm` |
| **目的** | 在庫品目の登録・更新 |

---

## 12. シフト管理

### 12.1 シフト管理カレンダー

| 項目 | 内容 |
|------|------|
| **ルート** | `/shifts` |
| **コンポーネント** | `[R] ShiftCalendarPage` |
| **目的** | スタッフの勤務シフトをカレンダー形式で管理する |

---

## 13. 設定・マスタ管理

### 13.1 マスタ設定トップ

| 項目 | 内容 |
|------|------|
| **ルート** | `/settings` |
| **コンポーネント** | `[R] MasterSettingsIndex` |
| **目的** | マスタカテゴリ一覧を表示（Notionスタイルのセクションリスト） |

### 13.2 個別マスタ設定（共通パターン）

| 項目 | 内容 |
|------|------|
| **ルート** | `/settings/*` |
| **コンポーネント** | `[R] *Settings` (例: `StaffSettings`) |
| **目的** | 各種マスタデータのCRUD管理 |

**画面構成:**
- `[C] MasterCRUDPage`: 一覧テーブル（データテーブル）+ サイドパネル
- `[C] MasterSidePanel`: Notionスタイルのスライドイン編集パネル。タイトル、ステータス切替、各種プロパティ行。

---

## 14. 共通ペット選択画面

各機能で対象を選択する際に使用される共通フロー。
- コンポーネント: `[S] PetSelection`
- 使用ルート:
  - `/medical-records/select-pet`
  - `/hospitalization/select-pet`
  - `/trimming/select-pet`
  - `/accounting/select-pet`
  - `/examinations/select-pet`
  - `/vaccinations/select-pet`

---

## 15. ログイン

| 項目 | 内容 |
|------|------|
| **ルート** | `/login` |
| **コンポーネント** | `[R] Login` |
| **目的** | ユーザー認証 |
