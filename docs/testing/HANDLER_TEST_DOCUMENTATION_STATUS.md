# API ハンドラー・テストカバレッジ状況 (API Test Status)

> **目的**: ハンドラーテストの方針・カバレッジ状況を記録する。
> **読者**: 実装者・QA。
> **タイミング**: ハンドラーテスト方針の確認時。

> **Animal Ekarte**: バックエンドハンドラー（`backend/internal/handler/*_handler.go`・88 ファイル）の検証状態
> **最新更新**: 2026-07-11 | **全体カバレッジ**: backend 総計は CI の coverage-ratchet が自動計測・低下検知（baseline 89.9%、`backend/.coverage-baseline` / [coverage-policy.md](../coverage-policy.md) 参照）。handler パッケージ単体の内訳の実測には手動で `make test-cover` / `go test ./internal/handler/... -cover` が必要

---

## 1. 検証ステータス概要

本システムでは、HTTP エンドポイントに対して `testify/assert` を用いた結合テストを実施しています。

> 下表は主要カテゴリの代表的な内訳であり、ハンドラー総数(`*_handler.go` 88 ファイル)を網羅した集計ではない。

| カテゴリ | ハンドラー数(代表) | 検証状態 | 重点項目 |
|:---|:---|:---|:---|
| **コア業務** | 12 | ✅ 完了 | テナント隔離、FK依存、バリデーション。 |
| **会計・集計** | 8 | ✅ 完了 | 金額計算の精度、レコードロックの整合性。 |
| **LINE/Lステップ** | 6 | ✅ 完了 | API クライアントのモック、非同期発火の監査。 |
| **マスタ設定** | 5 | ✅ 完了 | 権限ガード、論理削除の安全性。 |

---

## 2. 共通テスト・ポリシー

全てのハンドラーテストは以下の 4 つの観点を必須項目として検証しています。

1.  **認可 (RBAC)**: 無権限ユーザーによる 403 エラーの発生。
2.  **テナント隔離**: 他院の ID 指定に対する 404/403 反応。
3.  **入力不備**: 不正な JSON 構造に対する `apperrors.WrapInvalidInput` の返却。
4.  **正常系リダイレクト**: 新規作成時の `Location` ヘッダーまたは ID 返却の正確性。

---

## 3. 今後の強化項目

- **負荷シミュレーション**: 大規模な会計集計クエリにおける、DB タイムアウトの挙動確認。
- **異常系カバレッジ**: 外部 API（Lステップ）が 503 を返した際のリトライ・フォールバック。

---

## 4. 実行コマンド

```bash
# 全バックエンドハンドラーのテストを実行
docker compose exec backend go test ./internal/handler/... -v -cover
```

---
