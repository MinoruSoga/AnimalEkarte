# MASTER: 医院マスタ - 院名の正式名称への修正

## 事象
シードデータ（`backend/migrations/002_seed_master.sql`）に登録されている医院名が正式名称と異なっている。

## 修正内容

| 現状 | 正式名称 |
|------|---------|
| 八王子院 | 八王子病院 |
| 城東医院 | 城東センター病院 |

## 対応方針
マスタデータ（シードSQL）を直接修正する。

- `backend/migrations/002_seed_master.sql` の `clinics` テーブル INSERT を修正
- ステージング・本番 DB に UPDATE 文を適用（スキーマ変更ではないため DB リセット不要）

## 対象ファイル
- `backend/migrations/002_seed_master.sql`
- 本番・ステージング DB への UPDATE 文（デプロイ時に適用）

## 期待動作
- 院名表示が正式名称に統一されること
- フロントエンドのすべての院名表示箇所（ドロップダウン、ヘッダー等）に反映されること
