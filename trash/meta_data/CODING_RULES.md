# 共通コーディング規約 (CODING_RULES)

本ドキュメントは、Animal Ekarte プロジェクト全体で遵守すべき共通のコーディング思想、命名規則、および開発プロセスを定義します。

技術スタックごとの具体的な実装詳細については、各ディレクトリの規約を参照してください。
- **[フロントエンド規約 (React/TS)](./frontend/CODING_RULES.md)**
- **[バックエンド規約 (Go)](./backend/CODING_RULES.md)**

---

## 1. 基本思想

### 1.1 型安全性最優先
- 動的な型（`any` 等）の使用を厳禁し、静的解析の恩恵を最大限に活用する。
- バックエンドのモデルを **Single Source of Truth** とし、フロントエンドの型は自動生成（`make codegen`）する。

### 1.2 SOLID原則と責務分離
- **単一責任の原則**: 1つの関数、コンポーネント、パッケージは1つの責務に集中させる。
- **依存性の逆転**: 抽象（インターフェース）に依存させ、具体的な実装（DB、外部API）を疎結合に保つ。

---

## 2. 命名・設計規約

### 2.1 言語別命名規則

| 対象 | TypeScript / React | Go | SQL |
|------|-------------------|----|-----|
| ファイル名 | `kebab-case.tsx` | `snake_case.go` | `001_xxx.sql` |
| 関数・変数 | `camelCase` | `camelCase` (非公開) | `snake_case` |
| エクスポート | `camelCase` / `PascalCase` | `PascalCase` | - |
| 型・構造体 | `PascalCase` | `PascalCase` | - |
| 定数 | `UPPER_SNAKE_CASE` | `UPPER_SNAKE_CASE` | - |

### 2.2 コメント方針
- 「何をしているか」ではなく**「なぜそうしているか（意図）」**を記述する。
- 複雑なビジネスロジックや計算式には、必ず背景説明を添える。

---

## 3. エラーハンドリングとロギング

### 3.1 共通原則
- エラーを握りつぶさない。必ず呼び出し元へ伝播させるか、ログ出力・ユーザー通知を行う。
- 構造化ログ（`slog` 等）を使用し、後から検索・分析可能な形式で出力する。

### 3.2 セキュリティ配慮
- ログに個人情報（氏名、電話番号等）や認証情報（パスワード、トークン）をそのまま出力しない。必要に応じてマスキングを行う。

---

## 4. 開発プロセス (Git)

### 4.1 コミットメッセージ
Conventional Commits 準拠を推奨。
- `feat`: 新機能
- `fix`: バグ修正
- `refactor`: 機能変更を伴わないコード整理
- `docs`: ドキュメント更新

### 4.2 ブランチ運用
- `main`: リリース・デプロイ用ブランチ。
- `develop`: 開発の統合ブランチ。
- `feature/*`: 各機能開発。

---

## 5. ドキュメントの維持

コードの変更が仕様に影響を与える場合、必ず対応する画面定義書（`docs/screens/*.md`）やアーキテクチャ図を同時に更新し、ドキュメントが常に「生きた設計図」である状態を維持すること。
