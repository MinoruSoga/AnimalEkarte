# AnimalEkarte 受入テスト バグ報告（Round 8）

- 実施日: 2026-08-24〜25（JST）
- 実施環境: ローカル（`http://localhost:3003`、Go/Ginバックエンド、seed `003_demo`）
- 対象: `docs/ops/testing/scenarios` 配下の業務フローシナリオ S01〜S13、および単体フォーム検証 V01〜V05（全9グループ・9並列エージェント + S04-06のみ1回リトライ）
- 起点: bug.md記載の全項目対応完了後の再受入テスト依頼

---

## 総括

Round 7で報告した全項目（BUG-001〜BUG-140台の一連の指摘、および前回の重大指摘だったBUG-001「一般ロールRBAC逸脱」）について、開発チームによる修正・調査がRound 8開始前に完了していた。今回はこれらの回帰有無の確認を主軸に、S01〜S13の業務フローシナリオとV01〜V05の単体フォーム検証を全て並列実施した。

最大の成果は、シフトデータの日付陳腐化という根本原因（`shift_entries`が過去のラウンドを跨ぐうちに固定日付が「本日」に対して古くなっていた問題）が開発チーム側で修正され（コミット `7d286ca71`, `99e3849a9`）、あわせて将来日付の陳腐化を防ぐCIリントゲート（`seed_future_horizon_lint_test.go`）が新設されたことで、S04（LIFF飼い主予約ジャーニー通し）とV05-6（フルブッキングフロー）の両方が**今回初めてエンドツーエンドで完走**した点である。またRound 7最大の懸念事項だったBUG-001「一般ロールでの確定済み会計への訂正操作が可能」は、**Round 7自身のテスト設定ミス（対象アカウントが実際は執行ロールだった）による誤検知**であったことが、真の一般ロールアカウント（`vet@example.com`）を用いた三重の裏取り（`/api/v1/me`でのロール確認・API 403・UI読み取り専用化）により確定した。V03のNEW-1（システム管理者が自分の医院をスタッフの所属医院から除外した際の挙動）も、開発チームの診断通り「システム管理者は継続アクセス可・非管理者は404」が意図した仕様であることが確認され、これも新規バグではなく解消済みの確認事項となった。

一方で、フルフローが初めて完走したことにより**新規の重大な不具合が2件**発見された。1つはカルテの予防接種タブで保存ボタンが偽陽性の成功トーストを表示しながら実際は一切データを永続化しない「サイレントデータロス」（BUG-001）、もう1つは一般診察・一般診察(再診)・健康診断・基本トリミングコース（courseId 1, 2, 7, 9）が全スタッフ・全期間にわたって予約不可能になっている空き枠計算バグ（BUG-002）である。後者はLIFF予約フロー（V05）と院内予約API検証（S04-06のリトライ）の2つの独立したエージェントが、互いの詳細を知らないまま同一の症状をそれぞれ発見・拡張確認しており、信頼度の高い指摘となっている。

このほか、検査一覧・カルテ内定期健診タブ・予防接種の次回予定日検証・薬剤マスタの単価検証など、中程度の重要性を持つ導線/検証漏れ系の指摘が複数件見つかっている。詳細は以下を参照。

**本ラウンドの実施環境について**: 9エージェントが同一ブラウザプロファイル・同一ログインセッション・同一DBを共有して並行稼働したため、セッション強制ログアウト、ログインアカウントの無断切替、タブの意図しない遷移・クローズ、DB状態の並行変化など、プロダクト起因ではない環境ノイズが頻発した。各エージェントはコード確認・ネットワークログ・API直叩きの複数手法で裏付けが取れたものだけを不具合として報告しており、単発かつ再現性の確認できない事象は「参考記録」として区別している。

---

## 深刻度サマリ

| 深刻度 | 件数 | ID |
|:--|:--|:--|
| 重大（Critical） | 1 | BUG-001 |
| 高（High） | 1 | BUG-002 |
| 中（Medium） | 4 | BUG-003, BUG-004, BUG-005, BUG-006 |
| 低（Low） | 2 | BUG-007, BUG-010（継続・環境要因） |
| 参考記録（未確定・要再検証） | 6 | 下記「参考記録」セクション参照 |

---

## 新規・継続オープン項目

### BUG-001（重大）カルテ内「予防接種」タブの保存が偽陽性の成功トーストを出し、実際はデータが一切永続化されない

- **シナリオ**: S03
- **概要**: カルテの予防接種タブで「保存」を押すと「保存しました」という成功トーストが表示されるが、実際には`fetch`/`XMLHttpRequest`とも一切のネットワークリクエストが発行されず、DBにも接種記録が作成されない。ユーザーは記録が保存されたと信じたまま、実際には未記録のまま放置される。
- **再現手順**:
  1. カルテの「予防接種」タブを開く（例: `/medical-records/1000000022?tab=予防接種`）
  2. 「記録を追加」→ワクチン名を選択→接種日を選択→「保存」をクリック
  3. 「保存しました」の成功トーストが表示される
  4. しかし `window.fetch` / `XMLHttpRequest.prototype.open` の両方にスパイを仕込んで確認すると、保存ボタンクリック後にネットワークリクエストが0件（3回再現、いずれも同結果）。`GET /api/v1/vaccinations?pet_id=X` でも `{"data":[],"total":0}`。ブラウザコンソールにもエラーは出力されない
  5. 対照実験: 同一カルテの「問診」タブで**同じ保存ボタン**を押すと `PATCH /api/v1/medical-records/:id/inquiries` が正しく発行され保存される → ページ全体の保存メカニズムは機能しており、予防接種タブ固有の不具合
- **影響範囲**: カルテ経由の新規予防接種登録機能全体。独立予防接種登録画面は本ラウンドでも到達不能（後述BUG-003関連）のため、現状これが予防接種を新規登録できる唯一のUI経路であり、その唯一の経路が機能していない。
- **付随所見**: この経路の接種日カレンダーで未来日が選択可能（disabledになっていない）ことも確認したが、独立画面が到達不能なため仕様との整合は未確定。

### BUG-002（高）一般診察／一般診察(再診)／健康診断／トリミングコース（courseId 1, 2, 7, 9）が全スタッフ・全期間で予約不可

- **シナリオ**: V05-6, S04（2エージェントによる独立クロス確認）
- **概要**: LIFF飼い主予約アプリで「一般診察」「一般診察(再診)」「健康診断」「トリミングコース」を選択すると、指名なし・スタッフ指名いずれでも予約可能期間（本日+29日間）の全日が「スタッフ不在」判定になり1日も予約できない。一方、同じスタッフでもワクチン接種・お手入れ・狂犬病・フィラリア予防・部分カット等の他コースは全期間予約可能。
- **証跡**:
  ```
  GET /api/liff/1/available-dates?courseId=1&staffId=1（林文明・一般診察）→ available:0/29, reason:"staff_off"（全日）
  GET /api/liff/1/available-dates?courseId=3&staffId=1（同一スタッフ・ワクチン接種）→ available:29/29
  ```
  courseId=1（一般診察）, 2（一般診察再診）, 7（健康診断）, 9（トリミングコース）の4コースが、スタッフ5名全員（さくら/林/山﨑/三井/ノア）で0/29（`staff_off`）となることを確認。他コースは同一スタッフで29/29。
- **原因の推測**: 同一スタッフの他コースは正常に予約可能なため、シフトデータ自体の欠落ではなく、**特定のコースID（1, 2, 7, 9）に紐づく空き枠計算ロジック側の不具合**である可能性が高い。表面的なコース属性（`duration_minutes`等）からは共通点が特定できていない。
- **影響**: 一般診察はもっとも基本的な受診区分であり、これがLIFF予約で1ヶ月近く予約不可能な状態は本番運用上重大。
- **検出経緯**: V05エージェントがLINE予約アプリのフルブッキングフロー実施中に発見（courseId 1, 2）。これと独立してS04-06のリトライエージンドがAPIレベルの網羅確認で同一事象を検出し、影響範囲がcourseId 7, 9にも及ぶことを追加確認した。

### BUG-003（中）`/examinations` の新規検査登録導線が実際の検査記録を作成しない

- **シナリオ**: S02（NEW-1）, V01 §7（NEW-2、URL直打ちパターンとして重複確認）
- **概要**: 検査一覧の「新規検査登録」から`/examinations/select-pet`でペットを選択すると、期待される「検査フォームで検査種別を選択」ではなく、カルテ新規作成の汎用フロー（`POST /reservations`→`POST /medical-records`）に遷移してしまう。`POST /api/v1/examinations` は一度も発行されない。遷移先のカルテ検査タブは「記録を追加」を押しても新規作成フォームではなく「検査取り込み」ダイアログ（既存記録のカルテへの取り込み用）が開くだけで、検査記録が0件のペットでは行き止まりになる。
- **関連事象（V01発見）**: 仕様書記載の正規URL `/examinations/new?petId=xxx` に直接アクセスすると「検査記録が見つかりません」というエラー画面が表示される。原因はルート定義（`clinical-care-routes.tsx`）に`/examinations`用の`new`パスリダイレクトガードが存在しないため（`/vaccinations`・`/hospitalization`には存在することを確認済み）。`:id`キャッチオールが`id="new"`として処理され編集モード扱いになり失敗する。
- **回避策（検証で使用）**: `POST /api/v1/examinations`をAPI直叩きすれば記録は作成でき、以後`/examinations/:id`の編集フォーム自体は正常に機能する。
- **推奨対応**: `/vaccinations`と同様に`{ path: "new", element: <Navigate to={paths.examinations.selectPet.getHref()} replace /> }`のガードを追加。加えて`/examinations/select-pet`の選択ボタンの遷移先を、検査モジュール専用の作成フローに向け直す必要がある。

### BUG-004（中〜高）カルテ内「定期健診」タブに動的フィールド機能が未実装

- **シナリオ**: V01 §4（NEW-1）
- **概要**: 健診種別マスタに動的フィールド定義（例: 尿比重・尿pH等）を持つ種別を選択しても、カルテ内の定期健診タブでは該当項目が一切表示されない。一方、独立登録フォーム（`/checkups/new`、V01 §9）では同一マスタデータで動的フィールドが正しく描画されることを確認しており、カルテタブ側の実装漏れであることが明確。
- **根拠**: `frontend/src/features/medical-records/components/CheckupsTab/CheckupsTab.tsx` に `checkup_type_fields` / `DynamicCheckupFields` 関連のコードが一切存在しない（該当import 0件）。対して `frontend/src/features/checkups/routes/CheckupForm.tsx` は正しくimport・renderしている。
- **副次観測**: 動的フィールド定義を作成・編集するAPI自体（POST/PATCH/PUT）が存在せず、`GET /masters/checkup-types/:id/fields`のみ実装されている。マスタ画面にもフィールド定義UIがなく、仕様書が想定する「動的フィールド定義付き種別を新規作成する」手順自体がプロダクト上実行不能な状態。

### BUG-005（中）予防接種の「次回予定日」が接種日以前でもサーバー側で拒否されない（クライアント検証のみ）

- **シナリオ**: V01 §8（NEW-3）
- **概要**: 次回予定日（`next_date`）が接種日（`date`）と同日、あるいは接種日より前でも、サーバーAPIは検証なしに200 OKで保存を受理する。クライアント側（`use-vaccination-form.ts` L238-243、「BUG-024」参照コメント付き）には検証ロジックが存在するが、対応するサーバー側実装（`vaccination_service.go`）が存在しない。
- **証跡**: `PATCH /api/v1/vaccinations/:id {date:"2026-08-24T09:00", next_date:"2026-08-20T09:00"}`（次回予定日が接種日の4日前）→ **200**で受理。
- **対比**: 接種日自体の未来日拒否（隣接するBUG-024）は本ラウンドでクライアント・サーバー双方に実装済みであることを確認済み。次回予定日の論理検証だけがクライアント限定のまま残っている。
- **推奨対応**: `vaccination_service.go`の`Create`/`Update`に`next_date != nil && !next_date.After(date)`のガードを追加。

### BUG-006（中）薬剤マスタ（medicine）に単価の非負検証がサーバー側に存在しない

- **シナリオ**: V04（NEW-1）
- **概要**: 診療項目マスタ（処置・予防接種）は`validateNonNegativePrice`で負の単価を拒否するが、薬剤マスタの作成・更新処理にはこの検証が実装されておらず、負の単価がそのまま保存できる。
- **証跡**: `POST /api/v1/masters/medicines {"name":"...", "price":-100}` → **201 Created**（拒否されず保存）。
- **根拠**: `backend/internal/medicalrecord/medicine_service.go`のCreate/Updateには`validateNonNegativePrice`の呼び出しがない一方、`procedure_service.go`・`vaccine_service.go`は同検証を明示的に呼んでいる。
- **影響**: 負の単価の薬剤が登録されると、処方・会計時の明細金額計算に負の値が混入するリスクがある。
- **推奨対応**: `medicine_service.go`のCreate/Updateに`validateNonNegativePrice(input.Price)`を追加（procedure/vaccineと同パターン）。

### BUG-007（低・要確認）レジ締めプレビューで休診日バナーとレジ締めフォームが同時表示される

- **シナリオ**: V02（NEW-1）
- **概要**: `/accounting/close`で休診日（検証時は日曜日=曜日ベースの定休日）をプレビューすると、「この日は休診日として設定されています」バナーが表示される一方で、レジ締め実行フォーム（実際のレジ現金入力欄・「締める」ボタン）もそのまま表示され続ける。仕様上は「休診日・締め済みの日はバナー表示となりフォームは出ない」が期待値。
- **確信度**: 低。検証したのは`clinic_holidays`テーブルに明示登録された祝日ではなく曜日ベースの定休日だったため、同一ロジック由来か別経路かが未確認。実害（実際に締め処理が実行できてしまうか）も未検証（確認ダイアログまでは到達するが、締め自体は取り消し不可のため実行していない）。
- **申し送り**: 次ラウンドで明示的な`clinic_holidays`登録日での再現確認を推奨。

### BUG-010（低・環境要因、継続）LIFF実トークン認証がローカル環境ではモック限定で検証不可

- **シナリオ**: V05-5, S12
- **概要**: `frontend/.env.local`の`VITE_LIFF_MOCK=true`により、実LINEアカウント連携（IDトークン検証・409/期限切れトークン等の実挙動）はローカル環境では検証できない。無効なトークンでもモックが約800ms後に「連携が完了しました」と成功表示する既知の制約も再確認した。
- **判定**: アプリの不具合ではなくローカル検証環境の制約。BE `LIFF_MOCK`経由またはステージング環境での実施が必要（Round 7から変化なし、継続監視項目として番号を維持）。

---

## 参考記録（未確定・低確信度・製品バグと断定しない事項）

以下は、確度が低い、単発で再現性が未確認、または環境ノイズ（9エージェント並行稼働によるセッション競合・DB競合）による見かけ上の事象である可能性が高いため、確定バグとしては起票せず参考情報として記録する。

| 項目 | シナリオ | 内容 |
|:--|:--|:--|
| 確認ダイアログのクリック/キーボード無反応 | S08 | 「精算済みの会計を修正します」確認ダイアログがクリック・Enter・Escapeいずれにも無反応。並行エージェントによるフォーカス奪取が原因の可能性が高く、単独環境での再現待ち |
| 同一ラベルのボタンが複数箇所に存在 | S08 | 「修正を保存する」ボタンがヘッダーとPaymentCard内部の2箇所に実装されている。実害未確認、将来のE2E要素特定リスクとして情報提供 |
| セッション共有によるロール切替の全体波及 | S13 | ログアウト操作が同一ブラウザ上の全タブ（他エージェント含む）に強制波及する。製品バグではなくテスト基盤上のリスクとして申し送り |
| owners検索で姓名複合語が0件ヒット | S13 | `/owners`検索ボックスで「田中光広」「田中 光広」等の姓名連結・スペース入り検索が0件になる事象。他エージェントの同時操作の影響を受けた可能性があり確度中。次ラウンドでの静かな環境での再現確認を推奨 |
| 退院済みボードビューのケージ表示 | S05 | 直近退院患者ではなく別の過去退院患者のカードが表示される事象を1回観測。並行書き込みの影響を排除できず、新規バグとしては起票せず観察事項として記録 |
| カルテ削除拒否時のエラーメッセージの文言差異 | S06 | 確定済みカルテの削除がAPI経由で正しく409拒否されることは確認したが、返却メッセージが仕様書記載の文言と異なる（「診療中の予約に紐づくカルテは削除できません」）。削除拒否という保証自体は成立しているため参考記録に留める |

---

## 今回確認できた修正事項（旧バグ再確認・回帰なし）

以下はRound 7以前に報告済みだった項目で、Round 8で修正確認または誤検知の訂正が取れたものである。番号は過去のbug.mdおよびコード内コメントの表記に準ずる。

- **旧BUG-001（Round 7の最重要指摘の訂正）**: 「一般ロールで確定済み会計への訂正操作が可能」という重大RBAC逸脱の報告は完全な誤検知であったことを確定した。Round 7が使用した`admin@example.com`は実際には**執行**ロール（`accounting-post-close-edit:edit`権限を正当に保持）であり、一般ロールではなかった。Round 8では真の一般ロールアカウント`vet@example.com`で再検証し、`/api/v1/me`でのロール確認・クレジット訂正API直叩きでの403・UI読み取り専用化の三重の証拠により、RBAC逸脱が存在しないことを確認した。認可コードの変更は行われていない。
- **旧BUG-002**: 受付カンバンの編集ボタンでメモのみを変更した際、誤ったend_time再構成や不要なスケジュール系フィールド送信が発生する不具合 → 修正確認PASS（フロントエンドの差分送信ロジック＋バックエンドの`shouldReevaluateReservationBookingConstraintsOnUpdate`による多重防御の両方を確認）。
- **旧BUG-003**: 休診日登録がサーバー側で検証されず、休診日への予約作成が通ってしまう不具合 → 修正確認PASS（医師指定あり/なし両経路で400拒否を確認）。
- **在庫フォーム（`/inventory/new`）関連の旧修正**: バリデーションフィードバックの欠如、ヘッダー「登録」ボタンの未束縛 → いずれも修正確認PASS。
- **予約ステータス変更時のカルテ必須ガード**: `checked_in→in_consultation`遷移時にカルテが存在しない場合409で拒否するガードについて、一見バイパスに見える事象（API直叩きで最初から`checked_in`状態で予約作成した場合）を精査した結果、作成時のベストエフォート自動カルテ生成の副作用によるものであり、実際のUI操作フロー（PATCH経由の遷移）では引き続きガードが正しく機能することを確認。新規リグレッションなし。
- **旧BUG-010（スタッフ所属医院のサイレントデータ消失）**: 修正確認を維持。
- **旧BUG-011（QAR5-1787312315230残留文字列）**: **解消済み（RESOLVED）**。前回のDB再構築（コミット `99e3849a9`）以降、汚染は再現していないことを複数フォームで確認。今回のbug.mdからは open 項目として除外する。
- **旧BUG-012（集計ダッシュボードのタイムアウト）**: 修正維持を確認（約2秒で描画完了、タイムアウト上限に対し十分な余裕）。
- **旧BUG-013（未請求明細APIのenvelope形状）**: `items`+`warnings`形状が正しく機能していることをAPIレベルで確認（フルE2Eは環境要因で未完走だが要素技術は健全）。
- **旧BUG-014（無効化された医院の「（無効）」ラベル表示）**: 修正維持を確認（新規テスト医院で再現検証）。
- **エラーメッセージの日本語フォールバック（コミット `1fd6260df`関連）**: 未マップ409の英語生文言、トリミング予約からのカルテ作成失敗時の英語トースト等、複数箇所でバックエンドの生英語メッセージがユーザーに露出せず日本語フォールバックに変換されることを確認。
- **旧BUG-023（権限グループ重複名トースト）**: 修正維持を確認（構造化エラー`permission_group_name_conflict`+実グループ名を含むレスポンスを確認）。
- **旧BUG-024（未来日ワクチン接種の登録拒否）**: 修正確認（クライアント・サーバー双方で拒否を確認。BUG-005で報告した次回予定日の検証漏れとは別の項目）。
- **旧BUG-031（ログイン済み状態でのセッション復元）**: 正常動作を確認。
- **旧BUG-037（入院登録フォームのケージ・個室必須ガード）**: 健在を確認。
- **旧BUG-128（クリニック切替がURLパスではなくX-Clinic-IDヘッダー経由という設計）**: 意図した仕様であることを再確認。V04エージェントが素の`fetch()`でヘッダーを省略し一時「clinic_idが無視される重大バグ」と誤認したが、正しいヘッダー付きで再検証した結果アプリケーションは正常動作。テスト手法の誤りであり製品バグではない。
- **支払方法マスタの重複登録エラー（識別子空文字パターン）**: バックエンドの汎用エラーが空の識別子を含む形で返る実装の粗さはあるが、フロントエンドの`localizeAlreadyExistsMessage`が検知して汎用日本語文言にフォールバックするため、ユーザー影響はないことを確認。
- **旧BUG-140（権限グループの自己剥奪ガード）**: 修正維持を確認。
- **V03 NEW-1（Round 7最大の未確定事項の解消）**: システム管理者が自分の医院をスタッフの所属医院リストから除外した後もGET/PATCH/PUT(clinics)/DELETEすべてに継続してアクセスできる一方、非管理者は同条件で404になるという挙動は、開発チームの診断通り**意図した仕様**であることをRound 8で確定（CONFIRMED）。system admin側はライブAPIで完全再現・確認。非admin側はセッション競合リスクを避けライブ検証を見送ったが、専用回帰テスト（`staff_admin_removed_request_clinic_test.go`、671行、コミット`8fbfc6661`で新規追加）が両ケースを網羅的にアサートしていることをソースコードで確認した。新規バグではなく、Round 7の指摘は解消済みとして記録を終了する。
- **職種（occupation）FKロックの新設修正**: スタッフの残存所属医院に存在しない職種IDを設定しようとすると404で拒否されることを確認。正しく機能している。
- **パスワード変更ダイアログの現在パスワード欄クリア問題**: 修正確認（BE 400時に現在パスワード欄がクリアされる非対称挙動は解消）。

---

## 未実施・要フォローアップ項目

- **S13手順7**: 「view権限のみ許可・edit権限なし」という中間ケースを検証できる実アカウントが存在しない。依頼された4アカウント（vet/nurse/reception/trimmer@example.com）はいずれも`identity-links:view`が未付与であり、`viewer@example.com`も同権限を持たない。専用テストアカウントの用意が必要。
- **S09**: 締め時刻境界値（09:00, 13:30, 19:00, 日曜18:00ちょうど）のDBレベルテストデータ投入による判定検証が、`psql`/`docker`が環境に存在しないため引き続きブロック中。城東センター病院固有の締め設定との直接突合も、使用可能アカウントが同院未所属のため未実施（次ラウンドは`joto-vet@example.com`の使用を推奨）。
- **post_close_reason自動フォーカス修正（コミット `1fd6260df`）**: コード上の実装（`useEffect`での`formState.focusTarget`監視、`focus()`+`scrollIntoView`）は確認できたが、(a)通常操作ではクライアント側バリデーションにより400エラー自体が発生せず発火条件を再現できない、(b)確認ダイアログの無反応（参考記録参照）により最終確認が阻めた。次ラウンドでは意図的に400を誘発した上での再検証を推奨。
- **S06 A2**: 編集権限を持たないロールでの確定済み/作成中カルテ閲覧は、別ロールへの再ログインが必要で時間都合により未実施。
- **V01 §5・§6・§11 item2、FIELD-LEVEL-PROTOCOL.md**: セッション競合・タブ不安定化により時間切れで未実施/部分実施のまま。
- **V02 §2〜§4, §6, §7残項目, §10, §11残項目**: 時間都合により未実施。
- **V05-8〜V05-18**: LINE予約設定・Lステップ連携の個別フォーム検証群。V05-6のフルフロー完走とBUG-002の原因切り分けに時間を要したため未着手。次ラウンドでの優先実施を推奨。
- **S11・S13のフルE2E**: 並行負荷によるDOM参照失効・画面リロード頻発のため、いずれも今回も完走に至らず。要素技術（BUG-013 envelope等）はAPIレベルで健全性を確認済み。静かな時間帯での再試行、または専用シードデータでの実施を推奨。
- **S04/S05のキーボードドラッグ&ドロップ実地検証**: 本セッションのブラウザ自動化ツールでtrusted keyboard/pointerイベントがページに到達しない制約を統制実験で確認（アプリ側の実装は健在と判断）。次ラウンドで手動または別ツールでの再確認を推奨。

### クリーンアップ未完了の残存データ

- カルテ id=1000000017（下書き状態のダミーカルテ、V02検証で自動生成。`DELETE`が404で削除不可、削除APIが存在しない可能性）
- カルテ id=1000000022（さくら/pet_id=1000024、BUG-001検証で作成。予防接種の保存自体が失敗しているためワクチンデータは残っていないが、カルテ本体（draft状態）は残存）
- ブラウザタブ `/vaccinations/1000000008`（V01検証時に`beforeunload`ダイアログでロックされ、複数の回避策を試したが閉じきれず。次回セッションでの強制クローズを推奨）

---

## 環境上の注意

- 本ラウンドは9エージェントが同一ブラウザプロファイル・同一ログインセッション・同一DBを共有して並行稼働した。認証がブラウザ全体で共有されるCookieセッションであり、タブ単位の分離がないため、いずれかのタブでのログアウト操作が全並行セッションに強制波及する事象を複数エージェントが独立して観測している。次回以降、ロール切替を伴う検証は専用ブラウザプロファイル/トークンベース認証など、セッションが競合しない方式の検討を推奨する。
- DB状態の並行変化（他エージェントによる検証データの作成・削除・クリーンアップ）により、一部の観察事項（在庫アイテムの消失、退院済みボードのケージ表示等）は製品バグか環境ノイズか判別がつかなかった。いずれも確定的な証拠が得られなかったため参考記録に留めている。
- S09の締め時刻境界値検証など、DBへの直接アクセス手段（psql/docker等）が必要な項目は本環境では引き続き検証不可。

---

## 付録: 今回使用した主なテストアカウント

| メールアドレス | 表示名 | ロール/権限 | 備考 |
|:--|:--|:--|:--|
| `admin@noavet.jp` / `password1` | ノア | システム管理者・全クリニック | メイン検証アカウント |
| `admin@example.com` | 安田 希恵 | **執行**・八王子病院 | Round 7がRound誤って一般ロールとして扱い誤検知の原因となったアカウント。正しくは執行ロール |
| `vet@example.com` / `password` | 倉田 春香 | 一般・`accounting-post-close-edit:{edit:false}` 等 | 真の一般ロール検証に使用（BUG-001誤検知の訂正、S13手順7/8） |
| `viewer@example.com` / `password` | 閲覧 太郎 | 一般・`examinations:{view:true,create:false,edit:false,delete:false}` | 真の閲覧専用アカウント。`identity-links:view`は持たないため中間ケース検証には引き続き使用不可 |
| `joto-vet@example.com` | 城東 獣医 | 執行・城東センター病院 | 未使用。S09の城東センター病院固有設定検証に次ラウンドでの使用を推奨 |

デモアカウントの既定パスワードは`admin@noavet.jp`（`password1`）を除き全て`password`。
