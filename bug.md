# AnimalEkarte 受入テスト バグ報告（再検証）

実施日: 2026-08-09
実施環境: http://localhost:3003 （ローカル・seed 003_demo、スタッフ「ノア」システム管理者、八王子病院）
対象範囲: `docs/ops/testing/scenarios` 配下 S01〜S12（業務フローシナリオ）、V01〜V05（個別フォーム検証、計84フォーム）

**本レポートについて**: 前回の受入テスト（BUG-001〜BUG-032、計32件）で発見されたバグはすべて対応済みとのことで、本レポートはS01〜S12・V01〜V05の受入テストを一から再実施した結果である。前回検出済みのバグの再発有無も含めて確認し、新たに発見された問題（再発・新規いずれも）をここに記載する。番号は本レポート内で新規に振り直したもの（BUG-001〜）であり、前回レポートの番号とは対応しない。各項目の「備考」に前回バグとの関連がある場合はその旨を明記している。

**重要な申し送り事項（未検証範囲）**: S05（入院サイクル）はブラウザ環境の障害（後述）により**全く着手できなかった**。S04（LIFF予約ジャーニー）も手順1相当のみで大部分が未実施。**S06（カルテロック・監査証跡）については、過去に報告された最重要級のバグ「カルテ確定時、入力内容が保存されず身体検査所見が空文字列に、診断詳細・治療方針が固定文字列で上書きされる」という不具合の再発有無を、環境障害により最終確認（API応答での裏取り）できなかった**。UI操作自体は正常に見えたが、これは「バグなし」を意味しない。次回最優先で確認が必要。詳細は該当セクション参照。

---

## BUG-001: 死亡登録されたペットに対する新規会計作成が、URL直接アクセスで物理ブロックを完全に迂回でき、実際に金銭取引が確定してしまう【最重要】

- **重大度**: Critical（最重要）
- **発見シナリオ**: S01 手順4（新規会計）
- **再現手順**:
  1. ペットを死亡登録する（飼主編集画面→死亡ダイアログで死亡日・理由を入力し保存）
  2. 会計新規作成のペット選択画面で当該ペットを検索する → 仕様通り「選択不可」でグレーアウトされ選択できないことを確認（ここは正常）
  3. しかしブラウザのアドレスバーに直接 `/accounting/new?petId=<死亡ペットのID>` を入力して遷移すると、ペット選択画面を経由せずに通常どおり操作可能な会計新規作成フォームがエラー表示なく完全にレンダリングされる
  4. そのまま診療項目を追加して確定操作を実施したところ、`POST /api/v1/accountings/complete` が**201**で成功し、実際に¥1,100の会計レコード（accounting id 1384142）が `status: "completed"` として確定登録された（`GET`で再確認済み）
- **期待結果**: 死亡ペットへの新規会計作成はペット選択画面だけでなく物理的（バックエンド含む）にブロックされること
- **実際の結果**: ペット選択画面上のガードのみが存在し、URLを直接指定するだけで完全に迂回可能。バックエンド側には死亡ペットに対する会計作成を拒否するガードが一切存在せず、実際に金銭取引が死亡したペットに対して確定登録されてしまった
- **備考**: 入院登録（`/hospitalization/new?petId=X`）は直接URLアクセス時にも「死亡したペットは入院登録できません」と正しくブロックされる実装になっており、これが正しい参照実装。会計・カルテ（BUG-002）にも同様のバックエンドガードを追加すべき。作成された accounting id 1384142 は監査証跡として意図的に削除せず残置。

---

## BUG-002: 死亡登録されたペットに対する新規カルテ作成も、URL直接アクセスでバックエンドガードなしに成立してしまう

- **重大度**: High
- **発見シナリオ**: S01 手順3（新規カルテ）
- **再現手順**:
  1. 死亡登録済みペットに対し、カルテ新規作成のペット選択画面で検索 → 仕様通り「選択不可」（正常）
  2. `/medical-records/new?petId=<死亡ペットのID>` に直接アクセス → ペット選択画面を経由せずカルテ新規作成フォームがエラーなくレンダリングされ、ドラフトレコード自動作成の`POST /api/v1/medical-records`も成立（201、`/medical-records/{id}`へリダイレクト）
- **期待結果**: 死亡ペットへの新規カルテ作成が物理的にブロックされること（臨床安全ガード）
- **実際の結果**: フロントのペット選択画面のガードのみに依存しており、URL直叩きで完全に迂回可能。バックエンドにも拒否ガードが存在しない
- **備考**: BUG-001と同一の設計欠陥パターン。入院登録の実装（バックエンド側でも明示的にエラーメッセージを返しAPIコール自体を発行しない）を横展開すべき。

---

## BUG-003: 死亡理由がバックエンドから再取得できず、死亡登録済みペットの編集画面再オープン時に確認できない可能性がある

- **重大度**: Medium
- **発見シナリオ**: S01 異常系A2
- **再現手順**: 死亡登録時に死亡理由を入力して保存後、`GET /api/v1/pets/{id}` のレスポンスを確認
- **期待結果**: 死亡日・理由が保持されて表示される（レスポンスに理由フィールドが含まれる）
- **実際の結果**: `GET /api/v1/pets/{id}` のレスポンスJSONに死亡理由に相当するフィールドが一切含まれない（ステータス変更は反映されるが理由テキストのみ欠落＝実質書き込み専用）
- **備考**: UI側が別エンドポイント経由で理由を取得している可能性はあるが、標準のペット取得APIから理由が欠落している点は、監査・レポート等の他機能からの参照時にも同様の欠落を招くリスクがある。

---

## BUG-004: 予防接種登録フォームの接種日が当日デフォルトにならず空欄で開く（再発）

- **重大度**: Minor
- **発見シナリオ**: S03 手順1
- **再現手順**: `/vaccinations/new?petId=X` を開く（2回、異なるワクチンで再現）
- **期待結果**: 実施日のデフォルトが当日である
- **実際の結果**: 接種日フィールドは常に空欄で開き、当日日付が自動セットされない
- **備考**: 前回レポートで報告済みの軽微な不具合の再発（未修正）。

---

## BUG-005: ワクチン次回予定日を手動上書きすると、間隔セレクタ表示とDB上のデータが数学的に矛盾したまま永続化される（再発・深刻化）

- **重大度**: Minor〜Medium（表示不整合に加えDBレベルの不整合を新たに確認）
- **発見シナリオ**: S03 手順4→5（標準間隔「1年後」選択→次回予定日を手動で別日に上書き）
- **再現手順**:
  1. 接種日2026/08/10、次回の予定「1年後」→次回予定日が正しく2027/08/10に自動計算される（正常）
  2. カレンダーから直接2027/08/20に手動上書き
  3. 上書き後も間隔セレクタの表示は「1年後」のまま変化しない（「以外（手動）」に切り替わらない）
  4. この状態で保存すると201で成功し、`GET /api/v1/vaccinations?pet_id=X`で確認したところ、`next_schedule_type: "1year"` かつ `next_date: "2027-08-20"`という数学的に矛盾した組み合わせがそのまま永続化されている
  5. 再オープンしてもリロード後も同じ矛盾した表示が再現（一時的なUI状態のバグではない）
- **期待結果**: 手動調整時は「以外（手動）」等、選択中の間隔と実際の日付の食い違いが分かる形で保持されるべき
- **実際の結果**: 前回報告済みの表示不整合の再発に加え、バックエンドのデータそのもの（`next_schedule_type`フィールド）に矛盾が保存されることが新たに判明（当初想定より深刻）
- **備考**: 検証用データ（vaccination id 1091851）は削除して原状回復済み。

---

## BUG-006: 軽減税率（8%）品目を含む会計が、フロントエンドの税額集計誤り（10%固定計算）により確定不能になる【最重要】

- **重大度**: Critical
- **発見シナリオ**: S08確認観点／S10手順（新規会計での手動明細追加）
- **再現手順**:
  1. 会計に軽減税率8%対象の品目（例:「ヒルズ z/d 2kg」¥5,800）を追加する。明細行では正しく小計¥5,800→税額¥464→合計¥6,264と表示される
  2. ページ下部のフッター合計（消費税・合計）・「今回の請求金額」ヘッダーを確認する
  3. フロントエンド表示の合計額（¥6,380＝10%換算）で確定を試みる → バックエンドが正しく「支払い内訳の合計が請求金額と一致しません」で拒否
  4. バックエンドの正解額（¥6,264）を入力して再度確定を試みる → フロントの残額計算が誤ったまま「残り¥116未入力」となり確定ボタンがdisabledのまま
- **期待結果**: 明細の実税率を正しく積み上げた合計金額が一貫して表示され、その金額で確定できる
- **実際の結果**: フッターのサマリが明細の実税率（8%）を無視して一律10%換算で計算されており、フロント表示額・バックエンド正解額のいずれを入力しても確定が成立しない**行き止まり**が発生する。軽減税率対象品目（フード等）を扱う実運用（動物病院では一般的）で会計精算そのものが機能しなくなる
- **備考**: `calculateBillingTotals`という共有計算ロジックのフッターサマリ側の呼び出しで税率の扱いに差異がある可能性が高い。S10側でも同一症状を独立して確認しており（BUG-013参照）、根本原因は共通と推測される。破損した確定済みレコードは残っていない。

---

## BUG-007: クレジット訂正により生じる未収金（差額）がどの画面からも追跡できない

- **重大度**: High
- **発見シナリオ**: S08 #3〜#4、確認観点「未収金の定義整合」
- **再現手順**:
  1. カード全額精算・確定済みの会計（合計¥1,100）に対しクレジット訂正を実行し、カード金額を¥1,100→¥900（後に¥1,000へ再訂正）に減額する
  2. 訂正後、会計一覧・会計詳細・未納者一覧（`?tab=unpaid`、飼主単位／会計単位）を確認する
- **期待結果**: 訂正によって生じた差額（未収金）が未納者一覧や会計詳細のいずれかに一貫して表示される
- **実際の結果**: 会計ステータスは訂正後も「精算済み」のまま、`total_amount`も不変。訂正後の支払額との差額が生じているにもかかわらず、未納者一覧（飼主単位・会計単位とも）は当該期間で¥0件のまま表示され、当該会計は一切ヒットしない。会計詳細にも専用の「未納残高」表示は存在しない
- **備考**: スタッフ向けの未収金把握UIから、クレジット訂正由来の実在する未収金が完全に不可視になっている。部分入金経路（S08 #6〜9、シナリオ自身がUI到達不能と明記）とは別の経路。

---

## BUG-008: クレジット訂正ダイアログで理由未入力時、アプリ独自のエラーメッセージが一切表示されない

- **重大度**: Low
- **発見シナリオ**: S08 #2 / V02 §3
- **再現手順**: 確定済み会計のクレジット訂正ダイアログで「訂正理由」欄を空欄のまま「訂正する」をクリック
- **期待結果**: 「訂正理由を入力してください」等のトーストが表示され送信がブロックされる
- **実際の結果**: 送信はブロックされる（POSTは発生しない）が、アプリ独自のエラーメッセージが一切表示されない
- **備考**: `<Textarea required />` 等のネイティブHTML5バリデーション属性が、意図されたJS側バリデーション/トーストより先にフォーム送信をインターセプトしてしまっていることが原因と特定済み（`CreditCorrectionDialog.tsx`）。`required`/`min`属性を外すか`onInvalid`でネイティブバリデーションを抑止する対応を推奨。

---

## BUG-009: 確定済み会計の明細編集が締め状態に関わらず一律拒否され、「修正理由必須」の警告フローがUI上に存在しない

- **重大度**: Medium〜High
- **発見シナリオ**: S09 #8
- **再現手順**:
  1. 締め済み・締め前それぞれのケースで、確定済み会計の明細（割引欄等）を変更しようとする
  2. いずれも同一のトースト「確定済みまたは取消済みの会計明細は更新できません」で即座に拒否される
  3. `PATCH /api/v1/billing-items/{id}` は**409**を返しており、レジ締め状態と無関係に「status=completed の会計明細は一律更新不可」というサーバー制約であることを確認
- **期待結果**: 締め済み期間の会計を通常編集で再保存する場合は「レジ締め済み期間の編集」警告と修正理由（必須）の入力欄が表示され、理由入力後に保存すれば監査ログに記録される（仕様正本#115・#189）
- **実際の結果**: 想定される「警告＋修正理由入力欄」のUIがどこにも出現せず、現金決済の明細金額を締め後に理由付きで修正する経路がUI上に存在しない。唯一機能する訂正手段はカード系決済限定の「クレジット訂正」のみ
- **備考**: 仕様正本が想定する機能が未実装、または別の未発見の導線が存在する可能性がある。執行ロールでの通常操作範囲内では到達できないことを確認した。

---

## BUG-010: グローバル選択クリニックと異なるクリニックで飼主を新規登録すると、登録直後の画面遷移がクラッシュする

- **重大度**: Medium
- **発見シナリオ**: S09（データ準備中に発見）
- **再現手順**:
  1. グローバルクリニック選択が「八王子病院」の状態で、飼主新規登録フォームの「登録先医院」だけを別のクリニックに変更して登録
  2. `POST /api/v1/owners`は201で成功するが、直後の遷移先`GET /api/v1/owners/{id}`が404を返し、汎用エラー画面「予期しないエラーが発生しました」が全画面表示される
- **期待結果**: 登録成功後、作成した飼主の詳細画面へ正常に遷移する
- **実際の結果**: リダイレクト時のAPIリクエストがグローバル選択クリニックのX-Clinic-IDを使い続けるため、作成先クリニックと不一致で404になりクラッシュ画面が出る
- **備考**: 回避策あり（事前にグローバルクリニックを合わせるか、エラー後に手動切替すればデータ自体は正しく作成されている）。フォームが異なるクリニックへの登録を許可する以上、遷移先もそれに追従するか404を分かりやすくハンドリングすべき。

---

## BUG-011: トリミング＋診察の統合会計で、決済確定APIが医療記録由来の明細を含むと必ず400エラーになる【最重要】

- **重大度**: Critical
- **発見シナリオ**: S11 手順7（会計を確定する）
- **再現手順**:
  1. 同日同ペットでトリミング予約と一般診察をそれぞれ会計待ちまで進める
  2. `/accounting/new?petId=X`を開くと統合された明細（診察＋トリミング）が正しく表示される（読み取り側は正常、旧BUG-013の500エラーは解消済み）
  3. 支払方法を設定して「会計を確定する」を押す → `POST /api/v1/accountings/complete`のリクエストボディに**`medical_record_id`が一切含まれていない**
  4. サーバーは**400 `{"error":"参照先の組み合わせが正しくありません"}`**を返し確定できない
  5. 切り分け: トリミング明細のみ→201成功。診察明細のみ（トリミングなし）→同じ400エラー。**医療記録由来の明細を1件でも含む新規会計確定は常に失敗する**ことを確認
- **期待結果**: 201で会計が確定し「精算済」になること
- **実際の結果**: フロントが`completeAccounting`呼び出し時に`medical_record_id`を送信せず、バックエンドの`ValidateCreateReferences`が`treatment_id`付き明細に対し`medical_record_id`必須のチェックで無条件拒否する
- **備考**: コード確認済み（`frontend/src/features/accounting/hooks/use-accounting-completion-action.ts`、`backend/internal/billing/billing_item_repository.go`約197-208行）。S11の統合会計に限らず、新規会計確定の主経路（`/accounting/new?petId=X`）で診察由来の明細を含む場合に広く再現する可能性が高い。

---

## BUG-012: 集計ダッシュボードの「来院回数」「最終来院」タブが実質常に0件になる

- **重大度**: High
- **発見シナリオ**: S10 手順4〜6周辺
- **再現手順**: `/aggregation`で「来院回数」「最終来院」タブを開く。`include_zero`パラメータなしでは`total=0`だが、付与すると数千件返る
- **期待結果**: 該当期間・条件に該当するオーナーが一覧表示される
- **実際の結果**: `annual_amount=0`のオーナーが一律除外されるロジック（`ltv_repository.go`）が全クエリタイプに無条件適用されている一方、「0円を含む」チェックボックスは売上ランキングタブにしかUI上存在せず、来院回数・最終来院タブではデフォルトも`include_zero: true`になっていない。ユーザーがこれらのタブで0円を含める手段が皆無
- **備考**: 現行seedの偏り（2026年の完了会計が少ない）が症状を顕在化させているが、根本原因はコードロジックの結合不備。

---

## BUG-013: 8%（軽減税率）明細を含む会計で、集計/会計画面のフッター合計が10%固定で計算され金額不一致になる

- **重大度**: Medium〜High
- **発見シナリオ**: S10（新規会計での手動明細追加）
- **再現手順**: 軽減税率対象の明細（例:「ヒルズ z/d 2kg」¥5,800）を追加する
- **期待結果**: 明細ごとの税率どおりに小計・消費税・合計が計算され、明細合計とフッター請求額が一致する
- **実際の結果**: 明細行自体は8%として正しいが、フッターのサマリが10%固定で計算されており¥116の不一致が発生。決済確定がブロックされ得る
- **備考**: BUG-006と同一症状・同一根本原因の可能性が高い（独立して2つのシナリオから発見されたため、影響範囲の広さを示す傍証として両方記載する）。

---

## BUG-014: LIFFペットヘルスページで`clinic_id`欠落時、専用のエラー文言ではなく「しばらくして再試行」という誤解を招く汎用文言が表示される

- **重大度**: Low〜Medium
- **発見シナリオ**: S12 手順8
- **再現手順**: `http://localhost:3003/liff/`（`clinic_id`クエリなし）を開く
- **期待結果**: 「クリニックIDが見つかりません」系の専用エラーが表示される
- **実際の結果**: 「データ取得に失敗しました。しばらく経ってから再度お試しください。」という一時障害を示唆する汎用文言＋「再試行」ボタンが表示される。専用文言は一切出ない
- **備考**: `PetHealthPage.tsx`が投げる`Error('クリニックIDが見つかりません')`に`.status`が無いため、共通エラーハンドラ`handle-fetch-error.ts`が元のメッセージを無視して汎用文言で上書きしてしまう。`clinic_id`欠落は再試行しても解決しない恒久的な問題であるため、誤ったユーザー誘導になる。

---

## BUG-015: カルテ内予防接種タブで、ワクチン未選択のまま追加を試みると無音失敗する

- **重大度**: Medium
- **発見シナリオ**: V01 §5-1
- **再現手順**: 既存カルテの「予防接種」タブでワクチン未選択のまま「接種記録を追加」相当のボタンを押す
- **期待結果**: 「ワクチン種別を選択してください」等のインラインエラーが表示され保存されない
- **実際の結果**: 画面上に一切のエラー文言・トーストが表示されず、APIへのPOSTも発行されない（無音失敗）。独立画面（予防接種フォーム単体）では正しくエラーが表示されるため、カルテ内タブのみ挙動が異なる
- **備考**: シナリオ本文で明記されている「無音失敗にならないこと」という懸念がまさに的中した形。

---

## BUG-016: カルテの見積タブで、初回保存後の2回目以降の保存が偽の成功トーストを出しながら実際には送信されず、変更内容がサイレントに失われる

- **重大度**: High
- **発見シナリオ**: V01 §6-7
- **再現手順**:
  1. カルテの見積タブで件名・明細を入力しメイン保存 → `POST /api/v1/estimates`が201で成功（旧BUG-011の409エラーは再現せず、修正確認）
  2. 見積書件名を手動で書き換え、再度メイン保存 → 画面上には成功トーストが表示される
  3. `read_network_requests`で確認すると`/api/`宛のリクエストが一切発生していない（0件）
  4. `fetch`でDB実値を確認 → タイトルは最初の値のまま、`updated_at`も更新されていない
  5. ページを完全リロードしても手動更新した内容は反映されていない
- **期待結果**: メイン保存のたびに変更内容が永続化され、成功トーストの表示とDBの実際の保存状態が一致すること
- **実際の結果**: 2回目以降のメイン保存では偽の成功トーストが表示されるにもかかわらず実際にはAPIリクエストが発行されず、変更内容がサイレントに失われる
- **備考**: 旧BUG-011（409エラーで保存不可）という具体的症状は解消されているが、同じ見積タブのメイン保存経路に症状の異なる新しい欠陥が存在する。会計・保険請求に関わる情報の消失につながるため重大度は高い。

---

## BUG-017: カルテ本体で存在しないIDへの直接URLアクセス時、エラー画面ではなく完全な白紙画面が表示される

- **重大度**: Medium
- **発見シナリオ**: V01 §1-7（C3-3）
- **再現手順**: `/medical-records/999999999`（存在しないID）に直接アクセスする
- **期待結果**: 「カルテが見つかりません」等のエラー画面が表示される（検査・予防接種・入院・トリミングの各独立フォームは修正済み・正しくエラー表示されることを確認済み）
- **実際の結果**: サイドバー・ヘッダー（タイトルのみ）は表示されるが、メインコンテンツ領域が完全に空白。`GET`は404を返しているが、フロントがこの404を捕捉してエラー画面を出す処理をしていない
- **備考**: 検査・予防接種・入院・トリミングの独立フォームでの同種の不具合（旧BUG-016）は修正済みだが、カルテ本体には同じ修正が適用されていない。

---

## BUG-018: クレジット訂正ダイアログの入力エラー時、アプリ独自のエラーメッセージ（トースト）が表示されない

- **重大度**: Medium
- **発見シナリオ**: V02 §3（C1 入力バリデーション）
- **再現手順**: カード払い完了済み会計のクレジット訂正ダイアログで「訂正理由」欄を空欄のまま「訂正する」をクリック
- **期待結果**: 「訂正理由を入力してください」等のトーストが表示され送信がブロックされる
- **実際の結果**: 送信は正しくブロックされるが、アプリ側のトーストが表示されずユーザーには無反応に見える
- **備考**: BUG-008（S08側で発見）と同一事象。ネイティブHTML5バリデーション属性（`required`/`min`）がJS側バリデーションより先にフォーム送信をインターセプトしていることが原因。

---

## BUG-019: 飼主の電話番号重複時の409エラーメッセージが英日混在の壊れた文字列になる

- **重大度**: Low〜Medium
- **発見シナリオ**: V02（飼主新規登録フォームでの重複バリデーション）/ V03 §1手順7
- **再現手順**: `/owners/new`で既存飼主と同じ電話番号を入力して登録する
- **期待結果**: 「この電話番号はすでに登録されています」等、自然な日本語のエラーメッセージがトースト表示される
- **実際の結果**: `owner 'この電話番号はすでに登録されています' already exists`という、英語テンプレートに日本語文がそのまま埋め込まれた不自然な文字列が表示される
- **備考**: `backend/internal/owner/service_core.go`で`apperrors.WrapAlreadyExists("owner", "<日本語完成文>")`と、既に日本語の完成文を`identifier`引数として渡しているが、`WrapAlreadyExists`は`"%s '%s' already exists"`という英語専用テンプレートのため二重に組み合わさって壊れる。V03側ではメールアドレス重複でも同一パターンを確認（BUG-024参照）。

---

## BUG-020: 予約カレンダー詳細モーダルからのステータス変更（特にキャンセル）に確認ダイアログが無く、担当医不在時に不要な409エラーが発生する

- **重大度**: High
- **発見シナリオ**: V02 §7 予約モーダル ステータス変更
- **再現手順**:
  1. `/reservations`カレンダーで既存予約の詳細モーダルを開き、ステータスのドロップダウンから「キャンセル」等を選択する
  2. 選択した瞬間、確認なしで即座にAPIが呼ばれる
  3. このモーダル経由の更新は`start_time`/`end_time`/`doctor_id`等を含むフルペイロードを再送するため、該当時間帯に担当医が不在だと409 Conflictで失敗する（ステータスのみの変更であり日時・担当医に変更はないにもかかわらず）
- **期待結果**: 少なくともキャンセルのような破壊的操作には確認ダイアログが表示される。ステータスのみの変更であれば日時・担当医の再検証は行われない
- **実際の結果**: 確認ダイアログが無く、かつ不要な409エラーが発生しうる
- **備考**: 受付カンバン側の同機能（`update-appointment-status.ts`）は`{status}`のみを送る最小ペイロードで正しく動作しており、バグは予約カレンダー側のモーダル実装（`use-reservation-actions.ts`）に限定される。

---

## BUG-021（旧BUG-018の残存）: レジ締め済み期間の会計への明細追加が、修正理由欄を入力してもUI上から一切成功しない

- **重大度**: High
- **発見シナリオ**: V02 §2×§5 / S09 #8関連
- **再現手順**:
  1. レジ締め済みの会計を開くと「修正理由（必須）」入力欄が表示される
  2. 理由欄を空欄のまま明細追加すると400エラー「post_close_reason の入力が必要です」（想定通り）
  3. 理由欄に文字列を入力した**上で**再度同じ操作を行っても、状況が変わらず同じ400エラーが再発する。理由欄の入力内容が明細追加のリクエストに一切反映されていない
- **期待結果**: 理由欄に入力した内容が明細追加リクエストに含まれ、締め後編集として明細が正常に追加される
- **実際の結果**: UIからレジ締め済み期間の会計に明細を追加する手段が実質的に存在しない
- **備考**: `AccountingDetail.tsx`が管理する`postCloseReason`は確定アクションにのみ渡され、明細追加を担う`useAccountingItemActions`にはそもそも`postCloseReason`を受け取るパラメータが存在しない。バックエンド側は`PATCH /accountings/{id}`に`post_close_reason`+`is_post_close:true`を付与すれば200で成功することを確認済みで、フロントの配線不備と特定。旧BUG-018の「金額と明細が不整合なレコードが残る」というデータ不整合自体は解消されているが、機能としては依然使用不能。

---

## BUG-022: 新規飼主登録フローでpending追加したペットの「詳細・編集」を開くと、仮IDによる400エラーAPI呼び出しが発生する

- **重大度**: Medium
- **発見シナリオ**: V03 §3
- **再現手順**:
  1. `/owners/new`でペットをpending追加（未保存、API未送信は正常）
  2. ペット一覧行の「…」→「詳細・編集」を開く
  3. `GET /api/v1/pets/temp-xxxxxxx`（400）と`GET /api/v1/pets/temp-xxxxxxx/sub-owners`（400）が発行され、赤いエラートースト「パラメータの形式が不正です: id」が表示される
- **期待結果**: pending（未保存）ペットは実在するIDを持たないため、詳細・編集モーダルを開いてもAPI呼び出しは発生せずエラーも出ないこと
- **実際の結果**: フロントが生成した仮ID（`temp-xxxxxxx`）をそのままバックエンドのIDパスパラメータとして使用してしまう
- **備考**: 同じ「…」メニューには実IDを前提とする「予約作成」「カルテ作成」等のアクションもpending行に対して有効な状態で表示されており、同様のリスクがあると推測されるが未検証。

---

## BUG-023: 飼主フォームで形式違反・範囲外の値を保存しようとしても、保存は静かにブロックされるだけでエラーメッセージが一切表示されない

- **重大度**: Medium〜High
- **発見シナリオ**: V03 §1
- **再現手順**: メールアドレス`abc`、電話番号`090-ABCD-4444`、郵便番号`12-3456`、値引率`101`など、形式違反・範囲外の値を入力して保存
- **期待結果**: 必須項目未入力時と同様に、該当フィールドにインラインエラーメッセージが表示される
- **実際の結果**: 保存は正しくブロックされる（POST/PATCH未送信）が、赤枠表示・エラーメッセージ・トーストのいずれも一切表示されない。必須項目チェック（C1-1）は正しく機能しているのに対し、形式チェック・範囲チェックだけエラー表示の実装が欠落している
- **備考**: ペット編集モーダルの体重バリデーションでは明確なエラーメッセージが表示されており、同じフォーム基盤内でも実装のばらつきがある。

---

## BUG-024: 飼主の重複メールアドレス／重複電話番号チェックのエラートーストが未整形の生メッセージのまま表示される

- **重大度**: Low〜Medium
- **発見シナリオ**: V03 §1手順7（C3-2）
- **再現手順**: 既存飼主と同一のメールアドレス／電話番号で新規飼主登録を試みる
- **期待結果**: 整形済みの日本語のみのメッセージが表示される
- **実際の結果**: `owner 'このメールアドレスはすでに登録されています' already exists`という英日混在の生メッセージが表示される（拒否自体は409で正しい）
- **備考**: 旧BUG-023（権限グループ名重複時の生メッセージ問題）と全く同じパターンの不具合が別箇所（飼主のメール／電話重複）に残存している。修正が権限グループのエラーハンドリング箇所のみに閉じており横展開されていない。BUG-019と同一事象。

---

## BUG-025: Lステップ連携「条件タグ対応」で疾患コード51文字入力時に500エラー

- **重大度**: Medium
- **発見シナリオ**: V04 §8-7
- **再現手順**: `/settings/integrations/lstep`の「条件タグ対応」フォームで疾患コードに51文字（VARCHAR(50)超過）を入力して追加
- **期待結果**: 400 Bad Request +「condition_code は 50 以下で入力してください」等の分かりやすいバリデーションエラー
- **実際の結果**: `500 Internal Server Error`、レスポンスは`{"error":"internal server error"}`のみ
- **備考**: 50文字ちょうど（境界値）は正常に201で受理されることを確認済み。DBのVARCHAR(50)制約違反がハンドラ側でキャッチされずそのまま500として露出していると推測。同フォーム内の「自動管理プレフィックス」は正しく409を返しており、この文字数超過パスのみ検証漏れの可能性が高い。

---

## BUG-026: シフトテンプレート／Lステップ自動管理タグの重複エラーメッセージが内部識別子＋空文字列表示のまま（既存修正の横展開漏れ）

- **重大度**: Low〜Medium
- **発見シナリオ**: V04 §7・§8
- **再現手順**:
  1. `/settings/shift-templates`で既存と同名のテンプレートを保存 → `409 Toast: "shift_template '' already exists"`
  2. `/settings/integrations/lstep`の自動管理プレフィックスで既存の`checkup_`を再度追加 → `409 Toast: "lstep_auto_managed_prefix '' already exists"`
- **期待結果**: 実際の入力値を含む分かりやすい日本語エラーメッセージ
- **実際の結果**: いずれも`{内部テーブル名} '' already exists`という形式のまま、名前部分も空文字列
- **備考**: 前回報告された同種バグ（保険マスタ・支払方法マスタでの重複エラー）は修正確認済みだが、修正が一部のマスタにのみ適用されており、本ラウンドで検証したシフトテンプレートマスタ・Lステップ自動管理タグ設定には未適用。BUG-019/024と合わせて、この「内部識別子＋空文字列」パターンの修正が複数箇所で横展開漏れになっていることが分かる。

---

## BUG-027（要確認・仕様乖離）: 締め時間設定の境界逆転ケースが「受理」から「拒否」に変化しており、テスト仕様書の実測記録と矛盾する

- **重大度**: 情報提供／要確認
- **発見シナリオ**: V04 §6
- **再現手順**: `/settings/closing-time`でAM/PM境界を14:00、平日終了を12:00（境界より前＝逆転）に設定して保存
- **期待結果（仕様書記載の過去実測）**: `PATCH /api/v1/closing-settings`が200で受理される
- **実際の結果**: 400 Bad Requestで拒否される
- **備考**: データ破損はない（失敗したPATCHは反映されず既存値が維持される）。過去の実測以降にサーバー側で境界逆転を禁止するバリデーションが追加された可能性が高い。プロダクトとしては妥当な制約強化とも言えるが、テスト仕様書の期待値と現在の挙動が乖離しているため、仕様書側の更新か、意図しない変更であれば元の仕様に戻すか、開発・PM側の判断が必要。

---

## BUG-028: LINE予約設定「最短予約受付（日数）」保存後、フォーム表示が更新されず保存前の値が表示され続ける

- **重大度**: Medium
- **発見シナリオ**: V05-8
- **再現手順**:
  1. `/line-reservation/settings`で最短予約受付欄を0に変更し保存 → 成功トースト表示
  2. 直後にフォーム欄を確認 → 保存前の値「2」のまま変化しない
  3. `GET /api/v1/clinics/1/line-reservation-settings`で確認 → DBには正しく`0`が保存されている
  4. ページをリロードすると正しい最新値が表示される
- **期待結果**: 保存成功後、フォームには保存した新しい値が表示され続ける
- **実際の結果**: DBへの永続化自体は正しいが、保存直後のフロント側表示状態がリフレッシュされない
- **備考**: 旧BUG-030（値0が保存されない問題）のDB永続化自体は修正確認済み。本件はそれとは別の新規UI不具合。2回連続で再現。

---

## BUG-029: LINE予約のマイ予約キャンセル後、再読込すると対象予約がマイ予約一覧・院内カレンダー双方から完全に消える

- **重大度**: Medium
- **発見シナリオ**: V05-7 手順2・3
- **再現手順**:
  1. 飼い主側でLINE新規予約を作成・確定（院内`/reservations`に正しく反映されることを確認）
  2. マイ予約からキャンセルを実行（実行直後は「キャンセル済」バッジ表示、正常）
  3. ページ再読込 → マイ予約一覧は「予約はありません」と表示され、キャンセル済みの予約が一切表示されない（`GET /api/liff/1/my-reservations`が空配列）
  4. 院内側`/reservations`（フィルタ「すべて」）でも該当日時の予約が一切表示されない
  5. ネットワークログを確認すると、キャンセル操作時に`DELETE /api/liff/1/my-reservations/{id}`（204）が使用されている（PATCHによるstatus更新ではない）
- **期待結果**: 再読込後もcancelledとして永続表示される（削除ではなくstatus更新であること）
- **実際の結果**: キャンセル後、飼い主も病院スタッフも記録を一切参照できなくなる
- **備考**: DB上で論理削除か物理削除かはAPI応答だけからは判別できない。運用上、キャンセル履歴の監査・問い合わせ対応に支障が出る可能性がある。DB参照によるstatus実態確認を推奨。

---

## BUG-030（旧BUG-032の残存・症状変化）: 健診対象者抽出プレビューAPIが依然として機能しない（ハング→500/503エラーに変化）

- **重大度**: High
- **発見シナリオ**: V05-18
- **再現手順**: `/lstep/checkup-sync`で各検診種別（annual/dental/blood/skin/cancer）を選択して「対象者を検索」を実行、`GET /api/v1/clinics/1/lstep/checkup-sync/preview`を直接fetchでも確認
- **期待結果**: 検索結果（対象者一覧または0件）が正常に返る（旧BUG-032は修正済みとされていた）
- **実際の結果**: annualは`500 internal server error`、dentalは8秒でタイムアウト（ハング再現）後に`503`、blood/skin/cancerは`500`。5種類すべてで一度も正常な結果を得られなかった
- **備考**: 症状が「常に無応答」から「遅延の末に500/503を返す」に変化しているが、機能として使用不能な点は変わらず未修正と判断する。再現性5/5。

---

# 前回バグの修正確認状況（サマリ）

前回レポート（BUG-001〜032）のうち、本ラウンドで再検証できた項目の結果一覧。

| 前回の内容 | 今回の結果 |
|:--|:--|
| 死亡ペット状態がフルリロード後も「生存」のまま表示される | **修正確認（FIXED）** — S01・V03で確認 |
| LIFFモック認証のAuthorizationヘッダー欠落による401連鎖（予約・ペットヘルス） | **修正確認（FIXED）** — S04・S12・V05で確認。飼い主向けLINE予約・キャンセル・アカウント連携が一気通貫で動作することを確認 |
| 見積書2件目以降の新規作成が常に409エラー | **修正確認（FIXED）** — S07・V01で確認（ただしV01で見積タブの別の欠陥=BUG-016を新規発見） |
| オーナー集計ダッシュボードの無期限ハング | **修正確認（FIXED）** — S10で確認（220〜300ms応答） |
| 未請求明細取得API（トリミング×診察統合）の500エラー | **症状はFIXED** — S10-12で確認。ただし統合会計の決済確定処理に新規の欠陥（BUG-011）を発見し、統合会計を実際に完了させることは依然できない |
| バイタル体重Kg/g単位切替で数値が変換されない | **修正確認（FIXED）** — V01で確認 |
| 独立フォーム（検査・予防接種・入院・トリミング）での存在しないID直叩き時の空フォーム表示 | **修正確認（FIXED）** — V01で確認（ただしカルテ本体のみ別症状=BUG-017が残存） |
| 検査フォームの必須項目未入力時にエラー表示なし | **修正確認（FIXED）** — V01で確認 |
| レジ締め済み期間の会計に明細追加すると金額と明細が不整合になる | **データ不整合は解消、機能自体は依然使用不能=BUG-021** — V02・S09で確認 |
| 存在しない見積IDで空白フォームが表示される | **修正確認（FIXED）** — V02で確認 |
| 電話番号エラーメッセージの表示残留 | **修正確認（FIXED）** — V02で確認 |
| 死亡登録ダイアログの必須・未来日エラー非表示 | **修正確認（FIXED）** — V03で確認 |
| 権限グループ名重複時の生メッセージ表示 | **修正確認（FIXED、ただし他フォームに同パターン残存=BUG-019/024/026）** — V03で確認 |
| 権限グループの権限マトリクス編集が保存されない | **修正確認（FIXED）** — V03で確認 |
| 主訴種別・問診テンプレートの新規作成がis_active=falseで保存される | **修正確認（FIXED）** — V04で確認 |
| 保険マスタで範囲外値保存時に偽の成功トースト | **修正確認（FIXED、ただし他フォームに同パターン残存=BUG-026）** — V04で確認 |
| 動物種類マスタの重複エラーで生メッセージ表示 | **修正確認（FIXED）** — V04で確認 |
| 支払方法マスタで重複時に偽の成功トースト | **修正確認（FIXED）** — V04で確認 |
| LINE予約設定の最短予約受付日数0が保存されない | **DB永続化は修正確認（FIXED）。ただし表示側に新規バグ=BUG-028** — V05で確認 |
| ログイン済み状態での`/login`直アクセスがリダイレクトされない | **修正確認（FIXED）** — V05で確認 |
| 健診対象者抽出プレビューAPIのハング | **未修正（症状変化、regression）=BUG-030** — V05で確認 |

---

# 各シナリオ 実施状況まとめ

## S01（死亡ペット誤操作の物理ブロック）: 不合格
BUG-001（Critical）・BUG-002（High）・BUG-003（Medium）を新規発見。フルリロード時の表示バグは修正確認。入院登録・予約既定検索・過去記録閲覧は正常。

## S02（検査異常値ハイライト・確定ロック）: 合格
H/L判定・境界値・確定ロック（フロント＋バックエンド二重ガード）・削除拒否すべて仕様通り。新規バグなし。手順8（閲覧専用ロールでの検証）は共有セッション保護のためBLOCKED。

## S03（予防接種 次回予定日自動計算）: 不合格
BUG-004・BUG-005（いずれも既知バグの再発）を確認。日数計算ロジック自体は正しい。異常系A2（猫での犬専用ワクチン制限）はマスタ設計上未実装と判断（新規バグとして扱わず）。

## S04（LIFF予約ジャーニー）: 大部分未実施
手順1（新規予約フロー突入）とLIFF認証ヘッダーの重大回帰チェックのみ実測完了（**FIXED確認**）。手順2〜12（コース選択・枠計算・予約確定・受付カンバン反映・キャンセル等）は下記の環境障害により**未実施**。

## S05（入院サイクル）: 全く未着手
下記の環境障害により、入院管理画面への遷移すら試みられず、シナリオ全体が未実施。**次回最優先で実施が必要。**

## S06（カルテロック・監査証跡）: 一部実施・核心部分は未検証
カルテ作成・問診/診察タブへの入力・保存操作までは実施できたが、**保存内容が実際にAPI応答上で正しく保持されているかの最終確認（過去の重大バグ「カルテ確定時のデータ上書き」の再発有無）が環境障害により完了できなかった**。確定(Lock)・訂正追記・削除拒否・監査証跡の各手順も未実施。**「バグなし」ではなく「未検証」として扱うこと。**

**S04〜S06で発生した環境障害**: 複数テスターが同一ブラウザタブグループを共有しており、担当エージェント専用タブが作成直後に頻繁に強制クローズされる事象が継続し、加えて作業中盤以降ブラウザ操作ツール（navigate/クリック系）の安全性判定サービスが90分以上にわたり応答不能になった。読み取り専用ツールは動作していたため、環境全体ではなくアクション系ツールの障害と判断される。この環境問題が解消され次第、S05全体とS06の核心的な回帰チェック（カルテ`#1425548`のAPI応答確認）を最優先で再実施する必要がある。

## S07（見積書ステータス制御）: 合格
旧BUG-011（見積2件目409エラー）は再現せず修正確認。確定ロックのUI・API不変条件、状態フィルタ、対照群（下書きの編集・削除）すべて仕様通り。新規バグなし。

## S08（会計訂正系）: 不合格
BUG-006（Critical）・BUG-007（High）・BUG-008（Low）を新規発見。理由必須制御・監査ログ記録・締め後訂正の区別記録自体は正しく機能。部分入金→未収金統合フロー（#6〜9）はシナリオ記載通りUI到達不能（既知の制約）。

## S09（レジ締め）: 一部不合格
BUG-009（Medium-High）・BUG-010（Medium）を新規発見。締め時間設定・越日EMG・過不足算出・二重締め拒否・締め履歴と区分フィルタは正常動作。一部の完了時刻境界値検証はDB直接操作が必要でBLOCKED。

## S10（顧客集計ダッシュボード）: 一部不合格
旧BUG-012（無期限ハング）は修正確認。BUG-012・BUG-013（新規番号、集計タブの0件表示・軽減税率計算誤り）を新規発見。

## S11（トリミング統合会計）: 不合格
旧BUG-013（500エラー）は症状として修正確認。しかしBUG-011（Critical、決済確定が常に400エラー）により、S11が本来証明すべき「統合会計を実際に完了できる」という結果は依然として達成できていない。異常系A1〜A3はBUG-011によりブロックされ未実施。

## S12（LIFFペットヘルス）: 一部不合格
旧BUG-014（401エラー）は修正確認。BUG-014（新規番号、clinic_id欠落時のエラー文言）を新規発見。LIFF連携ページのモック限界（実APIを呼ばない）は既知の制約でバグではない。バックエンド停止時のリトライUX確認は共有環境保護のためBLOCKED。

## V01（臨床系フォーム）: 一部バグあり
BUG-015・BUG-016・BUG-017を新規発見。旧BUG-015（体重単位）・旧BUG-017（検査必須項目）は修正確認。旧BUG-016（存在しないID）は検査/予防接種/入院/トリミングは修正確認だがカルテ本体のみ別症状（BUG-017）で残存。旧BUG-011具体的症状（409）は修正確認だが新規の欠陥（BUG-016）を発見。§4・§5・§9・§10本体・§12本体を中心に検証、§2・§3境界値・§7・§8・§11・画像アップロード等は時間都合で未実施。

## V02（会計・予約・受付系フォーム）: 一部バグあり
BUG-018・BUG-019・BUG-020・BUG-021を新規発見・再発確認。旧BUG-019・BUG-020・見積2件目409は修正確認。§10（シフト）・§11（休診日）は未実施。

## V03（飼主・ペット・スタッフ・権限系フォーム）: 一部バグあり
BUG-022・BUG-023・BUG-024を新規発見。旧BUG-021〜024はすべて修正確認。§1・§2の一部手順（C2永続化の網羅確認等）は時間都合で未実施。

## V04（設定マスタ系フォーム、30フォーム）: 一部バグあり
BUG-025・BUG-026・BUG-027を新規発見。旧BUG-025〜029はすべて修正確認（ただしBUG-026のパターンが他フォームに残存）。§6〜§10を重点的に検証、§1・§2の一部は前ラウンドの確認結果を踏襲しスポットチェックのみ。

## V05（認証・LINE系フォーム）: 一部バグあり
BUG-028・BUG-029・BUG-030を新規発見・再発確認。旧BUG-031（ログイン済み/login）は修正確認。旧BUG-008/014（LIFF認証）は修正確認、飼い主向けLINE予約フルフロー（新規予約〜キャンセル）を新たに実機で確認。旧BUG-032（健診対象者プレビュー）は未修正（症状変化）。V05-2〜4、V05-9/10/12〜18は前ラウンド確認済みのため深追いせず。

---

# 総括

- 前回報告された32件のバグのうち、本ラウンドで再検証できた範囲では**大部分が修正確認できた**（死亡ステータス表示、LIFF認証ヘッダー、見積書409エラー、集計ダッシュボードハング、体重単位変換、検査/予防接種等の存在しないID処理、権限グループ関連、主訂種別等のis_active、動物種類/支払方法の偽成功トースト、ログイン済みリダイレクト等）。
- 一方で、**新たに30件のバグ（BUG-001〜030）を発見**した。特に以下は業務上のリスクが大きく優先対応を推奨する。
  - **物理的な安全ガードの欠落**: BUG-001（死亡ペットへの会計確定がURL直叩きで成立してしまう）、BUG-002（同カルテ）— 臨床安全・金銭管理上、最優先の修正が必要
  - **統合会計・軽減税率まわりの機能不全**: BUG-011（トリミング×診察の統合会計が決済確定で必ず400エラー）、BUG-006/BUG-013（軽減税率8%品目を含む会計が確定不能）— 動物病院の実運用で頻出するケースが機能停止する
  - **サイレントなデータ消失・偽の成功通知**: BUG-016（見積タブの2回目以降の保存が偽の成功トーストを出しながら実際は保存されない）
  - **未収金の追跡不能**: BUG-007（クレジット訂正由来の未収金がどこにも表示されない）
  - **既存修正の横展開漏れ**: 「内部識別子＋空文字列」形式の生エラーメッセージ問題（BUG-019/024/026）と「偽の成功トースト」問題（BUG-026、旧BUG-026/029と同根）が、修正済みとされた箇所以外の複数フォームに残存していることが判明。修正時に共通ロジックへ寄せるか、全フォームへの横展開確認を行う運用を推奨する。
- **未検証のまま残っている重大リスク**: S05（入院サイクル）は全く未着手、S06（カルテロック）は過去の最重要級バグ（カルテ確定時のデータ上書き）の再発有無が最終確認できていない。ブラウザ環境障害（タブ競合・安全性判定サービスの長時間障害）が原因であり、環境が復旧次第、最優先で再実施が必要。
- S04の手順2〜12（コース選択・枠計算・キャンセル後の枠解放等）も同様の理由で未実施。

以上。

---

## BUG-031: 当日開始で登録した入院が「入院中」ではなく「予約」になる

- **発見日**: 2026-08-10
- **環境**: `http://localhost:3003` ローカル UAT
- **画面**: `/hospitalization/select-pet` → `/hospitalization/new?petId=...` → `/hospitalization`
- **再現手順**:
  1. 生存ペットを選択して「新規入院登録」を開く。
  2. 入院タイプ「入院」、開始日を当日、終了日を 1 週間後、空きケージを指定して登録する。
  3. 入院一覧の「入院中」および「すべて」を確認する。
- **期待結果**: 開始日が当日の入院は登録直後から「入院中」に表示され、入院中タブの件数にも含まれる。
- **実際の結果**: 登録時は「入院情報を登録しました」の成功トーストが表示されたが、一覧の「すべて」ではステータス「予約」と表示され、「入院中」タブには表示されなかった。
- **証拠**: `/hospitalization` のリストビューで当日開始の新規レコードが「予約」と表示された。

## BUG-032: 入院登録時に入力したケアプランが入院詳細へ保存されない

- **発見日**: 2026-08-10
- **環境**: `http://localhost:3003` ローカル UAT
- **画面**: `/hospitalization/new?petId=...` → `/hospitalization/:id`
- **再現手順**:
  1. 生存ペットの入院登録フォームで開始日・終了日・ケージを入力する。
  2. 治療プランを2行追加し、内容「給餌指示」「投薬指示」と各メモを入力する。
  3. 「登録」を押し、成功トーストを確認する。
  4. 登録された入院の詳細を開く。
- **期待結果**: 入力した治療プランが入院詳細のケアプランとして表示される。
- **実際の結果**: 登録成功トーストは表示されたが、入院詳細には「ケアプラン項目がありません」と表示され、入力した2行が存在しなかった。
- **証拠**: `/hospitalization/:id` の詳細画面で「ケアプラン項目がありません」を確認。

## BUG-033: 完了済み検査の結果入力・保存・削除がロックされていない

- **発見日**: 2026-08-10
- **環境**: `http://localhost:3003` ローカル UAT
- **画面**: `/examinations/:id`
- **再現手順**:
  1. 検査一覧からステータス「完了」の既存検査を開く。
  2. 結果値欄、保存ボタン、削除ボタンの状態を確認する。
- **期待結果**: 確定済み検査は結果項目が編集不可となり、保存・削除の導線が無効化または非表示になる。
- **実際の結果**: 完了済み検査で結果値の textbox が有効、保存ボタンと削除ボタンも表示されていた。既存データ保護のため変更・保存は実施していない。
- **証拠**: `/examinations/1014563` の画面スナップショットで、完了ステータスの結果値 textbox、保存、削除が有効表示。
