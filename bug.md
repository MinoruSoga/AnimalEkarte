# AnimalEkarte 受入テスト バグ報告（Round 9）

- 実施日: 2026-08-25（JST）
- 実施環境: ローカル（`http://localhost:3003`、Go/Ginバックエンド、seed `003_demo`）
- 対象: `docs/ops/testing/scenarios` 配下の業務フローシナリオ S01〜S13、単体フォーム検証 V01〜V05（全9グループ並列実施）
- 起点: 「すべての受入テストを実施して下さい。発見したバグはbug.mdに記載して下さい」（Round 8のBUG-001/BUG-002が未修正のまま再依頼された標準の再受入テスト）

---

## ⚠️ 最優先で確認をお願いしたい事項（実害を伴うインシデント）

本ラウンドの検証中、レジ締め機能の検証（V02担当エージェント）で、**本日（2026-08-25）午前の実データに対する取り消し不能なレジ締めレコードが意図せず作成されました**。

- **内容**: レジ締めAPI（`POST /api/v1/cash-register/closes`）に現金過不足金額として負数（`cash_amount:-100`）を送ったところ、本来はエラーで拒否されるべきところ`201 Created`で受理されてしまい、`actual_cash`が黙って`0`にクランプされた状態で締め処理が完了してしまいました。
- **作成されたレコード**: `id=9`（2026-08-25 AM、`theoretical_cash:6930, actual_cash:0, cash_difference:-6930`）
- **影響**: この締めは取り消し不能（アンドゥ機能なし）であり、`GET /api/v1/cash-register/preview?date=2026-08-25&period=am`は`is_already_closed:true`を返すようになっています。**本日午前分の正規のレジ締めが、通常の運用フローでは実行できなくなっている可能性があります。**
- なお、同じ検証の過程で休診日（2026-06-15 AM、過去日）に対しても同様の意図しない締めレコード（`id=8`, `cash_amount:0`）が作成されていますが、こちらは過去の任意日付のため実害は軽微です。

このデータの扱い（該当締めレコードのDB上での修正・無効化が必要か、本日午前の会計運用にどう対応するか）についてご判断をお願いします。技術的な原因（サーバー側バリデーション欠如）はBUG-007として下記に詳細を記載しています。

---

## 総括

前回（Round 8）ご報告した新規バグ2件（重大：予防接種タブの保存偽陽性、高：一般診察等コースの予約不可）はいずれも未修正のままであることを確認しました。あわせて、Round 8で未実施だった項目（S09のDB境界値検証・城東センター病院固有設定、S11・S13のフルE2E、S13手順7の閲覧専用中間ケース、V01/V02/V04/V05の残項目）の消化を優先的に進めました。

成果として、**S11（トリミング＋診察の統合会計）とS13（同一飼主・ペット連携）のフルE2Eが今回初めて最後まで完走**し、その過程でこれまで発見できていなかった重大な不具合を新たに検出しました。統合会計の中核機能（同日のトリミング明細と診療明細を1回の会計にまとめて精算する機能）が`400 参照先の組み合わせが正しくありません`で失敗し、実質的に機能していません（BUG-008、高）。またS13手順7（閲覧のみ・編集不可の中間ケース）もRound 7から3ラウンド越しで初めて完全に検証でき、PASSを確認しました。

一方、上記インシデントの通り、レジ締め機能のサーバー側バリデーションの弱さ（休診日ガード欠如、マイナス値の無検証受理）が実データへの実害を伴う形で明らかになりました（BUG-007、重大度を引き上げ）。またV04担当エージェントの検証により、単価の非負検証が薬剤マスタだけでなく診察・検査・定期健診マスタにも欠落していることが判明し、BUG-006の影響範囲がより広いことが分かりました。

Round 8で「環境要因の疑い」としていたS08の確認ダイアログ無反応（NEW-1）とS13のowners検索複合語問題（NEW-2）は、いずれも本ラウンドで単独に近い環境で再検証した結果**再現せず**、Round 8時点の懸念通り他エージェントとの並行負荷によるノイズだったことが裏付けられました。post_close_reason自動フォーカス修正も今回、静かな時間帯での再検証により**動作確認が取れ**ました。

なお、V03担当エージェントの検証中に、システム管理者が自分自身のスタッフアカウントを無効化でき、それによって同一セッションを共有する全タブが即座にロックアウトされる（唯一の管理者だった場合、復旧手段がなくなりうる）という重大な運用リスクが判明しました。これはバグと断定はできない（意図的な仕様か未定義の抜け穴かの判断が必要）ため、「要ユーザー判断」の事項として別途記載します。

**本ラウンドも9エージェントが同一ブラウザセッション・同一DBを共有して並行稼働しており**、強制ログアウトやアカウントの無断切替が頻発しました。各エージェントはAPI・ネットワークログでの裏取りを徹底しており、環境ノイズと製品バグを可能な限り切り分けて報告しています。

---

## 深刻度サマリ

| 深刻度 | 件数 | ID |
|:--|:--|:--|
| 重大（Critical） | 1 | BUG-001 |
| 高（High） | 2 | BUG-002, BUG-008 |
| 中〜高（Medium-High） | 3 | BUG-004, BUG-007（実害インシデントあり）, BUG-015（新規） |
| 中（Medium） | 5 | BUG-003, BUG-005, BUG-006, BUG-009, BUG-012 |
| 低〜中（Low-Medium） | 2 | BUG-013, BUG-016（新規） |
| 低（Low） | 3 | BUG-010（継続・環境要因）, BUG-011, BUG-014 |
| 要ユーザー判断（バグか仕様か未確定） | 1 | 自己スタッフ無効化によるセッションロックアウト |

---

## 継続オープン項目（Round 8から未修正・再確認）

### BUG-001（重大）カルテ内「予防接種」タブの保存が偽陽性の成功トーストを出し、実際はデータが一切永続化されない

**Round 9で挙動の詳細が判明しました。**

- **元の手順を厳密に再現した結果**: 「記録を追加」→ワクチン選択→日付選択→**画面右下の外側「保存」ボタン**をクリック → 「保存しました」の成功トーストが表示されるが、ネットワークリクエストは0件。データは一切保存されない（サイレントデータロス、未修正）。
- **新たに判明した点**: サブフォーム内の「◯◯記録を追加」ボタン（ワクチン名の下、フォーム最下部にある別のボタン）を経由すると、そのクリック時点で`POST /api/v1/vaccinations`が即座に201で発行され、実際に永続化されることが分かりました。つまり保存機能自体はこのボタンに実装されているものの、**画面右下の「保存」ボタン（本来の保存操作だとユーザーが直感的に認識するボタン）は予防接種タブでは常に無反応**という、ボタンの役割が入れ替わったような実装になっています。
- **開発チームへの推奨**: 「外側『保存』ボタンが予防接種タブで無反応かつ偽陽性トーストを出す」ことを主たる不具合として修正するとともに、サブフォーム内追加ボタンで既に保存が完了する仕様であれば、その旨をユーザーに明示するUI改善も合わせて検討してください。

### BUG-002（高）一般診察／一般診察(再診)／健康診断／トリミングコース（courseId 1, 2, 7, 9）が全スタッフ・全期間で予約不可

Round 9では独立した2エージェント（S04-06担当・V05担当）がそれぞれAPI（`GET /api/liff/1/available-dates`）とUI（LINE予約アプリの日付選択画面）の両方で再現を確認しました。courseId 1,2,7,9の4コースのみ全スタッフ・全29日が`staff_off`判定になる一方、他の9コース（ワクチン接種・お手入れ・狂犬病等）は同一スタッフで全て予約可能という状態に変化はありません。**未修正。**

### BUG-003（中）`/examinations` の新規検査登録導線が実際の検査記録を作成しない

Round 9で再現を確認。`/examinations/select-pet`でペットを選択すると、期待される検査フォームではなく汎用のカルテ新規作成フロー（`POST /reservations`→`POST /medical-records`）に遷移し、`POST /api/v1/examinations`は一度も発行されません。**未修正（変化なし）。**

### BUG-004（中〜高）カルテ内「定期健診」タブに動的フィールド機能が未実装

Round 9でソースコード比較により再現を確認（`CheckupsTab.tsx`に`checkup_type_fields`関連のコードが依然として存在しない一方、独立フォーム`CheckupForm.tsx`には実装済み）。**未修正（変化なし）。**

### BUG-005（中）予防接種の「次回予定日」が接種日以前でもサーバー側で拒否されない

Round 9で再実機確認。既存レコード（id=1000000008）に`next_date`を接種日の4日前に設定するPATCHが200で受理されることを再確認し、テスト後は正しい値に復旧済み。**未修正（変化なし）。**

### BUG-006（中）診療項目マスタの単価非負検証がサーバー側に欠落 — **影響範囲が拡大**

Round 8では薬剤マスタのみの指摘でしたが、Round 9のV04担当エージェントの検証により、**診察（consultations）・検査（examination-types）・定期健診（checkup-types）の3マスタにも同じ欠陥があることが新たに判明**しました。処置（procedures）・予防接種/ワクチン（vaccines）の2マスタのみ正しく検証されています。

| マスタ種別 | BE非負検証 | UI直接入力での到達可否 |
|:--|:--|:--|
| 薬剤（medicines） | ✗ 欠落（Round 8既知） | 入力自体をブロック（`-`キー無音フィルタ+min=0スピナー）。ただし一覧の単価列で負値が「–」表示になり視認できない副次不具合あり |
| 診察（consultations） | ✗ 欠落（新規判明） | キー入力は可能だがFEのインラインバリデーションで保存自体がブロックされる（POST発火せず） |
| 検査（examination-types） | ✗ 欠落（新規判明） | 診察と同一コンポーネントのため同様と推定（未確認） |
| 定期健診（checkup-types） | ✗ 欠落（新規判明） | 同上（未確認） |
| 処置（procedures） | ○ 正常 | — |
| 予防接種/ワクチン（vaccines） | ○ 正常 | — |

いずれも通常のUI操作では到達困難ですが、API直叩き・外部連携・将来のFE実装漏れからは到達可能であり、バックエンド側の`validateNonNegativePrice`相当の呼び出し漏れが薬剤サービス以外にも及んでいる可能性が高い状態です。

### BUG-007（中〜高・重大度を引き上げ）レジ締め処理のサーバー側ガードが全般的に弱く、休診日・不正値のいずれも実際に締め処理が完了してしまう

Round 8では「休診日にバナーとフォームが同時表示される」という表示上の指摘（Low・要確認）でしたが、Round 9で**バックエンド自体が休診日チェックを一切行っていないこと、および現金過不足金額の負数チェックも欠落していることが確認され、いずれも実際に取り消し不能な締めレコードが作成される実害を伴う**ことが判明しました。

1. **休診日ガード欠如**: 曜日ベースの定休日・`clinic_holidays`への明示登録日のいずれに対しても、`POST /api/v1/cash-register/closes`が休診日チェックなしで`201`を返し、締め処理が成立してしまう（検証用の過去日2026-06-15で実証、id=8として残留）。
2. **現金過不足金額の負数チェック欠如**: `cash_amount:-100`を送信しても拒否されず`201`で受理され、`actual_cash`が黙って`0`にクランプされる。**この検証を本日（2026-08-25）AM期間の実データに対して実行してしまった結果、取り消し不能な締めレコード（id=9）が作成される実害が発生しました**（詳細は冒頭の「最優先で確認をお願いしたい事項」参照）。

推奨対応: `cash_register_service.go`（推定）にて、締め処理受理前に(a)対象日が休診日でないことの検証、(b)`actual_cash`が0以上であることの検証、の両方を追加することを推奨します。

### BUG-010（低・環境要因、継続）LIFF実トークン認証がローカル環境ではモック限定で検証不可

Round 9でも状況は変化なし（`VITE_LIFF_MOCK=true`によりローカルでは実LINE連携の検証が不可）。アプリの不具合ではなく環境制約として継続監視。

---

## 新規バグ（Round 9で新たに発見）

### BUG-008（高・新規）統合会計（同日トリミング＋診療明細の一括精算）が`400 参照先の組み合わせが正しくありません`で失敗する

- **シナリオ**: S11
- **概要**: 同一ペットの同日分について、`source:"medical_record"`の診療明細1件と`source:"trimming"`のトリミング明細2件を同一の`POST /api/v1/accountings/complete`リクエストに含めて送信すると、常に`400`で失敗する。トリミングのみ・医療のみをそれぞれ単独で送信すると正常に`201`で完了することを確認しており、**両方のsourceを混在させた場合にのみ発生する**。
- **影響**: S11シナリオの中核機能である「同日のトリミングと診察を1回の会計にまとめる」がまったく機能しない。準備用API（`unbilled-details`等）は両方のsourceを正しく返しUI上は統合可能に見えるため、ユーザーは原因不明のエラーで繰り返し失敗する可能性が高い。
- **ワークアラウンド**: トリミングと医療を別々の会計として個別に精算すれば正常完了する（本ラウンドではこの方法で残務を精算済み）。ただし「1つの会計にまとめる」という本来の要件は満たせない。
- **推奨対応**: バックエンドの会計確定処理における明細source検証ロジック（`参照先の組み合わせ`チェック）が、`medical_record`と`trimming`の混在を誤って拒否している可能性が高く、該当バリデーションの見直しを推奨。

### BUG-009（中・新規）カルテを会計確認（医師）より先に確定すると、以後会計確認が永久にブロックされ明細が統合会計から恒久的に除外される

- **シナリオ**: S11
- **概要**: 「会計確認（医師）→カルテ確定」という正しい順序をUIが強制しておらず、順序を誤って先にカルテを確定してしまうと、事後の会計確認要求が`409 確定済みカルテの会計確認は変更できません`で永久にブロックされる。この結果、当該診療明細は恒久的に「同日統合対象外」のまま残り、`GET /api/v1/billing-items/ungrouped-same-day`が`has_ungrouped:true`を返し続ける。
- **影響**: データ自体は失われないが、統合会計の対象から外れたまま回復手段がない状態が恒久的に残る。
- **推奨対応**: (a) カルテ確定ボタン押下時に「会計確認が未完了です」の警告を表示する、または (b) 確定済みカルテでも会計確認の変更を許可する、のいずれかの是正を推奨。

### BUG-011（低・新規）薬剤マスタの重複名エラーメッセージが他の診療項目マスタと異なり非構造・名前欠落

- **シナリオ**: V04
- **概要**: 診察・検査・処置・予防接種・定期健診の5マスタはいずれも重複名エラーで構造化された409レスポンス（`code`と`params.name`に実際の入力名を含む）を返すのに対し、薬剤マスタのみ非構造の文字列エラーを返し、かつ入力した実際の名称ではなく空文字が埋め込まれる（例: `{"error":"medicine '' already exists"}`）。
- **重大度**: 低。409で正しく拒否され保存自体は防がれているため実害は小さいが、UIトーストの文言品質（実名を含めるという既存要件）を満たしていない。

### BUG-012（中・新規）健診対象者一括タグ付与の抽出条件プレビューAPIにサーバー側の境界値検証が存在しない

- **シナリオ**: V05-18
- **概要**: `GET /api/v1/clinics/1/lstep/checkup-sync/preview`に対し、負の年齢（`min_age=-3`）・小数の年齢（`min_age=2.5`）・最小>最大（`min_age=10&max_age=5`）・負の来院回数（`min_annual_visits=-1`）・検診種別未指定のいずれを送っても`200`で受理され結果が返る。比較対象の`min_total_amount`のみ正しく`400`で拒否される。
- **影響**: 通常のUI操作ではクライアント側バリデーションで防御されているため実害は限定的だが、API直叩きや将来のFE実装ミスにより境界値検証を完全にバイパスして顧客一覧を抽出できてしまう。データ抽出のガードレールが実質的にFE依存になっている設計リスク。

### BUG-013（低〜中・新規）`/identity-links`管理画面のunlinkボタンが常時disabledで機能しない

- **シナリオ**: S13
- **概要**: 実際にリンク済みの飼主ペアを選択しても、管理画面上の「unlink」ボタンが常にdisabledのままクリックできない（複数回再現）。一方、バックエンドAPI（`DELETE /api/v1/identity-links/owner-groups/:id/members`）自体は直接叩けば正常に機能することを確認済み。フロントエンドが選択ペアの実際のグループ所属状態を正しく把握できていない可能性が高い。
- **重大度**: 低〜中。この画面は内部ワークベンチ的な位置づけで実運用への影響は限定的だが、UIから解除操作が一切できない状態は改善が必要。

### BUG-014（低・新規、環境要因の可能性あり）検査取り込みモーダルが確定済み検査を除外せず選択候補として表示する

- **シナリオ**: V01
- **概要**: カルテの検査取込モーダルが、選択すると必ず`409 確定済みの検査は編集できません`で失敗する確定済み検査を、候補として除外せずに表示してしまう。
- **重要な留保**: 検証時点で環境内の未紐付け検査記録が全て確定済みだったこと自体、他エージェントの並行操作の影響である可能性が高く、製品側の設計欠陥と断定はしない。ただし「選択すれば必ず失敗する項目をユーザーに選ばせてしまう」UXギャップとしては記録に値する。

### BUG-015（中〜高・新規）予約区分マスタを無効化すると、それを参照する既存予約の編集画面が壊れ、予約区分・時間が空欄化する

- **シナリオ**: V02（削除済みマスタの参照ズレ検証）
- **概要**: 使用中の予約区分マスタは`DELETE`が409で保護されている一方、`PATCH {"is_active":false}`による「無効化」は使用中でも実行できてしまう。無効化後、その区分を参照する既存予約（無効化前に作成された正当なデータ）を編集しようとすると、空き時間枠取得API`GET /api/v1/reservations/available-times`が`400 {"error":"reservation type is inactive"}`を返し、フロントは「データ取得に失敗しました」トーストを出したうえで**予約区分欄・時間欄を空値にリセットしてしまう**（他フィールドは保持される）。API本体（`GET /reservations/:id`）はデータを正しく保持しており、破壊されるのは編集フォームの表示のみ。
- **影響**: マスタの無効化という一見安全な管理操作が、既存の正当な予約データの編集可否に予期せず波及する。気づかずに他フィールドだけ修正して保存しようとすると、区分未選択のバリデーションで保存自体に失敗する可能性が高い。
- **推奨対応**: 既存予約が参照する区分が無効化済みであっても、編集画面は正常に開き既存の区分名・時間を初期値として保持したうえで、「このマスタは無効化されています」等の注記とともに区分自体の変更のみ制限する設計に見直すことを推奨。

### BUG-016（低〜中・新規）無効化されたマスタを参照する予約がカレンダー上で無彩色（グレー）表示になるが理由の説明がない

- **シナリオ**: V02（BUG-015と同一検証の副次発見）
- **概要**: 予約区分マスタを無効化すると、それを参照する予約カードがカレンダー上で元のカテゴリ色からグレー無彩色に変化する（`is_active`の値に追従することを確認済み、意図的なフォールバック実装と推測）。しかし、この無彩色化には理由を示す文言・アイコン・ツールチップが一切なく、キャンセル済み等の他の弱調表示パターンと視覚的に区別できない。予約詳細モーダルを開いても区分名はそのまま表示され「(無効)」等のサフィックスは付与されない。
- **重大度**: 低〜中。Round 8のBUG-014（無効化された医院に「（無効）」ラベルが表示される、という正しい実装パターン）と対照的な事例であり、同種の考慮を横展開して見直す価値がある。

---

## 参考記録（未確定・情報提供のみ・製品バグと断定しない事項）

| 項目 | シナリオ | 内容 |
|:--|:--|:--|
| LINE予約「受付停止中」の事前警告がない | V05 | 停止中でも予約フォーム全体を入力させ、最終確定時に初めて409で拒否される。UX改善候補として次ラウンド以降での起票を検討 |
| `calendar_months=0`のサーバー側検証なし | V05 | 0を保存すると翌月送りが実質機能しなくなる。誤操作時の実害が大きいため`>=1`等の検証を推奨 |
| `booking_window_min_days=0`のサーバー側検証なし | V05 | 即日予約許容の仕様である可能性もあり断定せず | 
| 予約・LIFF案内文言フィールドに文字数上限がない | V05 | 10,000文字でも切り詰めなく保存される |
| 会計の割引欄（150円）が合計に反映されず送信時0にロールバックする | S08 | 未確定・要掘り下げ。バリデーション仕様が不明 |
| 見積詳細画面に「後継ドラフトを作成」ボタンが新規に存在 | S07 | 動作は未検証。ロック済み見積の是正導線として機能追加された可能性、次ラウンドでの仕様確認を推奨 |
| 八王子病院の締め設定に`special_periods`（夏季休暇による一時境界変更）が存在 | S09 | S09仕様書に記載がなく、仕様と実装の整合確認を推奨 |
| `identity-links`専用検索がスペースなし複合語（例:「田中光広」）で0件になる | S13 | `/owners`検索とは別実装。実務上はスペース区切り検索が一般的なため影響軽微 |
| **新規予約の作成がAPI・UIとも全面的にブロックされている** | V02追加検証 | `POST /api/v1/reservations`が区分・日付・時刻を問わず`409 本日は医師が出勤していないため予約できません`を返す。原因は`shifts`テーブルの当月シフト登録がほぼ空（過去ラウンドの残留1件のみ）であるため。既存の予約データは編集ベースの検証で対応可能だが、**「新規に予約を1件作る」系のテストケースは次ラウンド以降すべてこの制約に引っかかる**見込み。Round 8で修正されたはずのシフトデータ（2026-12-22までリベース済みのはず）が今回は再び枯渇しているため、シード投入・CIリントゲートの動作状況の確認を推奨 |

---

## 要ユーザー判断: システム管理者が自分自身のスタッフアカウントを無効化でき、即座に全セッションがロックアウトされる

V03担当エージェントの検証中、システム管理者（`admin@noavet.jp`）が自分自身のスタッフレコードのステータスを「無効」に変更する操作を試みたところ、**バックエンドはこれを拒否せず保存が成功**しました。直後、同一Cookieセッションを共有する全タブで認証が失効（`GET /api/v1/me`が403）し、実質的に即座にログアウト状態（操作不能）に陥りました。再ログインも「無効化されたアカウント」として拒否されました。

本検証環境では、たまたま別のシステム管理者アカウント（`hayashi@noah-vet.co.jp`）が同一セッション上でアクティブだったため、そちらからAPI経由で無効化を取り消し復旧できましたが、**唯一のシステム管理者アカウントしか存在しない体制で同様の誤操作が起きた場合、復旧手段がなくシステム全体がロックアウトされうる**というリスクを示しています。

Round 7/8で確認された「自己剥奪ガード（BUG-140）」は、権限グループの`master-permission`編集権限の自己剥奪や、自分の医院をスタッフの所属医院から外す操作に対するガードであり、今回検出した「自分自身のスタッフステータスを無効にする操作」に対する既存ガードとは別物である可能性があります。

この事象をバグとして修正すべきか（例: 自分自身のステータス無効化を禁止する、または唯一の有効なシステム管理者を無効化しようとした場合に拒否する）は仕様判断が必要なため、ご確認をお願いします。

---

## 今回確認できた修正・解消事項

- **post_close_reason自動フォーカス修正（コミット `1fd6260df`関連）**: Round 8では確認ダイアログの無反応により実地確認できていませんでしたが、Round 9で並行負荷の少ない時間帯に再検証したところ、確認ダイアログは正常に応答し、サーバー400エラー発生時に`postCloseReason`テキストエリアへ自動フォーカスが移ることを実測できました。**修正確認（動作OK）。**
- **S08 NEW-1（確認ダイアログのクリック/キーボード無反応）**: 単独に近い環境で再検証したところ再現せず。Round 8の懸念通り、他エージェントとの並行フォーカス奪取が原因だったことが裏付けられました。**環境ノイズと判定、クローズ。**
- **S13 NEW-2（`/owners`検索での姓名複合語0件問題）**: 静かな環境で再検証したところ「田中光広」（スペースなし）で正しく1件ヒットし、再現しませんでした。**環境ノイズと判定、クローズ。**
- **S13手順7（閲覧のみ・編集不可の中間ケース）**: Round 7〜8では対応する権限構成のテストアカウントが存在せず一貫してブロックされていましたが、Round 9で権限グループへの一時的な権限付与→`viewer@example.com`での検証→権限復元、という手順で**初めて完全に検証・PASSを確認**しました。
- **S13フルE2E（unlink→relink→ペットリンク→履歴確認）**: 新規のクリーンなオーナーペアを用意する方式で、今回初めて最後まで完走しPASSしました。
- **S11フルE2E（トリミング＋診察の統合会計）**: 今回初めて最後まで完走しましたが、その過程でBUG-008・BUG-009を新たに検出しています（完走はしたが結果としてバグが見つかった、という位置づけです）。

### 追加検証（時間都合で未実施だった項目を本ラウンド内で実施）

Round 9の一次報告後、時間都合で見送っていた項目を追加で実施しました。

- **S02の詳細回帰**（HIGH/LOW判定・確定ロック・確定解除フロー・印刷スナップショット等7観点）: すべてPASS、回帰なし。検証の前提としてBUG-003（検査新規登録導線）に改めて遭遇しましたが、既知の回避策で問題なく検証を完了できました。新規バグはなし。
- **V04: 薬剤マスタの投与量パラメータ（dose-params）エンドポイント404の切り分け**: **製品バグではなく、テスト側のAPIパス推測ミスと判明しました。** 正しいエンドポイントは`PUT /api/v1/masters/medicines/{id}/dose-params/{species}`（`species`は`dog`/`cat`固定のパスセグメントが必須、`POST`は未実装で404、upsert方式）でした。この認識のもとで再検証したところ、上限必須・下限≦上限・重複防止（upsert方式による1薬剤×1種別=1行の保証）のいずれも仕様通り正しく機能していることを確認しました。
- **V04: 薬剤マスタの必須名称チェック・PATCH部分更新の非破壊確認**: いずれもPASS、問題なし。
- **BUG-006拡張分（検査・定期健診タブ）のUI直接入力での到達可否**: 診察タブと同様、両タブともFEのインラインバリデーションで負価格の保存がブロックされることを確認（バックエンドの検証欠落自体は既報告のBUG-006の通り）。
- **V02: 削除済みマスタの「soft-delete表示は別経路」の検証**: DELETEではなく「無効化（`is_active:false`、使用中でも実行可能）」という別経路で目的の状態を再現し、検証を完了しました。その結果、**新規バグ2件（BUG-015・BUG-016）を検出**しました。この過程で、環境内のシフトseedデータが枯渇しており新規予約の作成自体がブロックされている状態も判明しました（参考記録セクション参照）。

---

## 未実施・要フォローアップ項目

- **S09境界時刻ちょうどのDBレベル検証**: 引き続きブロック中。`psql`/`docker`が環境に存在せず、かつdevice_bash環境からアプリホストへのネットワーク到達性もないことを確認（原因の切り分けがRound 8より明確になった）。これは製品バグではなく検証環境のツール制約であり、環境が整い次第の実施となります。
- **S09城東センター病院でのUI目視確認**: 設定値のAPI直接照合（13:30/19:00/18:00が仕様通りであることを確認）はできましたが、セッション干渉により画面遷移が完了せず、UIレンダリングでの確認は未達。これも製品バグではなく、9エージェント並行稼働によるセッション共有が原因です。
- **V05-17（タグ一括解除の実行）**: 確認ダイアログの表示・導線は確認済みだが、対象が実シードデータかつ非可逆操作のため実行は意図的に見送り（バグではなく安全側の判断によるもの）。
- **V05-18の#3・#4・#6**: デモシードにLINE連携済み（`has_line:true`）オーナーが1件も存在しないため、確認ダイアログを開くことすら出来ず検証不可能。これは製品バグではなくテストデータ不足によるもので、次ラウンドまでにLINE連携済みテストオーナーを最低1件シードに追加することを推奨します。
- **新規予約作成のブロック（シフトseed枯渇）**: 上記の通り判明した環境課題。これが解消されない限り、次ラウンド以降も「新規に予約を1件作る」系のテストケースは同じ制約に引っかかります。

### クリーンアップ未完了・意図的に残置したデータ

- **レジ締め id=8, id=9**（BUG-007の実害証跡。id=9は本日実データ、要ユーザー判断）
- カルテ 1000000024（タマエ）・1000000026（ベル）: BUG-001/BUG-003検証で作成、削除APIが存在しないため削除不可
- カルテ 1000000025・予約1000000036（BUG-009の再現証跡として意図的に残置）
- S13で新規作成したオーナーペア（1000000010/1000000011）・ペットペア（1000000012/1000000013）: `owner-group id=9`/`pet-group id=7`としてリンク済み状態で残置（フルE2E完走の正常な副産物）
- 見積 id=1000000016（承認済み、削除ガード確認用に意図的に残置）
- `lstep-settings`のシークレット3種（Round 8から未復元、ダミー値のまま）: 部分クリアAPIが存在せず、全削除は他の復元済み設定も巻き込むため実行を見送り継続中
- S02追加検証で作成した`examinations id=1000000008/1000000010`（完了済み、削除API制約により残置）・`medical_records id=1000000029`（BUG-003検証の副作用、削除API不在のため残置）
- V04追加検証・V02追加検証（BUG-015/016）で作成したテストデータはすべてクリーンアップ済み（薬剤・投与量パラメータ・予約区分マスタとも削除確認済み、予約1000000035は元の値に復元確認済み）

---

## 環境上の注意

- 本ラウンドも9エージェントが同一ブラウザプロファイル・同一ログインセッション・同一DBを共有して並行稼働しました。強制ログアウト・アカウントの無断切替・タブの意図しない遷移が頻発しましたが、多くのケースで「静かな時間帯に再検証すると再現しない」ことが確認でき（post_close_reason、S08 NEW-1、S13 NEW-2）、Round 8までの複数の低確信度所見が環境ノイズであったことの裏付けが取れました。
- 一方で、今回のレジ締めインシデント（BUG-007関連）のように、**並行稼働環境での検証がそのまま実データへの実害につながるケースがある**ことも明らかになりました。次ラウンド以降、実データに不可逆な影響を与えうる操作（レジ締め・タグ一括解除等）の検証は、可能であれば専用のテスト用クリニック/データセットを用意するか、影響範囲を慎重に見極めた上で実施することを推奨します。

---

## 付録: 今回使用した主なテストアカウント

| メールアドレス | 表示名 | ロール/権限 | 備考 |
|:--|:--|:--|:--|
| `admin@noavet.jp` / `password1` | ノア | システム管理者・全クリニック | メイン検証アカウント |
| `hayashi@noah-vet.co.jp` | 林 文明 | システム管理者・全クリニック | セッション競合時の代替管理者アカウントとして複数エージェントが使用 |
| `vet@example.com` / `password` | 倉田 春香 | 一般 | S08一般ロールRBAC再確認、S06 A2 |
| `viewer@example.com` / `password` | 閲覧 太郎 | 一般・閲覧専用グループ | S06 A2、S13手順7（今回初めて完全検証） |
| `joto-vet@example.com` / `password` | 城東 獣医 | 執行・城東センター病院 | S09城東センター病院固有設定検証 |
