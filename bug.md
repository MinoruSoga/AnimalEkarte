# AnimalEkarte 受入テスト バグ台帳（残件のみ）

- 元 Round: 9（実施日 2026-08-25 JST）
- 台帳整理: **2026-08-27** — 対応済み項目を本文から削除
- main 統合（コード修正）: `43160a58b`（BUG-011/018/019/020/023–030 ほか過去修正済み分）

---

## 対応状況サマリ（2026-08-27）

| 区分 | 状態 |
|------|------|
| コード修正して main に入れたもの | **台帳から削除済み**（再掲しない） |
| **残件（このファイルに残す）** | 下記 4 系統のみ |
| 本番デプロイ | 別判断（未実施のまま） |

### 残件一覧

| ID / 項目 | 状態 | 次の行為 |
|-----------|------|----------|
| レジ締め実害データ（id=8/9） | **要人間判断** | DB 修正・無効化・当日 AM 運用の扱いを決める |
| **BUG-014** | **判断待ち**（製品 vs 環境が曖昧） | 再現条件の確定 or 仕様判断 |
| **BUG-010** | **環境制約・監視** | ローカル LIFF 実トークン検証は不可のまま |
| 管理者セルフ無効化ロックアウト | **要仕様判断** | バグ修正するか仕様として許容するかを決める |

---

## ⚠️ 最優先: レジ締め実害データ（要人間判断）

検証中、レジ締め API に負の現金過不足を送ったところ `201` で受理され、**取り消し不能な締めレコード**が作成された。

| 項目 | 内容 |
|------|------|
| 問題だったコード | 休診日ガード欠如・負 `actual_cash` 受理（**コード側は修正済み・main 済**） |
| 残っている実害 | **データの扱いが未決** |

- **id=9**: 2026-08-25 AM · `theoretical_cash:6930, actual_cash:0, cash_difference:-6930`  
  - `GET .../preview?date=2026-08-25&period=am` が `is_already_closed:true` になりうる  
  - **当日午前の正規レジ締めが通常フローで実行できない可能性**
- **id=8**: 2026-06-15 AM（過去休診日）· 実害は軽微だが同様の意図しない締め

**お願い:** 該当レコードの DB 修正・無効化の要否、および当日 AM 会計運用の対応を判断してください。

---

## BUG-014（低・判断待ち）検査取込モーダルが確定済み検査を候補表示する

- **シナリオ**: V01
- **概要**: カルテの検査取込モーダルが、選択すると必ず `409 確定済みの検査は編集できません` になる確定済み検査を、候補から除外せず表示する。
- **留保**: 検証時点で未紐付け検査がすべて確定済みだったこと自体、並行エージェントの影響の可能性があり、**製品欠陥と断定しない**。
- **残理由**: 「必ず失敗する項目を選ばせる」UX ギャップとしては記録価値あり。製品修正するか、環境ノイズとして閉じるかの判断が必要。
- **状態**: BLOCKED（agent-loop 自動実装対象外）

---

## BUG-010（低・環境制約）LIFF 実トークン認証がローカルではモック限定

- Round 9 以降も変化なし（`VITE_LIFF_MOCK=true` 等によりローカルでは実 LINE 連携の検証不可）。
- **アプリ不具合ではなく環境制約**。対応レーンには入れない。継続監視。
- **状態**: REFERENCE

---

## 要仕様判断: 管理者のセルフ無効化で全セッションロックアウト

- システム管理者が**自分自身**のスタッフレコードを「無効」にできる。
- 直後、同一 Cookie セッション全体が認証失効（`/me` 403）。再ログインも拒否。
- 唯一のシステム管理者しかいない運用では、**復旧手段なくロックアウトしうる**。
- Round 7/8 の自己剥奪ガード（権限・医院）とは別経路の可能性。
- **バグ修正（自己無効化禁止 / 最後の管理者は拒否）か、仕様として許容するか**を判断後に対応。
- **状態**: BLOCKED（仕様決定待ち）

---

## 残置データメモ（判断・クリーンアップ用）

コード修正済みでも、検証副産物として残っている可能性があるもの:

- レジ締め **id=8, id=9**（上記・要判断）
- カルテ / 予約 / identity-links 検証用データ（削除 API 制約で残置したものあり）
- `lstep-settings` シークレット類（ダミーのまま等）

詳細な ID 一覧が必要なら git 履歴上の旧 `bug.md` を参照。

---

## 削除済み（再掲しない）

以下は **対応済み** のため本台帳から削除した（コードは main `43160a58b` 系に含まれる）:

- BUG-001〜009, 011〜013, 015〜030（該当する修正済み分）
- 並列レーン claim / ファイル専有表 / 割当表（消化済みのため）
- Round 9 の長文総括・環境ノイズ解説の本体（履歴は git に残る）
