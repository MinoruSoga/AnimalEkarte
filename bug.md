# AnimalEkarte 受け入れテスト バグ報告（ラウンド5・再検証）

実施日: 2026-08-21
実施環境: http://localhost:3003（ローカル・seed 003_demo）
対象範囲: `docs/ops/testing/scenarios` 配下 S01〜S13（業務フローシナリオ、S13は今回追加された新規シナリオ）、V01〜V05（個別フォーム検証）
ログイン: 林文明 / 八王子病院（一部シナリオのみ城東センター病院・X-Clinic-IDヘッダで確認）

**本レポートについて**: 前回（ラウンド4）報告した全バグ（BUG-001〜BUG-022）はユーザーによりすべて対応済みとの前提で、S01〜S13・V01〜V05の受け入れテストを一から再実施した。番号は本レポート内で新規に振り直している（BUG-001〜）。

**総括**: ラウンド4で報告した既知バグは**ほぼすべて修正確認**できた。特にV05のパスワード変更ダイアログ全画面クラッシュ（前回最重要の回帰）は完全に解消し、V04の「保存ボタン無音無反応」パターンも3件とも修正確認、かつ今回新たにテストした範囲でも再発しなかった。S06のカルテ確定時データ上書き防止も**4ラウンド連続**で問題なし。

一方、**唯一の重大な既知バグ再発**として、カルテの来院種別（visit_type）に関するバグを検出した。前回の「保存ボタンなしでは反映されない」という報告よりも実態は深刻で、**フロントエンドの変換関数の実装漏れにより「緊急」「往診」等を選択しても常に無警告で「再診」として保存される**（根本原因をソースコードレベルで特定、詳細はBUG-003参照）。

新規バグは合計**18件**（High 6件 / Medium 6件 / Low 6件）。特にV02（会計・予約・受付系フォーム）で、予約編集・ステータス変更の代替導線から業務ルール（担当医シフトチェック、カルテ作成必須ガード）をバイパスできる重大なバグを2件発見した。また「保存ボタンが無音で反応しない」というV04で解消されたはずのパターンが、V02の別フォーム（お釣り欄・レジ現金欄）で新たに確認されており、横断的な点検を推奨する。

今回追加された新規シナリオS13（識別子リンク・手動補正）は初回テストを実施し、正常系は問題なく動作することを確認したが、エラーメッセージの可読性・検索の表記ゆれ耐性・一部APIレスポンスの正確性に軽微な不具合を発見した。

---

## 重大度サマリ

| 重大度 | 件数 | 該当 |
|:--|:--|:--|
| High | 6 | BUG-001, BUG-003, BUG-011, BUG-012, BUG-013, BUG-015 |
| Medium | 6 | BUG-005, BUG-006, BUG-007, BUG-009, BUG-010, BUG-016 |
| Low | 6 | BUG-002, BUG-004, BUG-008, BUG-014, BUG-017, BUG-018 |

---

# S01〜S03（死亡ペットガード／検査確定ロック／ワクチン自動計算）

**既知バグ3件すべて修正確認。新規バグ2件。**

- 予約検索の死亡ペット既定除外、入院登録フォームの事後バリデーション、予防接種一覧のサーバーサイド検索（`GET /vaccinations?search=`）はいずれも**修正確認**。

## BUG-001: 検査編集フォームで担当医（doctor_id）の変更が保存時にサーバーへ送信されず、確定操作がデッドロックする
- **シナリオ**: S01-S03 / **重大度**: High
- 検査の新規作成では`doctor_id`が正しく送信されるが、既存検査の更新（`PATCH /v1/examinations/:id`）を担う共有フックの型定義に`doctor_id`が含まれておらず、編集時の保存では常にこのフィールドが欠落する。一方でフロント側バリデーションは新規・編集問わず担当医の選択を必須としているため、以下の詰みが発生する: (1)担当医未設定の既存検査を開く→(2)担当医を選択し保存（成功するが送信されず反映されない）→(3)再度開くと完了ロックにより担当医セレクトがdisabledかつ空表示→(4)「確定」への変更時にクライアントバリデーションで「担当医を選択してください」エラーとなり**永久に確定へ遷移できない**。
- 直接API（`PATCH`に`doctor_id`を含める）では正常に永続化されることを確認しており、原因はフロントエンドの編集用ペイロード構築漏れ（`use-update-examination.ts`の型定義、`use-examination-form.ts`のPATCH構築処理）。既存の単体テストが`doctor_id`の有無をアサートしていないため検出されずに残っていた。

## BUG-002: 新規カルテの検査タブで、実際には表示されない「記録を追加」ボタンへの操作を促す空状態メッセージが出る
- **シナリオ**: S01-S03 / **重大度**: Low
- カルテ新規作成画面（`isNewRecord=true`）の検査タブが0件のとき、「記録を追加」ボタンから追加してください、という案内が表示されるが、当該ボタン自体は新規カルテでは条件分岐により描画されない（カルテを一度保存し既存レコードとして開き直すまで存在しない）。

---

# S04〜S06（LIFF予約ジャーニー／入院サイクル／カルテロック）

## 核心チェック（S06）: 修正確認・再発なし（4ラウンド連続）
確定操作・確定直後のデータ非欠落・直接API改ざん試行（メイン記録／子リソースとも409拒否）・削除試行（409拒否）・訂正追記（addendum）機能・確定解除エンドポイントの非存在、いずれも本ラウンドで問題なし。

既知バグ「入院フォームに主訴/担当医UIが存在しない」は**修正確認**（`POST /hospitalizations`のペイロード・レスポンス・一覧表示とも正しく反映）。S05の退院系フロー（会計込み退院・二重退院防止・ケアプラン紐付き削除防止）はすべて正常動作。

## BUG-003: カルテの来院種別で「緊急」「往診」を選択すると、無警告で「再診」として保存される（既知バグの再発・実態悪化）
- **シナリオ**: S04-S06 / V01（両シナリオで独立に発見、同一根本原因） / **重大度**: High
- 前回報告した「保存ボタンなしでは来院種別が反映されない」（旧BUG-005）は、実態としては**PATCHリクエストは即時発行されて200を返し、成功トーストまで表示されるにもかかわらず、`visit_type`の値自体がDB上で更新されない**という、より発見しづらい形で再発している（`version`は増加するが値は不変）。
- さらに、来院種別ドロップダウンで「緊急」「往診」を選択した場合、選択した値への変更成功トーストが表示されるが、実際に送信されるペイロードは常に`{"visit_type":"revisit"}`であり、選択内容と異なる値へ無警告ですり替えられて保存されることを2エージェント独立に確認した。
- **根本原因（ソースコードで特定済み）**: `frontend/src/features/medical-records/hooks/use-medical-record-form-model.ts`の変換関数`toVisitTypeValue()`が「初診」「first」のみを特別扱いし、それ以外（再診・緊急・往診）をすべて`"revisit"`にフォールバックさせている。この関数はヘッダーの即時PATCH処理のほか、カルテ新規自動作成時の初期値設定からも呼ばれており、影響範囲は広い可能性がある。バックエンドが正式サポートする値は"first"と"revisit"の2種類のみで、「緊急」「往診」に対応する値がそもそも存在しないため、フロントのUI選択肢自体が誤解を招く実装になっている。
- **影響**: 診療記録の来院種別という基幹データが、ユーザーの意図と異なる値でサイレントに保存される。統計・保険請求区分等の下流処理に影響しうる。

## BUG-004: 入院編集ページのペットヘッダーで年齢・性別・体重が「不明」と誤表示される
- **シナリオ**: S04-S06 / **重大度**: Low
- カルテ画面・入院詳細（閲覧専用）画面では正しく表示されるが、「入院情報の編集」画面固有のヘッダーコンポーネントでは年齢・性別・体重が「不明」表示になる。前ラウンドのBUG-011（独立登録画面の動物種別「不明」誤表示）と同系統・同根の可能性。

## BUG-005: 入院ボード（ケージ表示）のドラッグ&ドロップで、キーボード操作によるピックアップは成功するが移動操作が機能しない
- **シナリオ**: S04-S06 / **重大度**: Medium（アクセシビリティ）
- Space押下でのピックアップは`aria-live`で確認できるが、その後の矢印キー押下では移動が発生しない（15回の合成イベント送出でも無反応）。ケージ変更機能自体は編集フォームのセレクト経由では正常動作するため、バックエンドは健全でドラッグ操作のフロントエンド実装（特にキーボード操作の後半）に限定した問題。マウス操作については自動化の制約上、断定は避けるが次ラウンドで実機確認を推奨。

S04手順8（当日受付へのLINE予約反映）は、シードデータのスタッフシフト未登録により今回も未検証（環境制約、継続）。

---

# S07〜S09（見積ステータス制御／会計訂正系／締め境界）

**既知バグ2件（未納者一覧の初期表示不具合、レジ締め後エラー文言）すべて修正確認。新規バグ1件。S07は全項目パス。**

- 未納者一覧「飼主単位」「会計単位」タブは、期間未指定時にJST当月が自動セットされAPIが自動発火することを確認（**修正確認**）。
- レジ締め済み期間の会計編集には「⚠ レジ締め済み期間の編集 — 修正理由（必須）」という専用UIが実装され、以前の汎用エラー文言問題は解消（**修正確認**）。

## BUG-006: 部分入金をAPI直叩きすると、ドキュメント記載の400拒否ではなく201で受理され、送信した内訳が黙って上書きされる
- **シナリオ**: S07-S09 / **重大度**: Medium
- 合計金額と一致しない分割金額（例: 合計¥6,264中¥3,000のみ指定）を`POST /accountings/complete`に直接送信すると、期待される400拒否ではなく201で受理され、送信値ではなく全額（`billing_amount:6264, received_amount:0`）に黙って正規化される。UIからは確定ボタンが`remaining!==0`で無効化されるため到達不可で金銭的実害はないが、ドキュメントとの契約不一致とサイレント上書きという性質のため記録。

S09の境界時刻網羅テストは、バックエンドが`completed_at`を常に`time.Now()`で確定しAPIパラメータでの上書き手段がないこと、および本セッションからDBへの直接アクセス経路がないことをソースコードで特定した上で、今回も未実施（環境制約、継続）。それ以外（クリニック分離、UNIQUE制約、締め時間設定値）はすべて問題なし。

---

# S10〜S13（顧客集計ダッシュボード／トリミング併用精算／LIFFペットヘルス／識別子リンク・手動補正）

**既知バグ3件（CPM集計不一致、レジ締め後エラー文言、LIFF白画面503）すべて修正確認。新規シナリオS13を初回テストし、新規バグ3件。**

- 顧客集計ダッシュボードのCPMチップに`cpm_unclassified`区分が追加され、「すべて」件数とチップ合計の不一致は解消（**修正確認**）。
- LIFFペットヘルスアプリの`GET /liff/1/src/main.tsx`が200を返すようになり、白画面問題は解消（**修正確認**）。line-reserveアプリで過去に修正された同種バグ（BUG-402）と同型の問題が今回LIFF側でも解消された。

## S13（識別子リンク・手動補正）: 新規シナリオ初回テスト結果
正常系（オーナーlink→ペットlink→誤り訂正のunlink→正しい組み合わせでのrelink）は一貫してAPIレベルで期待通り動作した。アトミック性（不整合な組み合わせは409で全体拒否、部分書き込みなし）、soft-delete、「既存groupへのadd不可、必ず新規group」という仕様上の制約も実装通り。ただし以下の軽微な不具合を検出。なお、閲覧専用権限・医院カバレッジ不足403のテストは、本セッションで利用可能なアカウントがsystem admin（全権限チェックをバイパス）のみだったため実地API/UI検証ができず、コードレビュー・既存単体テストの確認に留まる（次ラウンドで別権限のテストアカウントを用意しての実施を推奨）。

## BUG-007: identity-links（識別子リンク）画面で、ペットグループ作成失敗時に生のaxiosエラー文言がそのまま表示される
- **シナリオ**: S13 / **重大度**: Medium
- オーナーグループに属さないペット同士のリンクを試みると、バックエンドは`{"error":"pet owner is not in the specified owner identity group"}`という理由を正しく返すが、フロントエンドがレスポンスボディをパースせず、"Request failed with status code 409"という生のaxios例外メッセージをそのまま表示する。操作者は失敗理由を判断できない。

## BUG-008: identity-linksのオーナー検索が全角/半角スペースを正規化しない
- **シナリオ**: S13 / **重大度**: Low
- 「田中　花子」（全角スペース区切り、標準的な入力方法）で検索すると0件になるが、「田中 花子」（半角）では1件ヒットする。DB上のデータ格納形式（全角/半角）と検索クエリの区切り文字が一致しないと発見できない。姓のみで検索する回避策はある。

## BUG-009: `GET /identity-links/pet-groups/:id` 等が `owner_group_id`・`version` を常に0または固定値で返す
- **シナリオ**: S13 / **重大度**: Medium
- 作成直後のレスポンスでは`owner_group_id`が正しく含まれるが、その後のGET取得では`owner_group_id:0`、`owner_group_created_clinic_id:0`、`version:1`固定という不正な値が返る。原因はバックエンド`GetPetGroup`がこれらのフィールドを未設定のまま構造体を手組みしていること（ソースコードで特定済み）。現行UIはこの値を消費していないため可視バグにはなっていないが、公開APIのレスポンス不正としては見過ごせない。

---

# V01（臨床系フォーム）

**既知バグ8件中、7件修正確認・1件再発（BUG-003として上記に統合）。重大な新規バグなし。**

薬剤マスタの親カテゴリなし問題、予防接種タブ一覧の404、見積書タブ再読込での明細消失、追記モーダルでの値消失、トリミング重複チェックのキー誤り、独立登録画面の動物種別「不明」表示、定期健診一覧の編集ボタン遷移先（前回BUG-022）は、いずれも**修正確認**。特に定期健診一覧の編集ボタンは、`/medical-records/{id}?tab=定期健診&checkupId=1`のようにクエリパラメータでタブと対象レコードを直接指定する形に改善され、該当レコードがインライン編集モードで直接開くようになっている。

来院種別の即時保存バグは再発を確認（詳細は上記BUG-003参照）。「保存ボタンが無音で反応しなくなる」というV04・V05既知パターンは、本ラウンドでV01の各フォーム（治療明細の数量境界値等）では再現しなかった。

§2薬量自動計算・ハードゲートの実機確認、§3バイタル境界値、§11入院デイリー記録群、§12トリミング無効化マスタ参照時の挙動は時間都合により未実施（次ラウンドで優先実施を推奨）。

---

# V02（会計・予約・受付系フォーム）

**既知バグ3件は引き続き修正確認。前回未実施だった§3〜§10を重点実施し、新規バグ4件（うち2件はHigh、業務ルールのバイパスに関わる重大な内容）。**

## BUG-010: 会計精算フォームのお釣り欄で負値を入力すると、保存ボタンが完全に無反応になる
- **シナリオ**: V02 / **重大度**: Medium
- 前回「軽微なUX観察」として計上見送りだった事象を正式なバグとして評価。HTML5の`rangeUnderflow`は真になるがボタンは有効なままで、クリックしてもエラー表示・APIリクエストとも一切発生しない。V04で解消されたはずの「無音無反応」パターンが別フォームで確認された。

## BUG-011: レジ締めフォームの「実際のレジ現金」欄で負値を入力すると、締め処理ボタンが完全に無反応になる
- **シナリオ**: V02 / **重大度**: High
- BUG-010と同一の無音無反応パターン。レジ締めは金銭確定という重要操作であり、「反応しない」という体験がシステム障害と誤認され、二重操作や問い合わせにつながるリスクがある。

## BUG-012: 予約編集モーダルが、メモ等の非日程系フィールドのみの変更でも「当日担当医不在」チェックでブロックされる
- **シナリオ**: V02 / **重大度**: High
- 受付済み予約のメモ欄のみを変更して保存すると、日程・担当医を一切変更していないにもかかわらず「本日は医師が出動していないため予約できません」で409拒否される。直接APIで`{notes:...}`のみをPATCHすると200で成功することから、原因はフロントエンドが予約の全フィールド（日付・時間帯を含む）を再送信し、バックエンドがその日程に対する担当医不在チェックを不要に再評価していること。当日シフト未登録・シフト管理が手薄な運用では、受付済み予約の軽微なメモ編集すら一切できなくなる。

## BUG-013: 予約詳細のステータスドロップダウンから「受付済→診療中」へ直接遷移でき、カルテ作成必須の業務ルールをバイパスできる
- **シナリオ**: V02 / **重大度**: High
- 仕様上は、この遷移はカルテ／トリミング記録の作成と同時にのみ許可され、単独では拒否されるべきだが、予約詳細のステータスドロップダウンから直接「診療中」を選択すると、カルテ未作成のまま200で遷移が成立してしまう。診療記録なしに診療フローが進行してしまうため、監査・業務整合性の観点で重大。カンバンD&D経由の同一遷移は本ラウンドでは確定的な再現ができなかったが、少なくともこの代替導線には保護がない。

前回report済みの「カンバンクリックでモーダルが開かない現象」は本ラウンドでは再現せず（一過性の事象だった可能性）。§3〜§10（クレジット訂正・返金・レジ締め・見積書・予約・当日受付・受付ステータス・シフト）を重点実施し、§10シフトフォームはおおむね良好（#1・#2・#4・#5パス）。§11休診日設定・§12在庫登録は時間都合で未実施。

---

# V03（飼主・ペット・スタッフ・権限系フォーム）

**既知バグ8件すべて修正確認。フォローアップ3項目（§7副作用、軽減税率境界値、§5同一メール重複拒否）もすべて正常動作を確認。新規バグ1件。**

生年月日欄の表示切り詰め問題（サイグラムウィジェットとの幅競合）はレイアウト変更により解消（**修正確認**）。owner_name_kana/pet_name_kana 400エラー、医院マスタ関連バグ、スタッフ登録の永久ブロック、権限グループの自己剥奪ガード（BUG-140）もすべて**修正確認**。

## BUG-014: スタッフ編集パネルの「所属医院」選択肢に、非アクティブ化済み・重複名の医院が無差別に表示される
- **シナリオ**: V03 / **重大度**: Low
- 同名医院を2件作成し一方を無効化した状態でスタッフ編集パネルを開くと、無効化済みの医院が有効な医院と全く同じ見た目・ラベルで選択肢に現れ、どちらがどのIDの医院かをUI上で判別する手段がない。退職・閉院した医院を誤ってスタッフに割り当ててしまうリスクがある。

---

# V04（設定マスタ系フォーム）

**既知バグ3件すべて修正確認。今回新たにテストした§2/§5/§7/§8でも「保存ボタン無音無反応」パターンの再発なし。新規バグ3件（うち1件は追加確認要）。**

薬剤マスタの投与量パラメータ、保険マスタの補償率、締め時間設定の個別休診日は、いずれも明確なエラー表示とともに拒否されるようになり、境界値も正しく受理されることを確認（**修正確認**）。3フォームにまたがり同一の修正パターンが確認できたことから、共通コンポーネントレベルでの是正が行われた可能性が高い。

## BUG-015: Lステップ連携設定のLIFF IDを空にして保存しても、実際にはクリアされない
- **シナリオ**: V04 / **重大度**: High
- 仕様上LIFF IDは「空文字でクリアできる唯一の項目」だが、保存操作は200を返すにもかかわらず、`GET /lstep-settings`で再取得すると値が変化していない。成功トーストが出るため、ユーザーはクリアできたと誤認する。

## BUG-016: 処置マスタで子処置（末端・孫なし）を削除すると500 Internal Server Errorになる
- **シナリオ**: V04 / **重大度**: Medium
- 子を持たない末端の処置レコードを`DELETE`すると500エラーになる（2回再現）。親処置の削除（子が存在するため409拒否）は正しくハンドリングされているのに対し、末端レコードの削除処理に不具合がある。無効化（is_active=false）での運用回避は可能。

## BUG-017: 診療項目マスタの重複名エラートーストが、実際の入力値ではなくタブ種別ラベルを表示する
- **シナリオ**: V04 / **重大度**: Low / **対応済み**
- **確定原因**: UNIQUE 違反時の `FromGORM` が `consultation '' already exists`（識別子空）を返し、FE の `localizeAlreadyExistsMessage` が空名をタブと同名の種別ラベル「診察」だけに落としていた。シフトテンプレートは `code` + `params.name` で実名を出していた。
- **対応**: 診療項目5タブ（診察・検査・処置・予防接種・定期健診）をシフトテンプレートと同じ name-conflict（実名エコー）に揃え、空識別子の種別ラベル単独表示をやめた。

§9（LINE予約ページ設定）は、共有セッション上で別エージェントが同一データを同時にテストしている痕跡（テキスト全欄に他エージェントのテストマーカーが残存）を検出したため、破壊的な検証を意図的に見送った。次ラウンドは単独セッションでの再実施を推奨。

---

# V05（認証・LINE関連フォーム）

**既知バグ5件すべて修正確認。特にBUG-016（旧）の全画面クラッシュは完全に解消。新規実施したV05-9・V05-10もバグなし。新規バグ1件（Low）。**

パスワード変更ダイアログの全画面クラッシュ（`axios.isAxiosError is not a function`）は再現せず、401エラー時もダイアログ内にインラインで正しくエラー表示されることを確認（**修正確認**）。セッションログアウトも発生しなかった。LIFFアカウント連携アプリの503白紙、Lステップベースurlプレースホルダー、慢性疾患コード重複エラーの識別子欠落、健診対象者抽出フォームの負値エラーメッセージも、いずれも**修正確認**。

## BUG-018: パスワード変更ダイアログで新旧パスワード不一致のバリデーションエラー時、正しく入力済みだった「現在のパスワード」欄の値も消去される
- **シナリオ**: V05 / **重大度**: Low
- 前回報告のBUG-009（追記モーダルで同様の症状）と同一パターン。新パスワードと確認用パスワードが不一致の場合、エラーメッセージは正しく表示されるが、3欄すべてが空文字列にリセットされ、正しく入力済みだった「現在のパスワード」欄も失われる。データ損失や機能停止はないが、再入力の手間が生じる。

マイ予約一覧のペット名不一致は、有効なLINEトークンが必要なため今回も未検証（環境制約、継続）。V05-1・V05-3・V05-4・V05-5の一部・V05-11〜18は時間都合により未実施。

---

## 未実施・要フォローアップ項目（次ラウンド以降で優先実施を推奨）

- S02の閲覧専用権限グループでのアカウント検証、S08手順5（一般ロールでの負例テスト）: 専用テストアカウントの認証情報が用意されていないため、複数ラウンドにわたり未実施。次ラウンドまでに一般ロール・閲覧専用ロールのテスト用ログイン情報を準備することを強く推奨。
- S09境界時刻の網羅テスト: `completed_at`がサーバー側で`time.Now()`固定でありAPIでの上書き手段がないため、テスト用の時刻上書きパラメータの追加、またはシードデータへの複数時刻帯データの追加を検討されたい。
- S13手順7・8（閲覧専用権限、医院カバレッジ不足403）: system adminアカウントのみでは再現不可のため、別権限のテストアカウントでの実地検証が必要。
- V01: §2薬量自動計算・ハードゲート実機確認、§3バイタル境界値、§11入院デイリー記録、§12トリミング無効化マスタ参照挙動。
- V02: §11休診日設定、§12在庫登録。
- V04: §9 LINE予約ページ設定の単独セッションでの再実施。
- V05: V05-1・V05-3・V05-4・V05-5の一部・V05-11〜18、マイ予約ペット名不一致の再検証（LINE予約が作成できる環境が整えば）。

## 環境上の注意（バグではないが記録）

- 本ラウンドでも複数のQAエージェントが同一ブラウザセッション・共有シードデータ環境で並行稼働しており、クリニックコンテキストの意図しない切替、タブの予期しないナビゲーション、レンダラーの一時フリーズ（いずれもURL再読込で回復・データ損失なし）が複数回観測された。各エージェントは専用タブでの作業を徹底し、影響を受けた場合はAPI直叩き（`X-Clinic-ID`ヘッダ等）で回避する運用としたが、次ラウンド以降はエージェントごとに独立したブラウザセッションを用意することを改めて推奨する。
- V04の§9のように、他エージェントの試験データと同時に触れる可能性がある共有マスタ系画面は、破壊的操作を伴うテストについて単独セッションでの実施を検討されたい。
