# AnimalEkarte 受け入れテスト バグ報告（ラウンド7・再検証）

実施日: 2026-08-22
実施環境: http://localhost:3003（ローカル・seed 003_demo）
対象範囲: `docs/ops/testing/scenarios` 配下 S01〜S13、V01〜V05
ログイン: admin@noavet.jp（表示名「ノア」）/ password1

**本レポートについて**: 前回（ラウンド6）報告した全バグ（BUG-001〜BUG-012）はユーザーによりすべて対応済みとの前提で、S01〜S13・V01〜V05の受け入れテストを一から再実施した。番号は本レポート内で新規に振り直している（BUG-001〜）。修正確認できた項目は本文中で「旧BUG-XXX」として言及するに留め、新規番号は付与していない。

**総括**: ラウンド6で報告した12件の既知バグのうち、**7件は修正確認**できた（旧BUG-001全角/半角検索、旧BUG-002英語エラー日本語化、旧BUG-003薬量ゲートエラー残留、旧BUG-004トリミングオプション消失、旧BUG-005トリミング未保存フラグ、旧BUG-007ステータス直接遷移、旧BUG-010スタッフ所属医院データ消失）。

**1件は部分修正**: 旧BUG-006（予約編集の医師出勤誤爆）は、Round 6で特定できなかった「当日の受付」キャンバン経由の編集導線が今回新たに判明し、そちらの経路では依然として同一の問題が残っていることがソースコード解析で確認された（カレンダー画面経由の編集は修正済み）。BUG-002として再掲。

**2件は未修正のまま継続**: 休診日が予約作成APIでサーバー側に強制されない（旧BUG-008→BUG-003）、在庫登録フォームの必須項目バリデーションが視覚的フィードバックなしに失敗する（旧BUG-009→BUG-004、ソースコードは修正されているように見えるが実行時に反映されていない不可解な乖離あり）。

**2件はテスト環境起因の制約として継続**（アプリのバグではない）: LIFFモック環境による実連携テスト不可（旧BUG-011→BUG-010）、過去ラウンドの残留テスト文字列（旧BUG-012→BUG-011、汚染範囲が本ラウンドで5フィールドに拡大していることが判明）。

新規バグは6件（High 1件 / Medium 2件 / Low 3件）。最も重大なのは**BUG-001（S08、High/重大）**で、権限グループ「一般」のスタッフが会計確定後のクレジット金額訂正をUI・API両方から実行できてしまうという、会計監査上重大なRBAC（権限制御）不備である。Round 6で完遂できなかったS08の負例テストが今回初めてクリーンな条件で実施でき、発見に至った。

なお、V03担当エージェントより「管理者自身の主医院をスタッフの所属医院から外すと、そのスタッフがGET/PUT/DELETEすべて404になり事実上編集・削除不能になる」という重大な所見（NEW-1）が報告されているが、本ラウンドの激しいセッション混線（共有ブラウザセッションでのログインアカウント無断切替）の影響を完全に排除できないため、確定バグとしては計上せず「未実施・要フォローアップ項目」に記載した。次ラウンドでの独立セッションでの再現確認を強く推奨する。

また、複数エージェントが独立に「タスク指示の“林文明 / admin@noavet.jp”という記載が実データと食い違っている（両者は別アカウント）」ことを報告しており、この誤認に起因してV05担当エージェントが誤って別アカウント（林文明 / hayashi@noah-vet.co.jp）のパスワードをリセットする事態が発生した（詳細は末尾「環境上の注意」）。次ラウンド以降はログイン画面のデモアカウント一覧（メールアドレス直書き）を正としてテスト指示を作成することを推奨する。

本ラウンドも複数エージェントの共有ブラウザセッション干渉（頻繁な強制ログアウト、クリニックコンテキストの意図しない切替、ログインアカウントの無断切替）が引き続き激しく、複数のシナリオで完全なUI経由の検証を断念しAPI直叩きおよびソースコード解析での検証に切り替えている。

---

## 重大度サマリ

| 重大度 | 件数 | 該当 |
|:--|:--|:--|
| High | 2 | BUG-001, BUG-002 |
| Medium | 4 | BUG-003, BUG-004, BUG-005, BUG-010 |
| Low | 5 | BUG-006, BUG-007, BUG-008, BUG-009, BUG-011 |

---

# S01〜S03（死亡ペットガード／検査確定ロック／ワクチン自動計算）

**既知バグ2件すべて修正維持を再確認。新規バグ1件。フォローアップ調査1件を完全解決。**

検査編集フォームのdoctor_id PATCHデッドロック、新規カルテ検査タブのボタン欠落ガイダンスは、いずれも**修正確認**を再現エビデンス付きで維持。

**フォローアップ解決**: Round 6で持ち越しとなっていた検査記録1013826の「担当医のみ変更したのに無関係フィールドのエラーが出る」現象は、原因を完全特定した。①親カルテが確定済みのため編集自体が一律409拒否される正常ロック、②項目名なしで結果値だけ入力された既存シード行に起因するフロントエンド側の正常な必須項目バリデーション、という2つの独立した正常機構が重なっていただけであり、**アプリケーションの不具合ではなく既存シードデータの品質問題**と判定した。

## BUG-005: 未来日の予防接種登録がバックエンドAPIレベルで拒否されない（仕様書記載の既知バグBUG-024に該当）
- **シナリオ**: S03 / **重大度**: Medium
- `POST /api/v1/vaccinations`に3日後の未来日を含むペイロードを送信すると、拒否されず`201 Created`で登録が成立してしまう。仕様書`S03-vaccination-next-due-autocalc.md`自身がこの挙動を既知バグ「BUG-024」として追跡しており、Round 7時点でも未解消であることをAPI直叩きで確認した。フロントエンドのDatePicker側（`disabledDays`による選択自体のブロック）は自動操作クリックがコンポーネントの相性問題により機能せず未検証。

参考所見（バグ番号なし・Low）: S03仕様書が「独立画面」（既定=1年後）と定義する到達経路が、実機では確認できたすべての経路（一覧からの新規登録、`petId`付き直接URL）で必ず「カルテタブ」（既定=4週後）に帰着し、独立画面自体に到達できなかった。次回リグレッション自体の対象ではないが、次回ラウンドで仕様書と実装の整合性の確認を推奨する。

---

# S04〜S06（LIFF予約ジャーニー／入院サイクル／カルテロック）

**既知バグ3件すべて修正維持を再確認。新規バグなし。S06核心チェックは6ラウンド連続クリーン。**

診察種別「緊急」「往診」の選択肢構造的排除、入院編集画面の年齢/性別表示、入院ボードのキーボードドラッグ&ドロップは、いずれも**修正確認**を維持。S05の入院サイクル（治療計画・デイリーカルテ・退院処理・二重退院防止・ケアプラン紐付き削除防止）は全項目PASS。S06のカルテロック後の改ざん防止・addendumフローも引き続き健全。

S04は、シフトデータ未投入という既知の環境制約により、Round 6時点の「手順8のみブロック」という記述より広い範囲（手順5の日付選択の時点）でブロックされることが判明した。全スタッフ・全コースの組み合わせで日程が`available:false, reason:"staff_off"`となり、手順5〜12が軒並み検証不能。これはアプリのロジック不具合ではなくテスト環境のマスタデータ不足に起因する可能性が高いが、影響範囲が従来の記述より広いことが分かったため、次ラウンド以降のためにスタッフシフトデータの投入を改めて強く推奨する。

---

# S07〜S09（見積ステータス制御／会計訂正系／締め境界）

**既知バグ1件修正維持。S08の負例テストが今回初めて完遂でき、重大な新規バグを検出。**

会計確定時の金額不一致400拒否（旧BUG-018）は引き続き**修正確認**。S07は全9ステップ、S09の締め処理関連（設定表示・締め実行・差異計算・二重締め拒否・履歴デフォルト期間）もすべて正常動作を維持。

## BUG-001: 一般ロールのスタッフが会計確定後のクレジット金額訂正をUI・APIともに実行できる（RBAC不備）
- **シナリオ**: S08 / **重大度**: High（会計監査・不正防止の観点で重大）
- Round 6では共有セッションでの並行ログイン競合により完遂できなかった一般ロールでの負例テストが、今回クリーンな条件で実施でき、重大な権限制御の不備を発見した。一般権限グループのスタッフ（安田希恵/admin@example.com）でログインし会計詳細画面を開くと、本来執行ロールのみに見せるべき「クレジット訂正」ボタンが表示される。クリックして理由を入力し保存すると、`POST /api/v1/accountings/:id/credit-correction`が`200`で成功し、実際にDBの`payments[0].updated_at`が更新されることを確認した。同一画面の「会計をキャンセル」「修正を保存する」ボタンも一般ロールに表示されており、権限漏れが波及している可能性が高い（今回は`credit-correction`のみ実行確認、他2つは表示確認のみ）。フロントエンドの権限ガードとバックエンドの権限チェックの双方に不備がある可能性が高い。
- **2026-08-23 フォローアップ: 不成立。** 実行中 DB で `admin@example.com` は staff_id=8（安田 希恵）、`is_system_admin=false`、`staff_permission_groups.group_ids=[1]`（clinic 1 の「執行」。group 2 が「一般」）。執行の `accounting-post-close-edit` は `can_view=true, can_create=false, can_edit=true, can_delete=false`（rule id 187）。`POST /api/v1/accountings/999999999/credit-correction` は権限通過後に 404（403 ではない）。バックエンドは `billing/routes.go` で `accounting-post-close-edit:edit` を要求し、フロントの「クレジット訂正」は `AccountingDetail.tsx` の `usePermission(ResourceAccountingPostCloseEdit)`、「会計をキャンセル」は `accounting-cancel:edit`。権限定義・ガードは変更していない。誤認の主因は DEV ログイン画面がこのアカウントを「一般」と表示していること（`LoginForm.tsx` の `permissionLabel`）。S08 負例の正しい一般アカウントは `vet@example.com`（staff 9 / group 2）。

## BUG-009: 新規会計登録画面のpost_close_reason入力欄が画面外に表示され気づきにくい
- **シナリオ**: S08 / **重大度**: Low
- レジ締め済み日に新規会計を確定しようとすると、トーストと`400`エラーが返るが、理由入力欄（`textarea#postCloseReason`）は画面下部・現在のビューポート外に表示され、エラー発生時の自動スクロールやフォーカス移動が行われない。機能自体は存在し最終的に利用可能だが、初見のオペレーターは「編集できない」と誤認しやすい。

S09の境界時刻厳密テスト（13:30:00ちょうど、日跨ぎEMG）は、DBへの直接アクセス手段（psql/docker等）がテスト環境になく、Round 6と同様に引き続きブロック。S08手順10の監査ログメタデータ（`post_close:true`）内容も、閲覧可能なAPI/UIが見つからず間接確認に留まった。

---

# S10〜S13（顧客集計ダッシュボード／トリミング併用精算／LIFFペットヘルス／識別子リンク・手動補正）

**既知バグ2件（S13）すべて修正確認。新規バグ1件。**

## BUG-006: トリミング予約からのカルテ作成拒否時、トースト通知に英語の生エラーメッセージが表示される
- **シナリオ**: S11 / **重大度**: Low
- トリミング種別の予約を「受付済→診療中」へ進めるにはカルテ作成が前提条件となるが、トリミング予約からカルテを作成しようとすると`POST /api/v1/medical-records`が`400 {"error":"appointment must use a general reservation type for a medical record"}`で拒否される（「カルテ作成には一般予約種別が必要」という制約自体は仕様通りの可能性がある）。この際、画面上部のバナーは適切な日本語メッセージにローカライズされているが、画面左上のトースト通知は英語の生メッセージがそのまま表示される。旧BUG-002の修正（`extractApiErrorMessage`）は409コンフリクト系のフォールバックが主眼であり、この400番台バリデーションエラー・トースト表示経路まではカバーされていないことを示す。なお、単独のトリミング予約のみでは会計待ちへの正しい遷移経路を特定できず、S11の完全なE2E検証（併設の一般診察予約との合算精算を含む）は完遂できなかった。次ラウンドでの想定運用フローの確認を推奨する。

BUG-001（全角/半角スペース検索正規化）・BUG-002（409未マップエラーの日本語フォールバック）は、20件以上のクエリと複数の実在飼主名での検証により、いずれも**修正確認**。S13の正常系（owner紐付け→pet紐付け→履歴→解除→再紐付け）も全ステップ再確認済み。S13手順7・8（権限系負例テスト）は引き続き専用テストアカウント未提供のためブロック。S12のLIFFヘルスカードAPIは、バックエンド側にも`LIFF_MOCK=true`によるローカル開発専用の認証バイパス機構があり（release モード起動時ガードあり、本番混入なし）、実トークン検証はテスト環境制約により引き続き未実施。

---

# V01（臨床系フォーム）

**既知バグ3件すべて修正維持を再確認。新規バグなし。**

薬量ハードゲートのエラー残留（旧BUG-003）、トリミング登録のオプションチェック消失（旧BUG-004）、トリミングマスタの未保存フラグ残留（旧BUG-005）は、いずれも複数回の独立した再現テストで**修正確認**。§12無効化マスタ参照、§11入院デイリー記録の一意性制約も維持を確認。§1の一部（petId無し新規カルテのリダイレクト、死亡ペットの選択不可表示）と、6モジュール横断の404異常系（存在しないID）も新規に検証しすべて正常動作を確認した。

§4〜§10、§11項目2、§12項目6、およびFIELD-LEVEL-PROTOCOL.mdに基づく網羅的フィールド単位検証は、時間配分の都合により次ラウンドへ持ち越し。

---

# V02（会計・予約・受付系フォーム）

**既知バグ4件中1件修正確認、1件は部分修正（新導線で再発）、2件は未修正のまま継続。新規バグ1件。**

## BUG-002: 予約編集モーダルが「当日の受付」キャンバン経由の場合、メモのみの変更でも「当日担当医不在」チェックでブロックされる（既知バグ・部分修正）
- **シナリオ**: V02 / **重大度**: High
- Round 6ではUI導線自体を特定できなかったが、今回「当日の受付」キャンバンの「編集」ボタン（受付済列のカードのみ表示）という導線を新たに特定した。カレンダー画面（`/reservations`）経由の編集は、バックエンドの`reservationScheduleFieldsChanged()`とフロントエンドの差分送信ロジックの両方が正しく修正されており問題ない。しかし受付キャンバン経由の編集（`use-reception-modal-handlers.ts`）には別のバグが残存する: 編集フォームを開く際に予約の実際の終了時刻を使わず`開始+1時間固定`で終了時刻を再構築しており、かつ保存時は差分比較なしに全項目を無条件送信する。多くの予約は所要時間が1時間未満のため、この時点で実データと異なる終了時刻がフォームにセットされ、メモだけを変更して保存しても「スケジュールが変更された」とバックエンドに誤認され、当日医師が退勤済み等の状況で409エラーが発生しうる。ソースコード解析による論理的再現性は確実だが、セッション不安定によりライブでの409発生の直接キャプチャは今回未達成。
- **2026-08-23 フォローアップ: 修正済み。** 受付 transform が API の `end_time` を落としていたのを `toJSTWallDate` で復元し、保存はカレンダーと同じフィールド差分（未変更の `start_time`/`end_time` は送らない）。バックエンドの `reservationScheduleFieldsChanged` は未変更。テスト: `use-reception-modal-handlers.test.ts` / `transforms.test.ts`。

## BUG-003: 休診日を設定しても、予約作成APIへの直接リクエストではバックエンド側の検証がなく作成できてしまう（既知バグ・未修正）
- **シナリオ**: V02 / **重大度**: Medium
- ソースコード調査で`backend/internal/reservation/`パッケージ全体に休診日（`clinic_holidays`）を参照する箇所が一切ないことを確認。休診日を登録した日付に対し`POST /api/v1/reservations`を直接実行すると、`201 Created`で予約が成立してしまうことをAPI実地検証で再確認した。

## BUG-004: 在庫登録フォームで必須項目未入力のまま登録を試みても、エラーメッセージ等の視覚的フィードバックが一切ない（既知バグ・未修正）
- **シナリオ**: V02 / **重大度**: Medium
- ソースコード上は`useActionState`ベースのバリデーション・`FormFieldError`表示ロジックが正しく実装されているように見えるが、ライブ環境で複数回・条件を変えて再現したところ、必須項目未入力のまま送信してもエラー要素（`aria-invalid`・インラインエラー・トースト）が一切表示されず、送信自体は正しくブロックされる（データ不整合は生じない）ものの、ユーザー視点では「反応しない」ボタンに見える。ソースコードと実行時挙動の乖離の原因は今回のセッション内では特定に至らなかった。

## BUG-007: 在庫登録フォームのヘッダー部分に、フォームに紐付いていない無反応な「登録」ボタンが重複して存在
- **シナリオ**: V02（新規発見） / **重大度**: Low
- 画面上部（見出し「在庫登録」の右）と、フォーム下部の2箇所に見た目がほぼ同一の「登録」ボタンが存在する。上部のボタンは`closest('form')`が`null`＝どの`<form>`にも属しておらず、クリックしてもネットワークリクエスト・エラー表示・状態変化のいずれも発生しない完全な無反応ボタンである。BUG-004の「反応しないように見える」というユーザー体感を悪化させる一因になりうる。

旧BUG-007（ステータス直接遷移によるカルテ必須ルールバイパス）は、カルテ0件の予約への直接API `PATCH {status:"in_consultation"}` が明確に`409`で拒否されることを確認し**修正確認**。会計精算のお釣り手動上書き・レジ締め実際現金のマイナス値の2件は、ソースコード上は修正が維持されていることを確認したが、セッション終盤の重度の環境不安定（10近い並行エージェントタブによる負荷と推測）により、ライブUIでの最終確認は未達成（BLOCKED、次ラウンドでの確定検証を推奨）。

---

# V03（飼主・ペット・スタッフ・権限系フォーム）

**最重要既知バグ（BUG-010）を完全な証跡チェーンで修正確認。新規回帰チェックも問題なし。**

旧BUG-010（スタッフ編集パネル保存時、非アクティブ化された医院への既存所属が無言で消失する）は、テスト用スタッフを新規作成し「医院割当→医院無効化→保存前GET→無関係項目のみ編集して保存→保存後GET→医院再有効化→再GET」という完全なbefore/afterチェーンを実機で通し、**修正確認**した。GET自体のフィルタ挙動も変更され、無効医院分の割当も正しく返るようになっている。併せて導入された「非adminによる権限グラフ外への医院割当PUTを拒否する」新設チェックについても、正規adminによる通常の医院割当変更（3件の独立試行）が誤ってブロックされていないことを確認し、回帰なし。旧BUG-014（無効医院への「（無効）」ラベル表示）、権限グループ重複名トーストの実名エコー、自己剥奪ガード（BUG-140）もすべて**修正確認**を維持。

**未確定の要フォローアップ所見（バグ番号なし）**: 管理者自身の主医院をスタッフの所属医院リストから外す`PUT`を実行すると、その直後から同一スタッフへの全GET/PUT/DELETEが404を返すようになり、当該adminセッションからそのスタッフが完全に不可視・編集不能・削除不能になる現象を、3件のテスト用スタッフすべてで再現した。ただし検証中、共有ブラウザセッション上で他エージェントの並行操作によるログインアカウントの無断切替（林文明→ノア）を複数回観測しており、このセッション混線が本現象に関与している可能性を完全には排除できない。確定バグとしてではなく、次ラウンドでの独立セッションでの再検証が必要な所見として記録する（詳細は「未実施・要フォローアップ項目」参照）。

---

# V04（設定マスタ系フォーム）

**既知バグ3件すべて修正維持を再確認。新規バグなし。長年の§9データ汚染も今回解消。**

Lステップ連携設定のLIFF IDクリア、処置マスタの子処置削除500エラー、診療項目マスタの重複名トースト実名表示は、いずれも**修正確認**を維持。コアチェック対象7フォーム（動物種類・保険・支払方法・薬剤マスタ・予約区分・シフトパターン・診断カテゴリ）もすべて期待通り動作。

§9（LINE予約ページ設定）に複数ラウンドにわたり残留していた`QAR5-1787312315230`系のテストマーカー汚染を、backend seed CSVから原本値を特定した上で今回すべて除去・復元した。§5・§6・§10も読み取り専用で検証し問題なし。§8（Lステップ連携のタグ設定・コードマッピング・配信優先順位）の未実施分も今回すべて補完実施し問題なし。

---

# V05（認証・LINE関連フォーム）

**既知バグ1件のうち一部分岐で新規バグを検出。BUG-011・BUG-012は環境要因として継続。**

## BUG-008: パスワード変更ダイアログで、バックエンドのパスワード強度エラー（400）時に「現在のパスワード」欄までクリアされる
- **シナリオ**: V05 / **重大度**: Low
- 旧BUG-018の修正により、「現在のパスワードが不一致」（401）分岐では「現在のパスワード」欄が保持されることを再確認した。しかし、新パスワードが強度ポリシー（英数字混在必須）を満たさない場合の`400`エラー分岐では、現在のパスワード欄を含む3欄すべてが空文字にクリアされることを新たに発見した（2回再現確認）。`ChangePasswordDialog.tsx`のエラーハンドリングが401分岐のみを対象としており、他の4xxエラー全般はカバーしきれていないと見られる。

## BUG-010: ローカル環境のLIFFアカウント連携がモック実装のため、実際のトークン検証が実機検証できない（既知バグ・環境要因のため継続）
- **シナリオ**: V05 / **重大度**: Medium
- `VITE_LIFF_MOCK=true`が引き続き設定されており、不正・存在しないトークンでアクセスしても即座に「連携が完了しました」と表示される（バックエンドAPIは呼ばれない）ことを再確認した。コード修正の対象ではなくテスト環境設計上の制約。

## BUG-011: line-reserveアプリの表示ページ設定に、過去ラウンドのテストデータ残留文字列が付与されたままになっている（既知バグ・範囲拡大）
- **シナリオ**: V05 / **重大度**: Low
- Round 5由来の`QAR5-1787312315230`という文字列が、ヘッダーだけでなくV05-9の表示ページ編集5フィールド（header_text／reservation_notice／cancel_notice／privacy_policy／request_example）すべてに付与されたままであることを新たに確認した。ソースコードに該当文字列は存在せず、seedデータ起因の残留と判定。

V05-6のバリデーション系（空白のみ、電話番号形式、ペット名必須）は正常動作を確認。実スタッフを指名した完全な予約フローは、シフトデータ未投入によりRound 2から引き続きブロック。V05-7〜V05-18の大半は時間都合により未実施（次ラウンド持ち越し）。

---

## 未実施・要フォローアップ項目（次ラウンド以降で優先実施を推奨）

- **V03 NEW-1の独立セッションでの再現確認（最優先）**: 管理者自身の主医院をスタッフの所属医院から外すと当該スタッフが404で完全に不可視・編集不能になる現象。セッション混線の影響を排除するため、他エージェントと重複しない専用ブラウザプロファイル/シークレットウィンドウでの再現確認を強く推奨する。
  - **2026-08-23 フォローアップ: 再現し修正済み。** 原因は `resolveStaffWithClinic` / in-clinic lock / `hasCurrentClinic` が request clinic の assignment を要求し `isSystemAdmin` を見ていなかったこと。system admin は ID ロック + 残存所属医院スコープで GET/PATCH/PUT/DELETE が 404 にならない。non-admin は引き続き 404。職種 FK は残存所属医院で所有確認する。テスト: `staff_admin_removed_request_clinic_test.go`。残存 V03 テストスタッフ（ID 1000000003〜1000000005）は system admin から削除可能になったはずだが、本フォローアップでは DB 直接削除はしていない。
- BUG-002（V02、受付キャンバン経由の予約編集）のライブでの409発生の直接キャプチャ。今回はソースコード解析による論理的確認に留まった。
- BUG-004（V02在庫登録フォーム）のソースコードと実行時挙動の乖離原因の特定（開発サーバーのキャッシュ、Reactハイドレーション問題等の切り分け）。
- V02の会計お釣り手動上書き・レジ締め実際現金マイナス値の2件のライブUIでの確定検証（今回は環境不安定によりソースコード確認のみ）。
- S02手順8・S13手順7/8: 権限系の負例テストに必要な専用テストアカウントでの実地検証。S08は今回初めて完遂できたため、同様の手法（他エージェントと時間帯をずらす、または専用セッション確保）を他シナリオにも適用することを推奨。
- S09境界時刻の網羅テスト: DBへの直接アクセス手段（psql/docker等）がテスト環境に必要。
- S11のトリミング＋一般診察併用精算の完全なE2Eフロー（想定運用の確認を含む）。
- S04手順5以降・V05-6のフル予約作成フロー: 実スタッフのシフトデータがシードに一件も投入されていないため、Round 2以降継続してブロック。シード投入スクリプトへのシフトデータ追加を強く推奨（今回、影響範囲がより広いことが判明したため優先度を上げるべき）。
- V01: §4〜§10、§11項目2、§12項目6、FIELD-LEVEL-PROTOCOL.mdに基づく網羅的フィールド単位検証。
- V05: V05-7〜10・V05-12〜18（Lステップ関連個別検証）、マイ予約ペット名不一致の再調査（シフトデータ整備後）。

## 環境上の注意（バグではないが記録）

- **admin@noavet.jpのパスワードは`password1`のまま変更なし**（Round 6と同一）。
- **重要: タスク指示の「林文明 / admin@noavet.jp」という組み合わせは実データと食い違っている**。ログイン画面のデモアカウント一覧によれば、林文明は`hayashi@noah-vet.co.jp`、admin@noavet.jpの表示名は「ノア」であり、両者は別アカウント（いずれもシステム管理者ロール）。本ラウンドではこの誤認によりV05担当エージェントが誤って林文明（hayashi@noah-vet.co.jp）アカウントのパスワードをリセットする事態が発生し、結果的に共有ブラウザの全タブが強制ログアウトされ他エージェントの作業に影響した。次ラウンド以降はログイン画面のデモアカウント一覧（メールアドレス直書き）を正としてテスト指示を作成することを強く推奨する。なお、林文明（hayashi@noah-vet.co.jp）の現在のパスワードも`password1`である。
- 本ラウンドも複数QAエージェントの共有ブラウザセッション・共有`localStorage`・共有シードデータへの並行アクセスに起因する干渉が引き続き激しく、頻繁な全タブ強制ログアウト、クリニックコンテキストの意図しない切替、ログインアカウントの無断切替、他エージェントによるレコードの並行編集が多数観測された。これにより複数のシナリオで完全なUI経由の検証を断念し、API直叩き・ソースコード解析での検証に切り替えている。次ラウンドはエージェントごとに独立したブラウザセッション（別ウィンドウ・別プロファイル等）を用意することを改めて強く推奨する。
- V03の検証で作成したテスト用スタッフ3件（ID 1000000003〜1000000005、名前に「V03」含む）が、上記の未確定所見（NEW-1）により404状態となり削除できず残存している。DB直接操作での削除が必要。
