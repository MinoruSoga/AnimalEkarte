# AnimalEkarte 受入テスト バグ報告 — クローズ

- **元レポート実施日**: 2026-08-09
- **r2 クローズ**: 2026-08-10
- **r3 コード land**: 2026-08-11（main 通常 merge+push）
- **r3 環境反映 + ブラウザ再確認**: 2026-08-12
- **main tip（本更新）**: 下記 docs コミット時点の `origin/main`
- **実施環境**: http://localhost:3003（bind-mount main · `docker compose up -d --force-recreate frontend backend` 実施済み）

## 結論

| 区分 | 状態 |
|------|------|
| BUG-001〜026, 028〜032 | **FIXED on main** |
| BUG-001 / 002 | **FIXED** — r3 FE 直URLガード + 2026-08-12 ブラウザ PASS |
| BUG-027 | **SPEC**（実装不要） |
| BUG-033 | **FIXED** — コード r2 + 2026-08-12 ブラウザで完了検査の結果値/削除/追加が disabled（対象 ID 1014565。旧 1014563 は DB に無し） |
| BUG-034 / 035 | **FIXED** — r3 + 2026-08-12 ブラウザ/API 再確認 PASS |
| S05 ブラウザ E2E | **PASS**（2026-08-11） |
| S06 ブラウザ E2E | **PASS（ガード・ロック・notes 保持）** — 監査ログの DB 生 SQL 裏取りのみ任意未実施 |

**open / NG のコード欠陥は残っていない。**

## r3 実装（main）

| BUG | 要約 | tip | merge |
|-----|------|-----|-------|
| 034 | InquirySummary に notes 往復 | `fec644128` | 64cada44a |
| 035 | 確定済 fieldset lock（contents 撤去） | `a3643a58b` | d96ea911c |
| 002 | medical-records/new 死亡 hard-stop | `d9c9b193b` | 4d3c214de |
| 001 | accounting/new 死亡 FE ブロック | `6370be3ea` | c8623130b |

## 2026-08-12 再確認ログ（migrate 未適用）

**Unit（Docker `--entrypoint ''`）**
- BE billing/medicalrecord focused deceased + exam seal: **PASS**
- FE vitest 7 files / **145 passed** | 3 skipped

**環境**
- frontend/backend は host `main` を bind-mount。image rebuild は no-op（CACHED）のため **force-recreate** でプロセス更新。

**ブラウザ（八王子・一般スタッフ）**
| 項目 | 結果 |
|------|------|
| BUG-002 `/medical-records/new?petId=1000003` | 「死亡したペットは新規カルテを作成できません」表示 · 編集フォーム無し |
| BUG-001 `/accounting/new?petId=1000003` | 「死亡したペットは会計を作成できません」· 閲覧専用 · 確定操作無し |
| BUG-033 `/examinations/1014565`（status=completed） | 結果 textbox・項目削除・項目追加 disabled。保存ボタン無し |
| BUG-035 `/medical-records/1080036`（確定済） | バナー + 主訴/治療方針等 disabled。保存無し。追記のみ |
| BUG-034 | API GET `medical-records/1425546` の `inquiry.notes` = `UAT-r3 治療方針 keep`（確定後も保持）。画面 1080036 でも治療方針が `# 治療方針` に戻っていない |

## 人間側の任意・別レーン

1. **staging へ main を取り込む**（本レポート範囲外 · 人間のみ）
2. S06 監査ログの DB 生裏取り（任意）
3. BUG-027 は変更しない

以上。

---

## 2026-08-12 再受入テスト実ブラウザ再確認（追加記録）

前項の「2026-08-12 再確認ログ」と同じ localhost 環境を再度開き、画面上の実状態を確認した。

- **BUG-001 / BUG-002**: PASS。`/medical-records/new?petId=1000003` は「死亡したペットは新規カルテを作成できません」、`/accounting/new?petId=1000003` は「死亡したペットは会計を作成できません」と表示され、死亡ペットの新規処理を拒否した。
- **BUG-034**: PASS。確定済みカルテ `1425558` を再表示し、保存済みの主訴・治療方針が保持されていることを確認した。
- **BUG-033**: **再現（NG）**。完了済み検査 `1014563` は、結果値 input、削除ボタン、保存・削除ボタン、検査項目追加が有効表示された。修正確認対象として記載された `1014565` は画面上で対象レコードを確認できず、`1014563` の実在する完了レコードで再現した。
- **BUG-035**: **再現（NG）**。確定済みカルテ `1425559` は「確定済み」「編集できません」と表示される一方、主訴・治療方針 textarea が `disabled=false` で残り、画面には「追記する」に加えて保存操作が残った。
- **S04**: 日付選択まで再実施。2026年8月の1〜31日がすべて disabled で、時間枠・予約確定・キャンセルは実行不能だった。
- **S12**: `clinic_id=1` の mock ヘルスカードは飼主「テストユーザー」を表示し、ペット情報なしを確認した。有効 LINE token は未準備。
- **S13**: `/identity-links` は受付トップへ戻り、権限グループ画面にも identity-links の実操作導線は確認できなかった。

したがって、**この再実施時点では全受入テスト PASS／全バグ対応済みとは判定できない**。BUG-033/035 は、対象 ID または稼働中コンテナの反映状態を含めて再調査が必要。

---

## 2026-08-13 追加受入テスト結果

- **S04**: お客様情報 → 一般診察 → 指名なし → 日付選択まで再実施。2026年8月1〜31日の全日付が disabled、`次へ` も disabled のため、時間枠・予約確定・キャンセル・枠解放は実施不能。
- **S06**: `audit_logs` をDBで読み取り確認。カルテ `1425558` / `1425559` について create・finalize・update があり、actor_id・created_at・old_value/new_value が記録されていた。監査ログのDB裏取りは PASS。
- **S13**: 権限グループ「閲覧専用」の編集画面で「同一飼主・ペット連携」リソースと表示/作成/編集/削除列を確認。複数医院所属・identity-links の実リンク／unlink 操作は前提データ不足で未実施。
- **V02**: 新規見積書フォームでタイトル未入力時の「作成」ボタン非活性を確認。

BUG-033/035 は今回も、実在する完了検査 `1014563` と確定済みカルテ `1425559` で編集可能状態が再現しているため、修正済みとは判定しない。

---

## 2026-08-13 UAT継続結果（V03〜V05）

- **V03 権限グループ**: 新規作成フォームでグループ名を空保存し、「グループ名を入力してください」を確認。保存されなかった。
- **V04 治療プラン／薬剤／予約区分／保険／問診テンプレート**: 各新規フォームで必須項目を空保存し、それぞれ名称またはタイトルの必須エラーを確認。テストレコードは作成していない。
- **V05 パスワードリセット**: `/forgot-password` の空送信で「メールアドレスを入力してください」、`/reset-password` のトークンなしで「無効なリンクです」を確認。
- **V02 存在しないID**: `/accounting/999999999` は「データが見つかりません」、`/estimates/999999999/edit` は「見積書が見つかりません」、`/owners/999999999` はページ未検出画面を表示し、白画面のままにはならなかった。

なお、V01〜V05の全フォームについてC1/C2/C3を個別に完遂した状態ではなく、S04の予約確定系、S12の有効LINE token、S13の複数医院リンク操作も前提不足で未実施。

---

## 2026-08-13 UAT継続結果（V01/V04追加）

- **V01 カルテ**: `/medical-records/new` はペット選択画面へ遷移。存在しないカルテIDはカルテ詳細のエラー状態を表示。
- **V01 臨床系存在しないID**: `/vaccinations/999999999`、`/hospitalization/999999999/edit`、`/trimming/999999999` は各詳細画面のエラー状態を表示。
- **V04 主訴／入院プラン／ケージ／職種／トリミングコース種別**: 新規フォームの空保存で必須エラーを確認し、テストレコードは作成していない。
- **V04 動物種類**: 一覧画面は表示されたが、新規登録ボタンがなく、ブラウザからの新規登録フォームは到達不能。
- **V04 キャンペーン**: 新規フォームは表示されたが、空保存時に「入力してください」形式のエラーを確認できず、入力項目構成の追加確認が必要。テストデータ作成は行っていない。

この時点でも、全フォームのC1/C2/C3網羅、S04の予約確定系、S12の有効LINE token、S13の複数医院リンク操作は未完了／BLOCKED。

---

## 2026-08-13 UAT継続結果（V02/V03/V05追加）

- **V02 予約作成モーダル**: 患者・予約区分未選択のまま「予約を確定」を実行し、「患者を選択してください」「予約区分を選択してください」を確認。予約は作成されなかった。
- **V03 スタッフ**: `/settings/staff` の新規フォームへ到達し、氏名・職種・資格番号・メール・パスワード等の入力欄を確認。空保存時は権限関連の `system administrator access required` 表示が残り、通常の氏名必須エラーをこの導線では確認できなかった（PARTIAL）。
- **V02 レジ締め**: `/accounting/close` の対象日・午前／午後／緊急・プレビュー導線を確認。実査金額を伴う締め確定は不可逆操作のため、確定データ未準備のまま未実施。
- **V05 LINE／Lステップ**: `/line-reservation/settings`、`/line-reservation/page-editor`、`/line-reservation/slots`、`/settings/integrations/lstep`、`/settings/lstep/tags`、`/lstep/analytics`、`/lstep/checkup-sync` の画面到達と主要入力項目を確認。実LINE token・実配信・CSVファイルは未準備のため実連携は未実施。

### 会計・飼主フォーム追確認

- **V02 返金ダイアログ**: 精算済み会計 `1384150` で返金ダイアログを開き、返金金額0の状態では「登録する」が disabled であることを確認。返金は実行していない。
- **V03 飼主登録**: `/owners/new` の空登録で飼主名・よみ・電話番号の必須エラーを確認。テスト飼主は作成していない。

### 受付・LIFF追確認

- **V02 受付カンバン**: `/` の「新規予約登録」モーダルで患者・予約区分未選択のまま確定し、「患者を選択してください」「予約区分を選択してください」を再確認。予約は作成されなかった。
- **S12 mockヘルスカード**: `/liff/health-card?clinic_id=1` を開き、飼主「テストユーザー」・「ペット情報はありません」を確認。有効LINE tokenによる本人連携は未実施。

### 主要ルート巡回追確認

- 受付、飼主、予約、カルテ、検査、会計、入院、予防接種、トリミング、健診、在庫、見積、シフト、集計、権限、締め時間、LINE予約、Lステップの主要20ルートを巡回し、画面到達を確認。
- 臨床・会計・見積等の存在しないIDを直叩きし、エラー状態または未検出画面を確認。今回の巡回では白画面のまま確定するケースは確認しなかった。
- これはルート到達性の確認であり、各画面の保存永続性、実LINE連携、複数医院権限、全C1/C2/C3を完了扱いにはしていない。

### Lステップ追確認

- `/lstep/checkup-sync` で検診種別未選択の必須エラーを再確認。年齢最小・最大などの検索条件入力欄も確認したが、外部送信は行っていない。
- `/settings/lstep/tags` でタグ一覧、対象者一覧、CSV導線、一括解除導線を確認。一括解除は実行していない。

### 設定画面の再到達確認

- `/settings/clinic` は画面表示できたが、医院マスタが0件で、医院編集・複数医院の確認前提を満たしていない。
- `/settings/staff`、`/settings/permission-groups`、`/settings/closing-time` は一覧・設定画面に到達し、既存のスタッフ・権限グループ・締め時間プレビューを確認した。
- この確認では既存設定の変更・保存は行っていない。

---

## BUG-038: 医院マスタ画面が有効医院を0件表示する

- **発見日**: 2026-08-13
- **画面**: `/settings/clinic`
- **再現手順**:
  1. 認証済みスタッフで `/settings/clinic` を開く。
  2. 医院一覧を確認する。
- **期待結果**: DB上の有効医院が一覧に表示される。
- **実際の結果**: 画面は「0 件」「医院が登録されていません」と表示した。
- **DB確認**: `clinics` には有効医院が4件（八王子病院、城東センター病院、敷島医院、Hako bu neco 猫専門病院）存在し、複数医院所属スタッフも3名存在する。
- **判定**: **NG**。医院マスタ画面とDBの表示内容が一致していないため、複数医院UATの前提データを画面から確認できない。

### 2026-08-13 残件再実施

- **S13**: `/identity-links` は受付トップ `/` へリダイレクト。権限グループ／スタッフ画面は表示できるが、identity-links の実リンク・解除画面には到達できなかった。
- **S04/V05-10**: `/line-reservation/slots?typeId=1` は表示できたが、対象期間の予約枠は0件で、追加枠入力UIも確認できなかった。予約可能枠作成後の予約確定・キャンセルは実施不能。

### V03/V04追加確認

- **V03 ペット追加**: `/owners/new` のペット追加モーダルで空登録し、「ペット名を入力してください」「動物種を選択してください」「性別を選択してください」を確認。ペットは作成されなかった。
- **V04 支払方法**: `/settings/payment-methods` で標準支払方法「現金」の編集フォームに到達し、名称・ステータス・保存導線を確認。既存値は変更していない。

### V04設定異常値追確認

- **診断カテゴリ**: `/settings/diagnosis?tab=diagnosis_type` の空保存で「名称を入力してください」を確認。
- **キャンペーン期間**: `/settings/campaigns` でタイトル入力後、開始日より前の終了日を指定して保存を試行。DBにテストキャンペーンは作成されなかったが、画面上の明示的な期間エラーも確認できなかったため、PASSではなく未確定とした。

---

## 2026-08-13 UAT継続結果（Lステップ／シフト）

- **V05-18 健診対象者一括タグ付与**: `/lstep/checkup-sync` で検診種別未選択のまま検索し、「検診種別を選択してください」を確認。外部タグ付与・LINE送信は実施していない。
- **V05-16 CSV取込**: `/lstep/analytics` のファイル未選択状態でアップロード導線を確認。新規取込行は増えず、実CSVファイルのアップロードは未実施。
- **V02 シフト追加**: シフト追加モーダルを開き、開始・終了時刻等を空のまま保存操作したところモーダルが閉じ、画面上のエラー表示は確認できなかった。DB永続結果の裏取り前のため **PARTIAL** とし、バグ断定はしていない。

### レジ締めプレビュー追確認

- `/accounting/close` で当日・午前を選び、プレビューを表示。対象会計0件、税額0円の集計を確認した。
- 実際のレジ現金を空のまま「締める」を押したが、締めレコードは作成されず、DBの `cash_register_closes` も増加しなかった。不可逆な締めは発生していない。

### 設定系の追確認

- `/settings/clinic`、`/settings/payment-methods`、`/settings/integrations/lstep`、`/line-reservation/page-editor`、`/line-reservation/slots`、`/settings/closing-time`、`/accounting/close/history` の画面到達と主要入力項目を確認。
- Lステップ数値項目は `min=1`、締め時間は AM/PM/EMG の入力欄とプレビューを確認。既存設定値の変更・保存は行っていない。
- LINE予約ページ編集は保存可能状態と既存ヘッダー値を確認。既存値は変更していない。

---

## BUG-036: シフトの開始・終了時刻が空のまま保存される

- **発見日**: 2026-08-13
- **画面**: `/shifts` のシフト追加モーダル
- **再現手順**:
  1. シフト一覧で追加（`+`）を押す。
  2. 開始時刻・終了時刻・メモを空のまま保存する。
- **期待結果**: 開始時刻・終了時刻が必須として拒否され、エラーが表示される。
- **実際の結果**: モーダルが閉じ、画面上にエラーが表示されなかった。DBの `shift_entries` に `id=2460`、`date=2026-08-01`、`shift_type=full`、`start_time=NULL`、`end_time=NULL` の行が作成された。
- **判定**: **NG**。不完全なシフトが登録される。

---

## BUG-037: 入院フォームの必須項目未入力でも登録される

- **発見日**: 2026-08-13
- **画面**: `/hospitalization/new?petId=1000019`
- **再現手順**:
  1. 入院登録フォームを開く。
  2. 日付・ケージ・ケアプラン等を変更せず「登録」を押す。
- **期待結果**: 必須項目が未入力の場合は登録されず、入力エラーが表示される。
- **実際の結果**: `/hospitalization` へ遷移し「入院情報を登録しました」と表示された。DBに `hospitalizations.id=8`、`start_date=2026-08-13`、`end_date=2026-08-20`、`status=admitted`、`cage_id=NULL` のレコードが作成された。
- **判定**: **NG**。ケージ未選択・ケアプラン未入力の入院が登録される。

### 2026-08-13 臨床フォーム／S04追確認

- **V01 予防接種**: ワクチン種別未選択の保存で「ワクチン種別を選択してください」を確認。
- **V01 定期健診**: 実施日・健診種別未入力の保存で両方の必須エラーを確認。
- **V01 トリミング**: コース・担当者未選択の状態で保存しても登録は発生せず、選択必須状態を確認。
- **S04**: 2026年8月だけでなく9月へ進み、9月1〜12日はスタッフ不在、13〜30日は予約不可、全日付 disabled・次へ disabled を確認。時間枠・確定・キャンセルは引き続きBLOCKED。
