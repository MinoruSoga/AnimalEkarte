# AnimalEkarte 受け入れテスト バグ報告（ラウンド4・再検証）

## 実装状況（2026-08-18）

| ID | 状態 | 対応概要 |
|:--|:--|:--|
| BUG-002 | 対応済 | 未納「飼主単位」「会計単位」は当月を既定期間にして API を発火 |
| BUG-003 | 対応済 | 集計チップに `cpm_unclassified` を追加し「すべて」と合計を一致 |
| BUG-004 | 対応済 | 締め後は理由欄を出し、complete は FK より先に理由必須。未入力・権限なしは確定を物理ブロック |
| BUG-005 | 対応済 | 来院種別 PATCH 後に詳細キャッシュを invalidate |
| BUG-006 | 対応済 | 未分類でも剤形/単価があれば薬剤扱い。治療検索からも除外しない |
| BUG-007 | 対応済 | 予防接種タブ左ペインに既存記録一覧を表示 |
| BUG-008 | 対応済 | 見積 create/update で明細を同一 tx 置換保存 |
| BUG-009 | 対応済 | 追記モーダルを controlled にしてバリデーション後も入力を保持 |
| BUG-010 | 対応済 | record_shortcut 既定時刻を固定 10:00 から一意な JST 時刻へ |
| BUG-011 | 対応済 | 独立登録ヘッダーに動物種を先頭表示 |
| BUG-012 | 対応済 | 飼主生年月日とサイグラムを縦積みし入力幅を確保 |
| BUG-013 | 対応済 | 投与量バリデーション失敗時にトースト。下限=上限は受理 |
| BUG-014 | 対応済 | 保険補償率の HTML `max` を外し、JS エラーを表示可能に |
| BUG-015 | 対応済 | 同一日の休診日追加は UPSERT せず 409 |
| BUG-016 | 対応済 | `isAxiosError` を axios 本体から import |
| BUG-017 / S12 | 対応済 | `/liff/{clinicId}/src/*` を実ファイルへ rewrite（line-reserve と同型） |
| BUG-018 | 対応済 | Lステップ URL プレースホルダーを `https://api.lstep.jp` に変更 |
| BUG-019 | 対応済 | 慢性疾患コード重複メッセージにコード値を載せる |
| BUG-020 | 既存で充足 | 負の最小年齢は「0 以上の整数」で拒否済み |
| BUG-022 | 対応済 | `/checkups` 編集は `?tab=定期健診&checkupId=` へ遷移 |
| BUG-001 | 対応済 | 既存 `owner_request` を主訴として入力し、ヘッダー担当医で `doctor_id` を保存 |

---


実施日: 2026-08-18
実施環境: http://localhost:3003（ローカル・seed 003_demo）
対象範囲: `docs/ops/testing/scenarios` 配下 S01〜S12（業務フローシナリオ）、V01〜V05（個別フォーム検証、計84フォーム）
ログイン: ノア / admin@noavet.jp / システム管理者 / 八王子病院（一部シナリオのみ城東センター病院）

**本レポートについて**: 前回ラウンド（機能バグBUG-001〜BUG-028、UI一貫性指摘BUG-029〜BUG-047）はすべて対応済みとの前提で、S01〜S12・V01〜V05の受け入れテストを一から再実施した。番号は本レポート内で新規に振り直している（BUG-001〜）。

**総括**: 新規バグ **22件**（High 8件 / Medium 10件 / Low 4件、うちBUG-022はユーザー指摘に基づく追加確認分）。前回報告した既知バグは大部分が修正確認されたが、**V01（カルテ・臨床系フォーム）で新規バグが7件と突出して多く**、特に「見積書タブの再読込で明細が消える」「予防接種タブの一覧がAPI 404で反映されない」「トリミング重複チェックの実装ミスで無関係な予約が一律ブロックされる」は業務影響度が高い。またV04・V05では「保存ボタンが無音で反応しなくなる」という前回修正されたはずの症状パターンが**別のフォームで繰り返し再発**しており、個別修正ではなく共通コンポーネントレベルの点検を推奨する。V05のパスワード変更エラーハンドリングは前回の「無言で閉じる」からアプリ全体のクラッシュへと**悪化**している。

---

## 重大度サマリ

| 重大度 | 件数 | 該当 |
|:--|:--|:--|
| High | 8 | BUG-004, BUG-008, BUG-010, BUG-012, BUG-013, BUG-017, BUG-019, BUG-020 |
| Medium | 10 | BUG-002, BUG-003, BUG-005, BUG-006, BUG-009, BUG-011, BUG-016, BUG-018, BUG-021, BUG-022 |
| Low | 4 | BUG-001, BUG-007, BUG-014, BUG-015 |

---

# S01〜S03（死亡ペットガード／検査確定ロック／ワクチン自動計算）

**新規バグ: 0件。既知バグ3件はいずれも修正確認。**

- 予約患者検索の死亡ペット既定除外 → **修正確認**
- 入院登録フォームURL直打ちの事後バリデーションのみ問題 → **修正確認**（遷移直後に即座にブロックされるよう改善）
- 予防接種一覧検索の先頭100件制限 → **修正確認**（サーバーサイド`search`パラメータに変更）

S02の閲覧専用権限グループでのログイン確認、S01のLステップ配信確認は共有テスト環境保護のため未実施（環境制約）。

---

# S04〜S06（LIFF予約ジャーニー／入院サイクル／カルテロック）

## 核心チェック（S06）: 修正確認・再発なし
カルテ確定時のデータ上書きバグは、保存直後・確定直後のAPI直接GET・直接API改ざん試行(409拒否)・addendum機能のいずれも本ラウンドで問題なし。3ラウンド連続で修正が維持されている。

## BUG-001: 入院フォームに「主訴」入力欄・「担当医」割当UIが存在しない（一覧の主訴/担当医列は常に空になる）
- **シナリオ**: S05 / **重大度**: Low
- 前回報告した「一覧に主訴・担当医列がない」表示側のバグは解消されたが（列自体は追加された）、その値を入力する手段がフォーム上のどこにも存在しない。ヘッダーの「担当医」ボタンもクリック無反応。`POST /api/v1/hospitalizations`のペイロード・レスポンスに`chief_complaint`相当のキーが一切ない。
- 影響: 新規入院は恒久的に主訴「-」・担当医「未設定」のまま。表示側の修正のみで入力側が未実装の可能性。

既知バグ「スタッフ不在ツールチップの不一致」（S04）は**修正確認**。休憩時間境界除外・予約競合検出(409)・LINEソースフィルタ・トリミング分岐等はすべて正常。S04手順8（当日受付へのLINE予約反映）はseedデータのシフト未登録・最短予約2日ルールにより環境制約で未検証。S09は前ラウンド同様、バックデート会計データがDBシード必須のためほぼ全項目未実施（アプリ側の問題ではない）。

---

# S07〜S09（見積ステータス制御／会計訂正系／締め境界）

## BUG-002: 未納者一覧「飼主単位」「会計単位」タブが初期状態でAPIを呼ばず、実在する未収データを「未納者はいません」と誤表示する
- **シナリオ**: S08 手順7 / **重大度**: Medium〜High
- 開始日・終了日が空欄のままだと両タブとも一切APIを呼ばず、実際には¥1,680の未収残高が存在するにもかかわらず断定的に「未納者はいません」と表示される。同一データを扱う「月次繰越」タブは当月を自動設定してAPIを自動発行し正しく表示するため、タブ間で挙動が非対称。
- 影響: 未収金・督促管理という会計業務の根幹機能で、既定の画面遷移だけでは常に「未収なし」という誤った表示になり、実運用で督促漏れにつながりうる（月次繰越タブでの回避は可能）。

既知バグ2件（レジ締めプレビュー「未分類・要確認」0件誤表示、プレビュー再クリックで更新されない）はいずれも**修正確認**。S07は全項目バグなし。S09は前回同様、DBシード制約により大部分未実施。

---

# S10〜S12（顧客集計ダッシュボード／トリミング併用精算／LIFFペットヘルス）

## BUG-003: 顧客集計ダッシュボード「最終来院」タブで「すべて」件数とCPMチップ合計が不一致（cpm_unclassified起因）
- **シナリオ**: S10 #2, #8 / **重大度**: Medium
- 「すべて」チップの件数と、CPM6区分（Encounter/Growing/Core/Spot/Noah/Dormant）チップの合計が1件ずれる。原因は飼主1名の`cpm_stage`が6区分に含まれない`cpm_unclassified`という未文書化の7区分目になっているため（会計実績ゼロだが来院記録95件というエッジケース）。
- 影響: CPMチップによる絞り込みだけでは全飼主を網羅できず、集計の整合性検証・業務判断を誤らせる可能性。

## BUG-004: レジ締め後の会計確定エラーメッセージが実際の原因と異なる汎用文言になる
- **シナリオ**: S11 #7 / **重大度**: High
- レジ締め済み期間に会計確定しようとすると、初回クリックでは「参照先の組み合わせが正しくありません」という無関係な汎用エラーが表示されるが、実際のAPIレスポンスは「レジ締め済み期間の会計編集には post_close_reason の入力が必要です」。UIには`post_close_reason`を入力する導線が一切存在せず、レジ締め後は新規会計を完了する手段がない。
- 備考: レジ締め自体は共有テスト環境で他エージェントが実施した可能性が高く、S11の統合精算ロジック自体（#5・#6）は正常動作を確認済み。

## S12: LIFFペットヘルスアプリ、白画面のまま起動しない（再発・原因特定）
- **重大度**: High（再掲・原因確定）
- 前ラウンドでは「環境ブロッカー」として独立バグに計上しなかったが、本ラウンドで`GET /liff/1/src/main.tsx`が**503**を返すことを直接確認し、技術的原因を特定した。単なるテスト環境の不備ではなく、ローカル開発環境のLIFFアプリ配信設定（Vite dev server統合）自体に問題がある可能性が高い。**再発（未修正）**として計上する。

S10の既知バグ「見積書新規作成がペット/飼主に紐付かない」は、**データ自体は正しく紐付いている**ことが判明（前回の分析が不正確だった）。ただし作成画面のUI上でペット/飼主名のヘッダー表示が引き続き欠落しており、表示問題としては未修正（UI一貫性の項目BUG-018として後述）。S11の中核バグ（トリミング×診察の併用精算統合）とS11の既知バグ2件（コースステータス表示反転、チェック完了ボタン404）はいずれも**修正確認**。

---

# V01（臨床系フォーム）

**既知バグ4件中、修正確認2件・未修正（再発）1件・部分修正1件。新規バグ7件（本ラウンド最多）。**

## BUG-005: カルテヘッダーの来院種別が保存ボタンなしでは永続化されない（既知バグ・再発）
- **重大度**: Medium
- 来院種別を変更しただけ（保存ボタン未押下）で画面遷移すると、再読込で「再診」等の元の値に戻る。エラー・警告表示なし（無音でデータロスト）。前回ラウンドで修正確認したはずが、本ラウンドで再現した。

## BUG-006: 薬剤マスタが「親カテゴリ: なし（未分類）」のまま保存されると、独立したルートカテゴリノードとして扱われ、カルテ治療明細タブの薬剤検索候補に一切表示されない
- **重大度**: High
- 新規薬剤登録時、親カテゴリを明示的に指定しないと、保存後に一覧上は薬品ではなく「カテゴリ」として扱われる（編集パネルで「親カテゴリ: なし（ルート）」「単価: 子項目に金額を設定」と表示）。この状態の薬剤はカルテ治療明細タブの検索候補（「該当する治療プランが見つかりません」）に一切現れず、実質的に使用不能になる。エラー・警告表示なし。
- 影響: 既知バグ「薬剤マスタの投与量パラメータ保存」自体はデータ永続化レベルでは修正確認できたが、本バグにより実際の治療タブでの用量自動計算・ハードゲート動作を実機検証できなかった。

## BUG-007: カルテ予防接種タブで登録した接種記録が同タブの一覧に反映されない（GET 404）
- **重大度**: High
- カルテの予防接種タブから記録を追加すると`POST /api/v1/vaccinations`は201で成功するが、タブ左側の記録一覧は常に「接種記録がありません」のまま。ネットワークログで`GET /api/v1/medical-records/{id}/vaccinations`が**404**を返しており、この存在しないエンドポイントを参照していると推測される。ペット単位API（`GET /api/v1/vaccinations?pet_id=`）や右側の「過去の接種履歴」パネルには正しく表示される。
- 影響: データ喪失はないが、ユーザーが「保存されていない」と誤認し、二重登録や混乱を招く実害あり。

## BUG-008: 見積書タブを保存後に再読み込みすると、明細行と金額（小計・消費税・請求額）がすべて空/¥0にリセットされ、件名しか復元されない
- **重大度**: High
- 見積書タブで明細・金額を入力・保存すると、API上は`title`・`subtotal`・`tax_total`・`total_amount`が正しく永続化される（`GET /api/v1/estimates?medical_record_id=`で確認）が、ページ再読込すると明細行テーブルが完全に空になり、小計・消費税・請求額はすべて「¥0」表示に戻る。件名のみ復元。GETレスポンスに明細行（items）に相当するフィールドが含まれていないことが原因と推測。
- 影響: ユーザーが一度作成・保存した見積書の内容が、画面を離れて戻るたびに消えているように見え、実質的に再入力を強いられる。業務影響大。

## BUG-009: 追記モーダルでバリデーションエラー時、正しく入力済みだった側のフィールドの値も消える
- **重大度**: Medium
- 確定済みカルテの「追記する」モーダルで、修正内容のみ入力し修正理由を空のまま保存すると、期待通りバリデーションエラーで保存はブロックされるが、この時点で既に入力済みだった「修正内容」欄の値も空文字列にリセットされる（逆方向でも同様に再現、2方向×2回で100%再現）。
- 影響: 長文を入力した後に片方を空のまま送信すると全て消え、再入力の手間・入力内容喪失感というUX上の実害がある。

## BUG-010: トリミング新規登録時、同じ担当スタッフが同日に他ペットの予定を1件でも持っていると、無関係な別ペットの新規登録が「既に登録されています」(409)で誤ってブロックされる
- **重大度**: High
- 重複チェックが本来あるべき`(pet_id, date)`ではなく誤って`(staff_id, date)`をキーにしている。担当スタッフを変更すると保存が成功することで確定（API証跡: 対象ペットに紐づく既存トリミングは0件だが、同日同スタッフの別ペット向けレコードが1件存在し、それが409の原因と一致）。
- 影響: トリミングサロン業務では1人のスタッフが同日に複数ペットを施術するのが通常運用であり、この誤った制約により「担当スタッフが同日に1件でも予定があると、それ以降そのスタッフでは他のどのペットのトリミングも新規登録できなくなる」という致命的な業務阻害が発生する。回避策（担当を変更）はあるが、本来の担当者による正しい記録ができない。

## BUG-011: 予防接種独立画面・定期健診独立登録画面・入院ホテル新規登録画面のヘッダーで動物種別が実データ「犬」にもかかわらず「不明」と誤表示される
- **重大度**: Medium
- カルテ画面・検査画面・入院詳細画面のヘッダーでは正しく「犬」と表示されるのに対し、独立登録系画面（予防接種・定期健診・入院新規登録）固有で種別表示が欠落する。共通のペット情報ヘッダーコンポーネントが独立登録画面系で別実装になっていることが示唆される。

### その他確認事項（バグなし）
問診タブ「主訴区分」・診察/治療プランタブ「診断1/2」の永続化はいずれも**修正確認**。バイタル(§3)、定期健診タブ(§4)、画像アップロード(§6-1)、検査取込(§6-3)、検査入力/結果登録(§7)、入院詳細インラインフォーム群(§11)はすべてバグなし。定期健診タブの動的フィールド定義機能は環境に未実装のため該当項目は未検証（バグとしては起票せず）。

---

# V02（会計・予約・受付系フォーム）

**既知バグ3件すべて修正確認。新規バグ0件。**

- 会計明細追加ダイアログの常時409エラー（業務停止級だった） → **修正確認**（201で正常動作、リロード後も永続）
- 予約作成モーダルの時間バリデーションエラー残留 → **修正確認**
- BUG-343（休診日設定が予約カレンダーに反映されない） → **修正確認**（休診日は`disabled:true`で正しくブロック）

軽微なUX観察（正式バグとしては計上せず）: 会計精算フォームのお釣り欄で負値を入力すると、送信ボタンは非活性化されずクリックしても無反応（HTML5 `min="0"`により無言でブロックされる、エラーメッセージ表示なし）。§3〜§10の一部フォーム（クレジット訂正・返金・レジ締め・見積書・受付・シフト）は前提データ作成の難航とブラウザ拡張の一時切断により未実施。

---

# V03（飼主・ペット・スタッフ・権限系フォーム）

**最重要既知バグ（owner_name_kana/pet_name_kana）修正確認。医院マスタ・スタッフパネルの既知バグもすべて修正確認。新規バグ1件。**

## BUG-012: 飼主生年月日欄が入力確定後に表示上「20」等に切り詰められる（サイグラムウィジェットとの幅競合）
- **重大度**: High
- 飼主生年月日欄に日付を入力し確定すると、表示が「20」＋×ボタンのみに切り詰められる。隣接する「サイグラム」（数秘術風ウィジェット）との横幅の奪い合いで入力欄の実描画幅が約17pxまで潰れているのが原因。DOM上の実際の値・API保存データ（`birth_date`）は正しく保持されているためデータ破損はないが、ユーザーが自分の入力内容を目視確認できない。

### 修正確認された既知バグ（全て再発なし）
- owner_name_kana/pet_name_kanaへの値入力で必ず400になり新規登録が不可能だった重大バグ → **修正確認**（新規飼主登録・ペット追加とも正常）
- 医院マスタ編集パネルの保存ボタン無反応 → **修正確認**
- 医院マスタ`requestSubmit()`強制送信によるデータ消失 → **修正確認**
- 医院編集パネルのトースト重なり → **修正確認**
- スタッフ登録パネルのバリデーションエラー後の永久ブロック → **修正確認**（BEエラー後も値を修正すれば同一パネル内で正常保存可能）
- 権限グループの自己剥奪ガード(BUG-140)、C3-2重複名409 → 引き続き正常動作

前回破損したテストデータ「V03医院」(id=5) はDB上に現存せず(404)、追加対応不要。飼主編集・ペット編集・ペット新規追加pending・死亡登録ダイアログ・権限グループはすべてバグなし。

---

# V04（設定マスタ系フォーム）

**既知バグ3件とも「概ね修正確認」だが、同一の症状パターン（保存ボタンの無音無反応）が3つの別フォームで再発。新規バグ3件。**

## BUG-013: 薬剤マスタの投与量パラメータで「下限(mg/kg) > 上限(mg/kg)」を入力すると保存ボタンが完全に無反応になる（エラー表示なし・APIリクエストなし）
- **重大度**: High
- 前ラウンドで修正確認した「割引キャンペーンの日付逆転で保存ボタン無反応」と全く同じ症状パターンが、修正されたはずの別フォーム（薬剤マスタ）で再現。下限=上限（本来は受理されるべき仕様）でも同様に無反応。クリック後もネットワークリクエストが一切発生しない。

## BUG-014: 保険マスタの補償率(%)に101以上を入力すると保存ボタンが完全に無反応になる（エラー表示なし・APIリクエストなし）
- **重大度**: Low〜Medium（症状パターンとしてはHigh寄り、他事例と合算して評価）
- BUG-013・既知バグ「割引キャンペーン日付逆転」と全く同じ「保存ボタン無反応・無音失敗」パターンが3つ目のフォームで発見された。境界値100は正常受理、101のみが無反応の原因。
- 備考: 同じ`useMasterSave`系の共通フロー内で、数値範囲バリデーション失敗時に例外を握りつぶし、エラー表示もAPI呼び出しも行わずに処理を終えている可能性が高い。**個別フォームへのパッチではなく、共通コンポーネントレベルでの全面点検を強く推奨する。**

## BUG-015: 締め時間設定「個別休診日」で既存日付と同じ日付を追加すると、重複エラーにならず既存レコードを無警告でUPSERT上書きしてしまう（データ消失）
- **重大度**: Low〜Medium（データ整合性の観点ではMedium〜High寄り）
- 既存の休診日（例: 2026-04-29「昭和の日」）と同じ日付で「追加」すると、`POST`は201で成功しトースト「休診日を追加しました」（＝新規追加のように見える文言）が表示されるが、実際にはid=1の既存レコードの`reason`が上書きされ、"昭和の日"というラベルが消失する（`created_at`は変化せず`updated_at`のみ変化＝UPSERTであることが確定）。
- 影響: サイレントなデータ破壊。ユーザーが気づかずに既存の休診日情報を失う可能性があり、祝日データのような参照頻度の高いマスタで発生するため実運用影響あり。

### 修正確認された既知バグ
- マスタ重複登録時の内部識別子+空文字列エラー表示（ケージ・職種・Lステップ条件タグ・送信目的プレフィックス等で検証） → **概ね修正確認**（全30マスタの悉皆確認ではない）
- SidePanelのトーストが保存ボタンを覆う → **修正確認**（トースト表示位置が画面左上に変更され、右下の保存ボタンと物理的に重ならなくなった）
- 割引キャンペーンの日付逆転で保存ボタン無反応 → **修正確認**（現在は明確なインラインエラー表示）

標準締め時間・特別期間の日付逆転チェック、予約区分マスタのC2/C3、支払方法マスタのシステム既定行名称変更（保護なし、仕様確認事項）はバグなし。§2診療項目マスタの大半、§5予約可能枠設定、§7シフトパターン、§8Lステップ連携の一部、§9・§10はブラウザ拡張の一時切断により未実施。

---

# V05（認証・LINE関連フォーム）

**既知バグのうち1件は修正確認、1件は「無言で閉じる」から「アプリ全体クラッシュ」へ悪化。新規バグ4件。**

## BUG-016: パスワード変更ダイアログがエラー応答時に必ずクラッシュする（`axios.isAxiosError is not a function`）
- **重大度**: High（既知バグの再発・悪化）
- 現在のパスワード誤入力や新パスワードのポリシー違反等、BE側が400/401を返す任意の操作で、フロントエンドの例外処理内で`TypeError: axios.isAxiosError is not a function`が発生し、React Routerのエラーバウンダリまで伝播。画面全体が「エラーが発生しました」という全画面エラーに置き換わり、リロードしないとアプリに戻れない。
- 前回ラウンドの既知バグ「現在のパスワードを誤入力するとエラー表示なしにダイアログが黙って閉じる」は未修正のまま、別の壊れ方（アプリ全体クラッシュ）に変化しており、**体験としてはむしろ悪化している**。
- 推定原因: `ChangePasswordDialog.tsx`141行目付近で`axios.isAxiosError(...)`を呼んでいるが、実行時の`axios`オブジェクトに`isAxiosError`が関数として存在しない（インポート方法の誤りの可能性）。

## BUG-017: LIFFアカウント連携アプリ（frontend/liff）のローカルdevサーバーが503で白紙になる
- **重大度**: High（ローカル環境限定の可能性はあるが、S12のLIFF問題と根が同じ可能性）
- `http://localhost:3003/liff/{clinicId}/account-link`等へのアクセスで画面が完全な白紙になる。`GET /liff/{clinicId}/src/main.tsx`が503を返す。対照実験で`line-reserve`アプリ（同じような構造の別サブアプリ）は正常稼働することを確認済みで、過去に`line-reserve`側で全く同じ症状のバグ(BUG-402)があり修正された実績があるため、`frontend/liff`側にも同様のdevサーバー起動/プロキシ設定漏れが疑われる。S12（LIFVペットヘルス）の白画面問題（BUG-004参照）と同根の可能性が高い。

## BUG-018: Lステップ連携設定のベースURL入力欄プレースホルダーの例示URLがそのままでは保存できない
- **重大度**: Low〜Medium
- 「LステップベースURL」欄にプレースホルダーの例示どおり`https://app.lstep.jp`を入力すると`400 lstep_base_url host "app.lstep.jp" is not allowed`で保存できない。一方`https://api.lstep.jp`は正常保存できる。BE側の許可ホスト一覧に、UIが例示しているホストが含まれていない。

## BUG-019: 慢性疾患コード重複時のエラーメッセージに識別子が欠落する
- **重大度**: Low
- 重複した`condition_code`（例: `ckd`）で追加を試みると正しく409で拒否されるが、エラーメッセージが`lstep_condition_tag_mapping '' already exists`となっており、本来コード値が入るべき箇所が空文字列になっている。前ラウンドで指摘した「内部識別子+空文字列エラー」パターンが、Lステップ連携設定でも部分的に残存。

## BUG-020: 健診対象者抽出フォームで年齢に負値を入力した際のエラーメッセージが不適切
- **重大度**: High（分類上はLowだが、他の類似パターンと合わせて一覧性のため掲載）※実際の重大度は低、機能的な実害はなし
- 年齢「最小」欄に負値（-3、最大欄は空欄）を入力すると、本来は「0以上の整数で入力してください」等の専用メッセージが出るべきところ、"最大年齢が存在しないのに"「最小年齢は最大年齢以下で指定してください」というmin>max比較用の汎用メッセージが誤って表示される。抽出自体は正しくブロックされ実害はない。

### 修正確認された既知バグ
- パスワード変更ダイアログの全項目空欄無反応 → **修正確認**
- LINE予約フォームの電話番号形式検証欠如 → **修正確認**（明確な形式エラーで拒否）

### 未検証（環境制約）
- マイ予約一覧のペット名不一致: LINE予約が1件も作成できず（シードデータのシフト未登録による全日「スタッフ不在」）検証不能
- V05-9（表示ページ編集）・V05-10（LINE予約枠設定）: 時間都合により未実施
- V05-4手順4-5、V05-5の手順1-4、V05-18の#3-6: いずれも環境制約（メール受信手段なし、LIFFアプリ起動不可、LINE連携済み飼主が0件）

---

## 追加検証（ユーザー指摘に基づく確認）: 定期健診一覧画面の「操作」ボタン遷移先

## BUG-022: 定期健診一覧（`/checkups`）の操作（編集）ボタンが、対象の健診記録ではなくカルテ編集画面の既定タブ（問診タブ）に遷移する
- **重大度**: Medium
- **手順**: `/checkups`（定期健診一覧）で、いずれかの行の「操作」列にある編集（鉛筆）アイコンをクリックする。
- **期待動作**: クリックした行の定期健診記録を直接編集できる画面（カルテ編集画面の「定期健診」タブ、かつ対象レコードが選択された状態）に遷移する。
- **実際の動作**: 該当ペットの該当カルテ自体（`/medical-records/{id}`）には正しく遷移する（1行目→`#1425549`、2行目→`#1425547`とレコードIDは行ごとに正しく対応）が、遷移先は常にカルテ編集画面の**既定タブ「問診」**であり、「定期健診」タブは選択されていない。ユーザーは手動で「定期健診」タブに切り替え、さらに同カルテ内に複数の定期健診記録がある場合はその中から目的のレコードを自分で探す必要がある。
- **影響**: 一覧画面から「編集」を意図してクリックしているにもかかわらず、目的の情報（定期健診の内容）に到達するまで追加のクリック・操作が必要になり、UXとして遷移先が実質機能していない。定期健診記録が複数あるカルテでは、どの記録を編集するつもりだったか見失う可能性もある。
- **参考**: ユーザーからの個別指摘により本ラウンドの通常テスト範囲外で追加確認したもの。

---

## 未実施・要フォローアップ項目（次ラウンド以降で優先実施を推奨）

- S02手順8・S01手順6・S09手順2〜10: 共有テスト環境保護・DBシード制約により継続未実施（アプリ側の問題ではない）
- S11 #7〜9、S12 #1〜9: レジ締め競合・LIFFアプリ起動不可によりブロック
- V01: §2数量境界値・既存行編集、BUG-006解消後の用量自動計算・ハードゲート実機確認、無効化マスタ参照時の挙動
- V02: §3クレジット訂正・§4返金・§5レジ締め・§6見積書・§8受付・§9受付ステータス変更(カンバンクリックでモーダルが開かない現象の原因調査)・§10シフトフォーム
- V03: §7副作用（スタッフ所属医院切替時の権限グループ自動作成）、軽減税率境界値、§5同一メール重複拒否
- V04: §2診療項目マスタ4タブ、§5予約可能枠設定、§7シフトパターン、§8Lステップ連携の数値閾値・LIFF ID・優先度並び替え、§9・§10
- V05: V05-9・V05-10、マイ予約ペット名不一致の再検証（LINE予約が作成できる状態に環境を整えた上で）

## 環境上の注意（バグではないが記録）
本ラウンドでもClaude Chrome拡張機能が複数回・長時間（最大20分以上）切断する事象が発生し、V01・V03・V04で検証が中断され、引き継ぎエージェントによる再開が必要になった。また、セッションログアウト（原因はBUG-016のクラッシュの可能性）も1回発生し、再ログインで復旧した。共有ブラウザセッション・共有シードデータによる複数エージェント間の干渉（クリニックコンテキストの意図しない切替、レジ締めの競合、テストデータの衝突）も引き続き複数回観測された。次回はエージェントごとの専用セッション用意を検討されたい。
