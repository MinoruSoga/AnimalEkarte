# AnimalEkarte 受け入れテスト バグ報告（ラウンド3・再検証）

実施日: 2026-08-17
実施環境: http://localhost:3003（ローカル・seed 003_demo）
対象範囲: `docs/ops/testing/scenarios` 配下 S01〜S12（業務フローシナリオ、S12は環境要因により一部のみ）、V01〜V05（個別フォーム検証、計84フォーム）
ログイン: ノア / admin@noavet.jp / システム管理者 / 八王子病院（一部シナリオのみ城東センター病院を使用）

**本レポートについて**: 前回ラウンド（BUG-001〜BUG-030）で発見されたバグはすべて対応済みとの前提で、S01〜S12・V01〜V05の受け入れテストを一から再実施した。番号は本レポート内で新規に振り直している（BUG-001〜）。前回2ラウンド連続で未検証だったS05（入院サイクル）とS06（カルテロック・データ整合性の核心チェック）を本ラウンドで最優先実施し、いずれも完了した。S06の核心チェック（カルテ確定時のデータ上書きバグ）は**再現せず、修正確認**。S05は完走し新規バグ1件のみ。

**総括**: 機能バグ 28件（Critical 4件 / High 11件 / Medium 9件 / Low 4件）＋ UI一貫性の指摘事項 19件（BUG-029〜BUG-047、いずれもクラッシュ・データ損失は伴わないがデザインシステム未整備による体験不統一）。特に **V03（医院マスタ編集）と V02（会計明細追加）に業務停止級（Critical）の不具合が集中**しており、最優先の対応を推奨する。UI一貫性についてはユーザー指摘の「定期健診登録」ページを起点に、診療業務クイック登録画面群・設定マスタ画面群・会計/予約/受付画面群の3グループを横断調査し追加報告した（BUG-029〜BUG-047）。

---

## 重大度サマリ

| 重大度 | 件数 | 該当 |
|:--|:--|:--|
| Critical | 4 | BUG-018, BUG-021, BUG-022, BUG-023 |
| High | 11 | BUG-001, BUG-003, BUG-008, BUG-011, BUG-013, BUG-014, BUG-017, BUG-019, BUG-024, BUG-026, BUG-027 |
| Medium | 9 | BUG-005, BUG-006, BUG-007, BUG-009, BUG-010, BUG-020, BUG-025, BUG-028, BUG-002(補足) |
| Low | 4 | BUG-004, BUG-012, BUG-015, BUG-016 |
| UI一貫性（Medium相当） | 7 | BUG-029, BUG-030, BUG-032, BUG-035, BUG-039, BUG-044, BUG-047 |
| UI一貫性（Low相当） | 12 | BUG-031, BUG-033, BUG-034, BUG-036, BUG-037, BUG-038, BUG-040, BUG-041, BUG-042, BUG-043(Medium寄り), BUG-045, BUG-046 |

---

# S01〜S03（死亡ペットガード／検査確定ロック／ワクチン自動計算）

## BUG-001: 予約の患者検索（既定=死亡除外のはず）が死亡ペットを結果に含めてしまう
- **シナリオ**: S01 手順2 / **重大度**: High
- ソースコード（`repository.go`）とユニットテストは「`include_deceased`未指定=死亡ペット除外」を前提にしているが、実際に稼働中のAPIは `include_deceased` 未指定・`false` いずれでも死亡ペットを検索結果に含めて返す。
- 証拠: `GET /api/v1/pets?search=山下`（`include_deceased`なし）→ `status:"deceased"` のペットが結果に含まれる。`include_deceased=false` を明示しても同様。全件取得でも50件中18件が死亡ペット。
- 影響: 選択自体はUI側でブロックされ誤登録には直結しないが、「既定検索は死亡ペットを含まない」という仕様（S01が明示）が崩れている。他の同様機能（見積・カルテ検索）にも波及の可能性。

## BUG-002: 入院登録フォームがURL直打ちで死亡ペットのまま最後まで入力できてしまう
- **シナリオ**: S01 手順5 / **重大度**: Medium
- `/hospitalization/new?petId=<死亡ペット>` に直接遷移すると、警告なしにフォーム全項目が最後まで操作可能になり、「登録」クリック時点で初めてブロックされる（クライアント側のみ、API未送信）。
- 比較: `/medical-records/new?petId=` と `/accounting/new?petId=` は遷移直後に即座に全面ブロックされる。入院登録のみ一貫性がなく、事後バリデーションが将来取り除かれた場合のリスクを抱えている。

## BUG-003: 予防接種一覧の検索が「先頭100件のみ」のクライアント側フィルタで、新規保存レコードが検索で見つからない
- **シナリオ**: S03 手順6 / **重大度**: High
- 一覧取得は常に `GET /api/v1/vaccinations?page=1&limit=100`（サーバー側`search`パラメータ自体が存在せず、送っても無視される＝`total=85267`の無フィルタ結果が返る）。取得済み最大100件に対しクライアント側フィルタのみを行う実装のため、seedの2029年データが先頭を占め、当日保存した新規レコードが検索対象外になる。
- 実測: ワクチン新規登録（`POST /api/v1/vaccinations → 201`、データは正しく保存確認済み）直後に一覧検索で該当ペット名を入力 →「0件・データが見つかりません」。
- 影響: 接種直後の確認ができず、二重登録や「自動計算が壊れている」という誤解を招く。S03手順6の「画面上でいつでも確認できる」という期待に反する。

### S02・S01・S03 のその他確認事項（バグなし）
検査確定ロック（確定後の全編集・削除がAPI直接呼び出しも含めて409で正しく拒否）、死亡登録・復活ライフサイクル、カルテ・会計の即時ブロック、ワクチン自動計算（3週後/4週後/1年後/手動）は全て正常動作を確認。S02手順8（閲覧専用アカウント）・S01異常系A1・S03異常系A2は共有テスト環境でのアカウント切替リスクのため未実施。

---

# S04〜S05（LIFF予約ジャーニー／入院サイクル）

## BUG-004: 入院一覧のリストビューに「主訴」「担当医」列が存在しない
- **シナリオ**: S05 手順2b / **重大度**: Low〜Medium
- リストビューの列は「入院NO/飼主名/ペット名/種/タイプ/入院開始日/退院予定日/ステータス/操作」の9列のみで、シナリオが要求する主訴・担当医列がない。

## BUG-005: 日付選択カレンダーで「スタッフ不在」ツールチップが実際の空き状況と食い違う
- **シナリオ**: S04 手順4 / **重大度**: Low
- バックエンドが `available:true` を返す日付にも黒背景ツールチップで「スタッフ不在」と表示される。クリックすれば正常に選択・予約確定でき機能的な支障はないが、表示を見た飼い主が予約を諦める可能性がある。

### S05・S04 のその他確認事項
S05は全手順完走（新規入院登録・ボード/リストビュー切替・バイタル/ケアログ追加・ケージ移動ドラッグ&ドロップ・退院処理（会計連携あり/なし両方）・二重会計拒否(400)・関連レコードありの削除拒否(409)、いずれも正常）。S04は12手順中11手順完走（LIFF予約作成→病院側確認→キャンセル→枠解放確認→トリミング分岐、いずれも正常）。着手時「全日予約不可」の状態はseedデータにシフト未登録が原因と判明（製品バグではない、シフト追加で解消）。受付カンバンのLINE予約反映確認は「当日の受付」が`?date=`パラメータを無視する仕様とLINE予約の最短2日前制約の組み合わせにより本環境では原理的に検証不能（製品バグとしては計上せず）。

---

# S06（カルテロック・監査証跡）【最優先確認事項】

## 核心チェック結果: 修正確認・バグなし
過去2ラウンド連続で未検証だった最重要級バグ「カルテ確定時、身体検査所見が空文字列に、診断詳細・治療方針が固定文字列で上書きされる」について、(1)保存直後のAPI直接GET (2)確定操作直後のAPI直接GET (3)確定後のページフルリロードの3重の方法で検証した結果、**本ラウンドでは再現しなかった**。入力内容（身体検査所見・診断詳細・治療方針）は完定前後で完全に一致していることをAPI応答で確認済み。

確定(Lock)、確定済みカルテへの編集拒否(409)・削除拒否(409)・治療追加拒否(409)、訂正追記(addendum)機能もすべて仕様通り正常動作を確認。

### 参考所見（バグとして未起票・要追試）
「問診」タブの主訴詳細・治療方針欄が、タブ切替後の保存でリロード時にデフォルトテンプレート状態に戻る現象を観測（保存先エンドポイント未特定、タブ個別保存が必要な仕様の可能性もあり原因未確定）。次ラウンドでの追試を推奨。

未実施: audit_logsのDB直接確認（USER実施指定）、カルテ一覧のステータスフィルタ確認、編集権限なしロールでの動作確認。

---

# S07〜S09（見積ステータス制御／会計訂正系／締め境界）

## BUG-006: レジ締めプレビューの部門別集計で「未分類・要確認」区分の件数が実際と一致しない（0件と誤表示）
- **シナリオ**: S08 手順#10 / **重大度**: Medium
- 部門別集計テーブルの「未分類・要確認」行が件数=0件と表示されるが、金額は¥2,600（非ゼロ）。同一画面の個別会計明細には該当2件が明示的に表示されており矛盾。
- 原因: サーバーレスポンス（`aggregate`）に件数フィールド自体が存在せず、フロントエンドが `billing_details` から独自に件数を再集計するロジックにバグがあり、同一飼主・ペットの重複ケースでカウントが相殺されていると推定。金額集計自体は正しい。

## BUG-007: レジ締めプレビュー画面で、対象データ変更後に「プレビュー」再クリックしても最新データに更新されない
- **シナリオ**: S08 手順#10 / **重大度**: High
- プレビュー表示後にバックグラウンドで対象会計に返金が発生しても、「プレビュー」ボタン再クリック時に新規APIリクエストが一切発生せず、古い（過大な）理論現金が表示され続ける。サーバー直接fetchでは最新値が正しく返る（フロント側の再取得漏れ）。フルリロードでのみ最新化される。
- 影響: 締め処理直前に他の操作でデータが変わった場合、実態と異なる表示のまま締めを実行してしまうリスクがあり、会計データの信頼性に直結する。

### S07〜S09 のその他確認事項
S07（見積ステータス制御・確定後の編集/削除ブロック・URL直打ちブロック・フィルタ）は全項目バグなし。S08の二重締め拒否(409)・締め後クレジット訂正の警告表示・#197 NULL非対称性の再発なしも確認。S09は城東センター病院の境界値設定（AM/PM/EMG）が仕様と完全一致することのみ確認（バックデート会計データが通常UI操作では作成不能なため、越日ロジック・二重締め・締め後編集警告の実データ検証は未実施＝要DBシード対応）。

---

# S10〜S12（顧客集計ダッシュボード／トリミング併用精算／LIFFペットヘルス）

## BUG-008: 最終来院タブ「来院なしを含む」チェックボックスが特定区分選択時に機能しない
- **シナリオ**: S10 #8-9 / **重大度**: Medium
- `include_no_visit=true` パラメータは `last_visit_bucket=no_visit` と組み合わせた場合のみ有効で、他の区分（`over_3m`, `over_6m`, `over_1y`等）と組み合わせると完全に無視され、チェックボックスの状態に関わらず件数が変化しない。

## BUG-009: 見積書新規作成（`/estimates/new?petId=X`）がペット・飼主に一切紐付かない
- **シナリオ**: S10 前提条件 / 見積書機能全般 / **重大度**: High
- `petId`クエリを伴って作成しても、レスポンスに `owner_id`/`pet_id` フィールドが存在せず、見積一覧の飼主名列が空欄になる。正しくシードされた見積（`owner_id`あり）と対照確認済み。他エージェント作成分でも再現し再現性あり。
- 影響: 見積が飼主のカルテ・集計ダッシュボードから永久に参照不能になる。S10手順#7（飼主別の見積混入確認）の前提が成立しない。

## BUG-010: トリミングマスタ「コース」一覧のステータス表示が実データと逆転（`is_active:true`なのに「無効」表示）
- **シナリオ**: S11 前提条件 / **重大度**: Medium
- 9件全コースが`is_active:true`にもかかわらず一覧は全件「無効」表示。同一レコードの編集パネルでは正しく「有効」と表示され、一覧表示ロジック側のバグと確定。

## BUG-011: カルテ「会計(医師確認)」タブの「チェック完了」ボタンが完全に無反応（診察処置が併用精算に一切含まれない）
- **シナリオ**: S11 #4-5 / **重大度**: High
- 「チェック完了」を3通りのクリック方法（座標・ref・フルポインタイベント）で試行しても一切ネットワークリクエストが発生しない。想定エンドポイントへの直接PATCH/POSTも**404**（バックエンドにルート自体が存在しない）。
- 影響: 診察側の会計確認ステータスが恒久的に`pending`のままとなり、`unbilled-details`にも診察処置が含まれない。トリミング単独では正しく未請求候補に現れるため、S11の核心機能（診察+トリミングの併用精算への自動統合）が診察側で完全に機能停止している。

### S12（LIFFペットヘルス）: 環境ブロッカーのみ、独立バグなし
連携用URL発行（`POST /line/link-token → 201`）は正常動作を確認。しかし発行されたLIFFアプリURL（`/liff/{clinicId}/{path}`）にアクセスしても画面が常に白いまま（`main.tsx`が断続的に503、Reactが一切マウントされない）で、手順#1〜#9はすべて未実施。原因（devサーバー不安定 or `VITE_LIFF_MOCK`未設定）はブラウザ操作のみでは切り分け不能なため、独立バグとしては計上せず環境ブロッカーとして記録。運用担当者によるバックエンド/インフラ設定の確認を推奨。

---

# V01（臨床系フォーム）

## BUG-012: カルテヘッダーの来院種別を保存ボタンなしで変更しても、実際には永続化されず再読込で元に戻る
- **重大度**: High
- ドロップダウン変更で成功トースト表示・`PATCH → 200`（`version`もインクリメント）されるが、再読込で「再診」に戻る。`GET`レスポンスに来院種別相当のフィールド自体が存在しない。2回再現し再現性確認済み。

## BUG-013: カルテ問診タブ「主訴区分」・診察/治療プランタブ「診断1/2」を保存しても永続化されず再読込で失われる
- **重大度**: High
- `PATCH /medical-records/{id}/inquiries` および `/clinical-plan` は200で成功するが、別タブでの新規アクセスや同タブのF5リロード後に未選択状態に戻る。3件の独立レコード（異なるペット3頭）で再現性確認済み。`GET`レスポンスの`inquiry.chief_complaint`等が空、または`inquiry.id`が別レコードのIDになっている疑義も確認。BUG-012と合わせ「保存成功表示と実際の永続化が一致しない」という共通パターンで、臨床記録欠落は医療安全上のリスクが高い。

## BUG-014: 薬剤マスタの「種別パラメータ（犬・猫）」投与量パラメータが保存されず、用量自動計算・過量防止ハードゲートが機能しない
- **重大度**: High（臨床安全に直結）
- 投与量・上下限・絶対上限を入力し「更新しました」の成功トーストが出るが、再度開くと全て未設定に戻る。`GET .../dose-params`は常に`[]`。保存操作時のネットワークログにも`dose-params`関連のPATCH/POST/PUTが一切発生しない（保存ボタンが偽装成功トーストを出しているのみ）。
- 影響: カルテ治療タブでの体重換算自動計算プリフィルが機能せず、数量は常にデフォルト「1」。絶対上限を超える数量のハードゲートも保存されないため実質機能せず、過量投与を防ぐ安全機構が働かない状態。

### V01 のその他確認事項（バグなし）
petIdなし直叩きのリダイレクト、診断カテゴリによる病名選択肢フィルタ、未保存離脱警告(NavigationBlocker)、タブ別PATCH部分更新、治療明細の数量/単価境界値ガード、バイタルの必須項目・未来日時拒否・体温範囲ガード(30〜45℃)、存在しないID直叩き時のエラー表示。§4〜§12（定期健診/予防接種/画像/検査/入院/トリミング関連フォーム）は共有テスト環境での衝突・時間制約により未実施。

---

# V02（会計・予約・受付系フォーム）

## BUG-015: 会計明細追加（物販・その他追加ダイアログ）が常に失敗し、いかなる会計にも明細を追加できない
- **重大度**: Critical
- 「マスタから選択」「手動入力」いずれのタブでも、`POST /api/v1/billing-items` が**常に409**を返す:`{"error":"未請求候補に請求不能な予防接種が含まれるため会計を確定できません"}`。このエラーメッセージは本来「会計確定」時に返るべき文言で、明細追加のバリデーションに誤って流用されている可能性が高い。複数の異なる会計・単価で再現、金額に依存しない。
- 影響: 会計業務のコア機能（明細追加）が完全に使用不能。

## BUG-016: 予約作成モーダルで時間バリデーションエラーの表示が値変更後もクリアされずに残留する
- **重大度**: Low
- 開始≥終了でエラー表示後、有効な値に変更しても赤字エラーが残留する（保存自体は正常に成功しトーストも正しく表示される、表示上の不具合のみ）。

### 既知バグの再確認: BUG-343（休診日設定が予約カレンダーに反映されない）
個別休診日を登録しても`/reservations`カレンダーがグレーアウトされず、新規予約が自由に作成できてしまう既知の追跡バグ（BUG-343）が本ラウンドでも未修正のまま再現。休診日運用ルールを守れず誤予約が発生しうる（Medium）。

### V02 のその他確認事項（バグなし）
会計精算フォーム（支払方法未選択/入金不足でのボタン非活性、split入力、クレジット訂正ダイアログの必須・境界値・永続化、返金ダイアログの上限境界値・残額0でのガード）、レジ締めフォーム（C1/C2/二重締め409）、見積書フォーム（C1/C2/PATCH部分更新/ロック編集ブロック/NavigationBlocker）、予約作成モーダル（終了≤開始の拒否・FE事前防止+BE 409による二重予約防止・永続化）、当日受付・受付カンバンのステータス変更、シフト追加ダイアログ（テンプレート適用・永続化・重複防止のUI設計）、休診日設定モーダル（特別期間・個別休診日の永続化、重複日付のupsert動作）は全てバグなし。

---

# V03（飼主・ペット・スタッフ・権限系フォーム）

## BUG-017: 飼主名よみ（owner_name_kana）に値を入れて保存すると、新規登録・既存編集ともに必ず400で失敗する
- **重大度**: Critical（ブロッカー）
- `owner_name_kana`が空文字または省略の場合は正常成功するが、1文字でも値を入れると必ず`400 {"error":"入力値が制約条件を満たしていません"}`。他フィールドの値は無関係。既存の複数箇所（`fetch`直接検証、実UIのPATCHペイロードとも一致）で再現。
- FE側は新規登録時に「飼主名よみ」を必須にしているため、**UIから新規飼主登録が事実上100%失敗し完全に機能停止**している。既存編集でも、よみが空欄のまま他フィールドを変更しようとするとFE必須チェックでブロックされ、よみを入力するとBEが400で拒否するという板挟みで、一切の編集ができない。
- 横展開確認: 同一症状が`pets`テーブルの`pet_name_kana`（PATCH時キー名`name_kana`）にも存在（"_kana"系カラムへの非空値書き込みが全社的に壊れている可能性）。
- 影響範囲: 飼主登録フォーム全体、ペット新規追加のペンディング一括登録機能全体（飼主が作成できないため到達不能）。

## BUG-018: 医院マスタ編集パネルで、実際のクリック操作では保存ボタンが一切反応せず更新できない
- **重大度**: Critical（ブロッカー）
- 既存医院の編集パネルで、有効な入力・ボタン活性化を確認した上でクリック（座標指定・要素参照・両方）しても`PATCH /api/v1/clinics/*`が一切発火しない。他フォーム（スタッフ・権限グループ・医院新規作成）では同じクリック方式で正常動作するため、既存医院編集パネル固有の問題と確定。

## BUG-019: 医院マスタ編集で`requestSubmit()`強制送信は成功するが、入力値が送信されず既存データが空文字で上書きされる（データ消失）
- **重大度**: Critical（データ消失）
- BUG-018の回避策として`form.requestSubmit()`を実行すると`200`で成功しトースト表示されるが、直後に確認すると郵便番号・住所・電話番号・FAX・登録番号・院長名・メール・Webサイトが**全て空文字**で上書きされている（サーバー側書き込みは成功、送信ペイロードが空だったことを意味する）。
- BUG-018と組み合わさることで「唯一動作する送信経路が必ずデータを消失させる」状態。既存医院の情報を通常操作で編集することが事実上不可能かつ危険。
- 後始末未完了: 検証用に作成した医院（V03医院, id=5）がデータ破損した状態のまま有効医院として残存。UI経由の無効化・削除もすべて失敗（トグル不可・削除ボタン無反応・編集パネル自体が開かなくなる二次症状も確認）。**運用担当者によるDB直接対応が必要**。

## BUG-020: 医院編集パネルでバリデーションエラーのトーストが保存ボタンに重なり再操作をブロックする
- **重大度**: Medium
- エラートーストが自動で消えず保存ボタン上に物理的に重なり、クリックがトーストに奪われる（`elementFromPoint`で確認）。トーストを手動で閉じれば再操作可能。

## BUG-021: スタッフ登録パネルで一度バリデーションエラーが発生すると、その後正しい値を入力しても保存が永久にブロックされ続ける
- **重大度**: High
- メール指定+パスワード空でエラー後、パネルを閉じずに有効なパスワード・職種を入力し直しても、同一パネルインスタンス内では二度とAPIリクエストが発火せず同じエラーが再表示され続ける。パネルを閉じて開き直した場合のみ正常に保存できる。FE側のバリデーション状態（エラーフラグ）が一度立つと再評価されない実装ミスと推定。

### V03 のその他確認事項（バグなし）
飼主登録の必須・形式チェック・重複検知(409)・404ハンドリング・生年月日null化のセマンティクス（API直叩きで確認）、ペット編集の体重境界値・性別空欄のBE挙動、死亡登録ダイアログの必須・未来日拒否・永続化・解除、権限グループの必須・重複名409・マトリクス永続化（実クリックで確認、自己剥奪ガードのみアカウント構成上未検証）、医院マスタの新規作成・インボイス番号・税率境界値。

---

# V04（設定マスタ系フォーム）

## BUG-022: 複数の設定マスタで重複登録時に内部識別子+空文字列の英語エラーがそのまま表示される
- **重大度**: High
- ケージマスタ・職種マスタで重複登録すると `cage '' already exists` / `occupation '' already exists` という内部テーブル名+空文字列の英語エラーがそのままトースト表示される。動物種類マスタは既に日本語化修正済み（`動物種類『V04動物種類』は既に使用されています`）だが、ケージ・職種を含む約20マスタが未対応と推定される。他マスタ（保険・診断カテゴリ・トリミング系・支払方法等）でも同様の再現が濃厚。

## BUG-023: 割引キャンペーンで終了日<開始日を入力すると保存ボタンが完全に無反応になる（無音失敗）
- **重大度**: High
- 終了日<開始日の状態で保存クリックしても、エラーメッセージなし・APIリクエストなしで何も起きない（DOM上にエラー要素も存在しない）。正常な日付順序では`201`で正常登録されることを対照確認済み。ユーザーからは保存ボタンが壊れているようにしか見えない。

## BUG-024（=BUG-020と同一パターン）: SidePanel全般でトースト表示中に保存ボタンが隠れてクリック不能になる
- **重大度**: Medium
- ケージマスタ・トリミングコース種別マスタで再現。トーストの表示領域が保存ボタンに重なり、クリックがトーストに奪われフォーム未送信になる。V03のBUG-020と同一の共通UIコンポーネントの問題と推定され、影響範囲は全SidePanel系マスタに及ぶ可能性が高い。

### V04 のその他確認事項（バグなし・軽微な指摘含む）
診断病名カテゴリ・保険マスタ・動物種類マスタの必須/境界値/重複チェック、治療プランマスタ（診察タブ）の負単価拒否、標準締め時間の境界値逆転拒否。軽微な指摘（バグ登録なし）: 無効化済みトリミングコース種別がドロップダウンで通常項目と区別なく選択可能。§2〜§10の大部分（薬剤マスタ、予約区分、予約可能枠、Lステップ連携等）は時間制約により未実施。

---

# V05（認証・LINE関連フォーム）

## BUG-025: パスワード変更ダイアログで全項目空欄のまま保存を押しても何のフィードバックもない
- **重大度**: Medium
- 「変更する」ボタンは押下可能だがクリックしてもAPIリクエスト・エラー表示・トースト・ダイアログクローズのいずれも発生しない。2回再現し同一挙動。保存はされない（安全側）が、原因不明のまま操作に詰まる。

## BUG-026: パスワード変更で現在のパスワードを誤入力すると、エラー表示なしにダイアログが黙って閉じる
- **重大度**: High
- `PUT /api/v1/users/me/password`が401を返した後、`POST /api/v1/auth/refresh`が発火し、ダイアログが何のエラーメッセージも表示せずに閉じる。パスワード自体は変更されていない（実害なし）が、ユーザーは成功/失敗を判別する手段がない。共通401ハンドラがセッション期限切れと誤認している可能性が高い（8文字未満等のBE側ポリシー違反エラーは正しく表示されるため、401ケースのみハンドリング欠落）。

## BUG-027: LINE予約フォームの電話番号欄に数字以外を入力しても、クライアント・サーバー双方で検証されずそのまま永続化される
- **重大度**: Medium
- 電話番号「abc」のまま最後まで予約フローを完了させると`POST /api/liff/1/reservations`が201で正常受理し、`customer_fields.phone: "abc"`がそのままDBに永続化される。院内スタッフが電話連絡しようとしても連絡先が存在しない実務影響あり。

## BUG-028: マイ予約一覧のペット名表示が、予約作成時に入力した実際のペット名と一致しない
- **重大度**: Medium
- 予約作成時に入力したペット名「テストポチ」ではなく、無関係な「さくら（デモ）」がマイ予約カードに表示される。院内側APIで確認した実データは正しく「テストポチ」で保存されているため、表示ロジックが誤った参照先（別の既存ペットのデモデータ）を見ている可能性が高い。

### 既知バグの修正確認（再発なし）
- 旧BUG-V05-NEW-2（マイ予約キャンセルがDELETEで完全削除される問題）→ **修正確認**。ソフトキャンセル（`status:"cancelled"`として永続化）に変更されている。
- 旧BUG-V05-NEW-1（LINE予約設定保存後のUI即時リフレッシュ不具合）→ **修正確認**。保存→リロード後も正しい値が表示され続ける。
- **最重要確認事項**: 過去3ラウンド連続で報告されていた健診対象者抽出プレビューAPI（`/lstep/checkup-sync/preview`）のハング/500/503 → **本ラウンドで再発せず、修正確認**（200 OK、正しいデータで応答）。

### V05 のその他確認事項（バグなし）
ログイン済み状態での`/login`直アクセスのリダイレクト、パスワードリセット申請のアカウント列挙防止、パスワード再設定フォームの各種バリデーション、LIFF連携URL発行、飼主⇄LINE顧客の紐付け・解除と永続化、LINE予約設定の数値境界値・非表示項目確認、表示ページ編集の非破壊マージ更新、LINE予約枠設定の不正typeIdフォールバック、配信優先順位のバリデーション。

---

---

# UI一貫性（デザイン統一性）の指摘事項

ユーザー指摘（「定期健診登録ページなど、UIの不統一なものもバグとして記載してほしい」）を受け、機能面ではなく**画面デザイン・レイアウト・UIパターンの統一性**を対象に追加調査を実施した。診療業務クイック登録画面群、設定マスタ画面群、会計・予約・受付画面群の3グループで横断比較を行い、以下の不統一を確認した。いずれもクラッシュやデータ損失は伴わないが、学習コスト増加・誤読・操作ミスの誘発につながるため、デザインシステムの整備を推奨する。

## 診療業務クイック登録画面群（定期健診／予防接種／トリミング／入院／カルテ内タブ）

## BUG-029: 過去履歴参照パネルの有無が画面ごとにバラバラ（定期健診・入院登録にはない）
- **重大度**: Medium
- 比較: `/vaccinations/new`（予防接種）・`/trimming/new`（トリミング）には右カラムに「過去の接種履歴」「施術履歴」パネル（日付レンジ検索・キーワード検索付き）があるが、`/checkups/new`（定期健診登録、ユーザー指摘の例）と `/hospitalization/new`（入院登録）には同種の過去実績参照パネルが一切ない。
- 影響: 過去の記録を見ながら入力するという同じ目的の画面なのに、健診と入院だけ参照ができず、スタッフが別タブでカルテを開いて確認する必要がある。

## BUG-030: ヘッダー（ペット情報バー）の表示項目・書式が画面ごとに異なる
- **重大度**: Medium
- 同一ペットで比較: 予防接種ヘッダーは「22才7ヶ月 / 不明 / 不明」（年齢・性別等3項目）を表示するが、定期健診・トリミングは「不明」の1項目のみで年齢が欠落。入院登録は「2004-01-01生 / うさぎ」と生年月日を年齢換算せず生表示し、動物種も他画面にない形で括弧書き表示（例:「マメラ(うさぎ)」）。保険プラン欄も、予防接種/健診/トリミングは「ペット保険Aプラン」を表示するのに、入院登録だけ「保険情報未登録」と表示され同一ペットのはずなのに情報が反映されない。
- 影響: 同じヘッダーコンポーネントのはずが画面ごとに出し分けが違い、画面遷移のたびに情報の見え方が不安定になる。

## BUG-031: 担当医バッジの有無・デフォルト値・UIパーツが画面ごとに違う
- **重大度**: Low
- 予防接種・定期健診はヘッダー右端に「医師A」バッジ、トリミングは同位置がラベルなしの空シェブロンのみ（何のボタンか不明瞭）、入院登録はログインユーザー名「ノア」がデフォルト表示。同一の「担当医設定」機能なのにラベル有無・初期値ロジックが異なる。

## BUG-032: 「次回予定」の入力UIパターンが画面ごとに異なる（4パターン混在）
- **重大度**: Medium
- 予防接種は「1年後/3週後/4週後/以外(手動)」プルダウン＋自動計算date picker。定期健診は単純date pickerのみ（ラベルも「次回予定日」で予防接種の「次回の予定」と表記ゆれ）。トリミング・入院登録は次回予定の入力欄自体が存在せず、ヘッダーの「次回 -」が常に空欄のまま無意味化している。
- 影響: 「次回来院予定」という同一概念の入力方法が統一されておらず、リマインド運用が一貫してできない。

## BUG-033: 保存/登録ボタンの文言が画面によって「保存」「登録」で表記ゆれ
- **重大度**: Low
- 予防接種・定期健診・トリミングは「保存」、入院登録は「登録」。ヘッダータイトルはいずれも「〜登録」で終わるのに確定ボタンの動詞だけ不統一。

## BUG-034: ペット選択画面（select-pet）群のタイトル文言・呼称が3パターンに分裂
- **重大度**: Low
- 検索条件欄・結果テーブルの列構成は6画面（定期健診/トリミング/入院/検査/予防接種/カルテ）で完全に統一されているが、タイトルだけ「◯◯登録 - ペット選択」「ワクチン接種 - ペット選択」（「登録」が付かず、サイドバーの機能名「予防接種」とも異なる別名称）「カルテ作成 - ペット選択」（動詞が「登録」でなく「作成」）の3パターンが混在。
- 影響: レイアウトが統一されているだけに文言の表記ゆれが目立ち、画面遷移時に「今どの機能にいるか」を一瞬迷わせる。

## BUG-035: カルテ内タブと対応する単独登録画面でUIパターン・文言が全く別物
- **重大度**: Medium
- 「次回の予定」入力が、単独の予防接種登録画面ではプルダウン（「1年後/3週後/4週後/以外（手動）」）だが、カルテ内「予防接種」タブでは同じ選択肢がラジオボタン（「3週間後/4週間後/1年後/以外」）で文言・並び順も異なる。履歴パネルの見出しも「過去の接種履歴」+「新→」ボタン（単独画面）と「予防接種履歴」+「降順」ボタン（カルテ内）で不統一。
- 定期健診はさらに顕著: 単独の`/checkups/new`はゆったりした1カラムフルフォームだが、カルテ内「定期健診」タブは一覧テーブル＋「記録を追加」ボタンで初めて現れる1行圧縮インラインフォームで、情報量・操作フローが別物。加えてこのインラインフォームは実施日欄の列幅不足で日付が「2」の1文字しか表示されない表示崩れも確認。
- 影響: 同じデータ（予防接種・健診記録）に対する入力画面が2種類存在し、使う画面によって体験が変わる。

## BUG-036: カルテ内タブ間で「空状態・追加導線」のパターンが3通り混在
- **重大度**: Low
- 同一カルテ編集画面内で、予防接種タブは記録が無くてもフォーム常時表示、定期健診タブは「記録がありません。下の「記録を追加」ボタンから」というガイド文+ボタン、検査タブはボタンなしで空メッセージのみ、という3通りの異なるUXパターンが混在。タブ切替のたびに追加方法を学び直す必要がある。

## BUG-037: 年齢の単位表記「才」「歳」の表記ゆれ、カルテ編集ヘッダーのみデザインシステムが別物
- **重大度**: Low
- 予防接種等のクイック登録画面ヘッダーは「22才7ヶ月」、カルテ編集ヘッダーは「22歳7...」と同一ペットで漢字が不統一。またカルテ編集ヘッダーは、他のクイック登録画面群（枠線付きピル/ボックスで診療種別・次回・担当医バッジを表示する共通デザイン）と異なり、境界線や背景色のないプレーンテキストの羅列になっており、独自項目（来院種別・診察日・レポート等）も混在する。

## BUG-038: フォームのカラム構成・カード有無が画面ごとに違う
- **重大度**: Low
- 予防接種は2カラム（フォーム＋履歴、カード枠なし）、トリミングは3カラム（うっすら背景色パネル区切り）、定期健診は1カラム白カード、入院登録は3カラム（見出しアイコン付き白背景+枠線カード）と、情報のグルーピングの見せ方が画面ごとに異なる。

## 設定マスタ画面群（13画面比較）

## BUG-039: 新規登録パネルの必須項目バリデーション表示の有無が画面によって異なる
- **重大度**: Medium
- スタッフマスタは氏名未入力で保存すると赤文字インラインエラーで明示的にブロックするが、ケージマスタは同様の操作をしても何のエラーも出さずパネルが無言で閉じる（実際は保存されていない）。ユーザーは保存できたのか失敗したのか判別できない。他の設定マスタでも同様の実装差がある可能性が高い。

## BUG-040: 割引キャンペーンマスタの新規作成パネルだけフォームレイアウトが全く異なる
- **重大度**: Low
- 他の12マスタ（ケージ/職種/保険/動物種類/スタッフ/医院/権限グループ/治療プラン/診断病名/支払方法/トリミング）は「左ラベル・右値」の1カラム行並びだが、割引キャンペーンだけ「上ラベル＋枠線付きボックス」の2カラムグリッド。ステータストグルの位置も他画面は最上部、キャンペーンだけ最下部で真逆。

## BUG-041: 「締め時間設定」の特別期間追加だけスライドインパネルでなくインライン展開フォーム
- **重大度**: Low
- 他の12画面は「新規登録」→右からサイドパネルがスライドインし「保存」ボタンで確定する統一パターンだが、締め時間設定の「特別期間」セクションだけ一覧直下にインラインでフォームが展開し、確定ボタンの文言も「保存」でなく「追加」になっている。

## 会計・予約・受付画面群

## BUG-042: 確認ダイアログのボタン構成が画面によって異なる（キャンセルボタンの有無）
- **重大度**: Low
- 「返金を登録」ダイアログは「キャンセル」（左）+「登録する」（右）の2ボタン構成だが、「クレジット金額の訂正」ダイアログは明示的なキャンセルボタンがなく右上「×」のみで閉じる。同じ「確定済み会計への訂正系操作」なのに離脱手段が画面ごとに違う。

## BUG-043: 「確定/保存」ボタンの配置パターンが最低4通り存在する
- **重大度**: Medium
- 会計新規登録は右カラムの決済情報パネル内に埋め込み（右上にキャンセル導線なし）、見積書新規作成・編集は画面右上に固定表示＋隣にキャンセルボタン常設、レジ締めはフォーム最下部・右寄せ単独配置（キャンセルなし）、予約作成モーダルはモーダル右下にキャンセルと対で配置。同じ「保存/確定する」操作なのに位置・キャンセル導線の有無がバラバラで、長い明細をスクロールした際に会計側は保存ボタンを見失いやすい。

## BUG-044: 日付表示フォーマットが画面群によって4パターン混在
- **重大度**: Medium
- 会計一覧・見積書詳細は`2026-08-17`のハイフン区切りISO形式、予約作成モーダルは`2026/08/18 (火)`のスラッシュ+曜日形式、当日受付ヘッダーは`2026年8月18日 (火)`の漢字+曜日形式、入院管理リストビューは`2026/07/28`のスラッシュのみ（曜日なし）形式。受付・会計・予約を横断する職員が画面ごとに表記ルールの違いに戸惑う可能性があり、月/日の区切り文字の違いは誤読を誘発しやすい。

## BUG-045: 締め履歴一覧の「日付」列が未整形の生ISO8601文字列のまま表示される
- **重大度**: Low
- `/accounting/close/history`の「日付」列が`2026-08-17T09:00:00+09:00`というタイムゾーンオフセット付きの生タイムスタンプのまま表示される。同一画面内の隣接列「締め時刻」は`08-17 23:00`（年なし）という別形式で、1画面内でも不統一。金銭を扱う重要画面での見た目の粗さはシステム全体の信頼性を損ないやすい。

## BUG-046: カンバン/ボードのカードデザインが「当日の受付」と「入院・ホテル管理」で統一されていない
- **重大度**: Low
- 受付カンバンのカードは左寄せ・時刻→飼主名（太字）→動物種+ペット名→区分タグ→「カルテ」ボタン（左下）という構成。入院ボードのケージカードは中央寄せ・ペット名（大きめ中央）→飼主名→動物種という順で、アクションボタンが一切ない。同じ「本日の状況を俯瞰する」目的なのに整列方向・情報順序・アクション有無が異なり、両画面を行き来するスタッフの学習コストが増える。

## BUG-047: ステータスバッジの配色ルールが画面群によって矛盾する（同じ色が逆の意味で使われる）
- **重大度**: Medium
- 「正常に完了した状態」を表す色が、会計・見積書では緑（精算済／承認済み）である一方、入院管理の完了状態「退院済」はグレーで表現される。一方グレーは見積書では「まだ何も進んでいない下書き」を表す色として使われており、同じグレーが「未着手」と「完了」という正反対の意味で使われている。「進行中」を表す色も受付カンバンは紫（診療中）、入院管理は青（入院中）と不統一。
- 影響: 色だけでステータスを素早く読み取る運用（一覧の流し見）において誤読のリスクがある。

---

## 未実施・要フォローアップ項目（次ラウンド以降で優先実施を推奨）

- S02手順8・S01異常系A1・S03異常系A2（共有テスト環境でのアカウント切替リスクのため見送り）
- S06: audit_logsのDB直接確認（USER実施指定）、カルテ一覧ステータスフィルタ、権限制御(A2)、問診タブのタブ切替時データ消失疑い（参考所見）の原因切り分け
- S09: AM/PM/EMG境界の実データ検証・二重締め・締め後編集警告（バックデート会計データがDBシード対応必須のため）
- S12: LIFFペットヘルスアプリの環境ブロッカー解消後の手順#1〜#9再検証
- V01: §4〜§12（定期健診・予防接種単独画面・画像/検査取込・入院インラインフォーム・トリミングフォーム）
- V02: 新規飼主モードの電話番号形式・複数ペット同時予約・担当医コンフリクト時の孤児データ確認
- V03: 自己剥奪ガード（専用テストアカウントでの再検証が必要）
- V04: 薬剤マスタ全般・予約区分マスタ・予約可能枠設定・Lステップ連携4フォーム・シフトパターンマスタ詳細
- V05: レート制限・パスワードリセット実フロー（メール受信手段が環境にないため）・LINE実OAuth連携・Lステップ詳細CRUD

## 環境上の注意（バグではないが記録）
本ラウンドは複数エージェントが同一ブラウザプロファイル・同一ログインセッションを共有して並行実行しており、医院切り替えやタブ競合による一時的な干渉（「ペットを選択してください」表示、カルテの409競合等）を複数回観測した。いずれもアプリの不具合ではなく並行テスト環境固有の事象と判断し、バグとしては計上していない。次回同様のテストでは、エージェントごとに専用アカウント/セッションを用意することを推奨する。
