# AnimalEkarte 受け入れテスト バグ報告（ラウンド6・再検証）

実施日: 2026-08-22
実施環境: http://localhost:3003（ローカル・seed 003_demo）
対象範囲: `docs/ops/testing/scenarios` 配下 S01〜S13、V01〜V05
ログイン: 林文明 / 八王子病院

**本レポートについて**: 前回（ラウンド5）報告した全バグ（BUG-001〜BUG-018）はユーザーによりすべて対応済みとの前提で、S01〜S13・V01〜V05の受け入れテストを一から再実施した。番号は本レポート内で新規に振り直している（BUG-001〜）。

**総括**: ラウンド5で報告した18件の既知バグのうち、**15件は修正確認**できた。特にS01（担当医PATCH未送信によるデッドロック）、S06核心チェック（5ラウンド連続クリーン）、V04の3件（Lステップ設定・処置マスタ削除・重複名トースト）はいずれも良好。

一方、**3件は未修正のまま継続**している。うち2件はV02（会計・予約・受付系）の重大な業務ルールバイパスで、前回から状況は変わっていない: 予約編集がメモのみの変更でも「当日担当医不在」チェックに誤って引っかかる問題(BUG-006)、予約詳細のステータスドロップダウンから「受付済→診療中」に直接遷移しカルテ作成必須ガードをバイパスできる問題(BUG-007)。もう1件はS13のオーナー検索が全角/半角スペースを正規化しない問題(BUG-001)で、前回よりむしろ**悪化**しており、seedデータの約8割を占める全角スペース区切りの氏名が完全一致で検索しても0件になる状態になっている。

新規バグは12件（High 3件 / Medium 6件 / Low 3件）。特に注目すべきは、V03で発見したスタッフ編集パネルの重大なデータ消失バグ(BUG-010, High)で、非アクティブ化した医院への既存所属が、無関係な項目を編集して保存しただけで無言かつ不可逆に失われる。また、V02では「フロントエンドのみ業務ルールを実装し、バックエンドでの強制がない」という設計パターンの再発（休診日設定のバックエンド未強制、BUG-008）も見つかっており、BUG-006/007と合わせて根本的な設計レビューを推奨する。

なお、本ラウンドは複数エージェントの共有ブラウザセッション干渉（頻繁な強制ログアウト、クリニックコンテキストの意図しない切替）が例年以上に激しく、複数のシナリオで完全なUI経由の検証を断念しAPI直叩きでの検証に切り替えている。次ラウンドはエージェントごとに独立したブラウザセッションを用意することを強く推奨する。

---

## 重大度サマリ

| 重大度 | 件数 | 該当 |
|:--|:--|:--|
| High | 3 | BUG-006, BUG-007, BUG-010 |
| Medium | 6 | BUG-001, BUG-003, BUG-004, BUG-008, BUG-009, BUG-011 |
| Low | 3 | BUG-002, BUG-005, BUG-012 |

---

# S01〜S03（死亡ペットガード／検査確定ロック／ワクチン自動計算）

**既知バグ2件すべて修正確認。新規バグなし。**

検査編集フォームで担当医(doctor_id)の変更がPATCHで送信されず確定操作がデッドロックする問題は、`UpdateExaminationRequest`構築部の修正により**修正確認**（担当医選択→保存→GETで正しく永続化、確定への遷移も成功）。新規カルテの検査タブでボタン不在の案内文が出る問題も**修正確認**。死亡ペットガード、確定ロック、ワクチン次回予定自動計算（3週後/4週後/1年後/以外）はいずれも問題なし。S02手順7b（確定解除の実機操作）・手順8（閲覧専用権限アカウント）は専用テストアカウント未提供のため今回も未実施（環境制約、継続）。

検査レコード`1013826`で、担当医のみ変更した保存時に無関係な項目のバリデーションエラーが発生する観察事項があったが、シードデータの既存不整合かバグかの切り分けができておらず、次ラウンドでの追調査項目とする。

---

# S04〜S06（LIFF予約ジャーニー／入院サイクル／カルテロック）

**既知バグ3件すべて修正確認。新規バグなし。S06核心チェックは5ラウンド連続クリーン。**

来院種別の「緊急」「往診」がサイレントに「再診」へ変換される問題(旧BUG-003)、入院編集画面の年齢/性別/体重「不明」誤表示(旧BUG-004)、入院ボードのキーボードドラッグ&ドロップ移動不可(旧BUG-005)は、いずれも**修正確認**。カルテ確定時の改ざん防止（直接API改ざん試行の409拒否、削除防止、addendumのみ許可）、二重退院防止、ケアプラン紐付き削除防止も引き続き健全。

前回「LIFFの新規予約ボタンが反応しない」と疑われた観察は、URLベースでの遷移確認という検証方法自体の誤りによる誤検知と判明し、取り下げられた（このLIFFアプリはstate遷移のSPAでURLルーティングを使わない設計）。S04手順8（当日受付へのLINE予約反映）は、実スタッフ（林文明・さくら等）のシフトデータがシードに一件も投入されていないため、ラウンド2以降継続して実施不可（環境制約）。

---

# S07〜S09（見積ステータス制御／会計訂正系／締め境界）

**既知バグ1件修正確認。新規バグなし。S07全項目パス。**

部分入金をAPI直叩きした際に契約と異なりサイレント上書きされる問題(旧BUG-006)は、`POST /accountings/complete`が明確な400エラー（"支払い内訳の合計（600）が請求金額（1100）と一致しません"）で正しく拒否されるようになり、亡霊レコードの残留もないことを確認（**修正確認**）。S08の一般ロール負例テストはテストアカウントの認証情報が今回用意されたが、共有セッションでの並行ログイン競合により完遂できず（次ラウンドは専用セッションでの実施を推奨）。

S09で城東センター病院への会計作成が意図せず拒否される事象が発生したが、調査の結果、共有ブラウザの`localStorage`（クリニックコンテキストのキー）が他エージェントの操作と競合して汚染されたことによる疑似陽性である可能性が高いと判断し、新規バグとしては計上しない。境界時刻の網羅テストは、`completed_at`のサーバー時刻固定という制約が今回も解消されず未実施（環境制約、継続）。

---

# S10〜S13（顧客集計ダッシュボード／トリミング併用精算／LIFFペットヘルス／識別子リンク・手動補正）

**既知バグ3件中2件修正確認、1件は未修正のまま悪化。新規バグ1件。**

## BUG-001: identity-linksのオーナー検索が全角/半角スペースを正規化しない（既知バグ・未修正・悪化）
- **シナリオ**: S13 / **重大度**: Medium（前回Lowから引き上げ）
- 前回は「DB値が半角スペースの場合に全角検索で0件」という一方向の不具合だったが、今回の検証では**DB値が全角スペースの場合（seedデータの約78%）、全角・半角いずれで検索してもDBと文字単位で完全一致するクエリを送っても0件**という、より深刻な状態に変化していることを確認した（`GET /identity-links/owners/search?q=田中　光広`が完全一致にもかかわらず0件）。姓のみ・名のみの単語検索は引き続き機能するため回避策はあるが、大多数の氏名で姓名2語検索が機能しない実務影響の大きい状態。原因箇所のソースコードレベルでの特定は次ラウンドの課題。

## BUG-002: identity-links 409エラー時、バックエンドの英語エラーメッセージがそのまま日本語UIに表示される
- **シナリオ**: S13 / **重大度**: Low
- ペットリンク失敗時の生のaxios文言表示問題(旧BUG-007)は`extractApiErrorMessage()`ヘルパー導入により**修正確認**したが、既知コード（重複名系）に一致しない409の場合、フォールバック先がバックエンドの英語メッセージ（例: "pet owner is not in the specified owner identity group"）そのままになる。日本語UIとしての一貫性の観点で軽微な問題として記録。

`GET /identity-links/pet-groups/:id`等が`owner_group_id`・`version`を不正返却する問題(旧BUG-009)は、バックエンド`FindPetGroupByID`が完全なGORMモデル取得に修正され**修正確認**。S13の正常系（オーナーlink→ペットlink→unlink→relink）は全手順パス。S13手順7・8（閲覧専用権限、医院カバレッジ不足403）は専用テストアカウントでの実地検証が今回も未実施（環境制約、継続）。CPM集計不一致、LIFF白画面503もそれぞれ引き続き**修正確認**。

---

# V01（臨床系フォーム）

**既知バグ1件修正確認。新規バグ3件。§2薬量ハードゲート・§3バイタル境界値・§11入院デイリー記録・§12無効化マスタ参照を重点実施。**

来院種別ドロップダウンから「緊急」「往診」の選択肢自体が削除され、「初診」「再診」の2値のみになったことでサイレント変換のリスクが構造的に解消されたことを確認（旧BUG-003、**修正確認**）。

## BUG-003: 薬量ハードゲートのエラーメッセージが正常値復帰後も画面に残留する
- **シナリオ**: V01 / **重大度**: Medium
- 治療タブで薬剤の投与量が絶対上限を超えて保存がブロックされた際、表示値は正常値に自動復帰するがエラーメッセージ自体は残り続け、F5リロードするまで消えない。データ不整合はなくクライアント側の再描画漏れ。

## BUG-004: トリミング登録で必須項目未入力エラー発生時、選択済みのオプションチェックボックスが消える
- **シナリオ**: V01 / **重大度**: Medium
- 担当者未選択のままトリミング登録を保存しようとしてバリデーションエラーになると、コース選択やスタイル希望は保持されるが、選択済みだったオプションのチェックのみが自動的に外れる（2回連続再現）。エラー修正後にオプションを再選択する手間が発生。

## BUG-005: トリミングマスタ設定画面で、保存成功後も「未保存の変更」フラグが残り遷移がブロックされる
- **シナリオ**: V01（設定/トリミングマスタ） / **重大度**: Low
- 保存トーストが表示され一覧にも反映されるにもかかわらず、直後の画面遷移でブラウザの「未保存の変更があります」確認ダイアログが出る。3回連続再現。データ損失はないがユーザーに保存の不安を与える。

§12（無効化マスタ参照、既知バグ#228関連）は新規登録での選択肢除外・既存レコード編集画面での「（無効）」ラベル付き保持のいずれも正常動作を確認（バグなし）。§11（入院デイリー記録の一意性制約）も正常動作を確認。§1残り・§4〜§10・§11項目2・§12項目6は時間都合により未実施（次ラウンド持ち越し）。

---

# V02（会計・予約・受付系フォーム）

**既知バグ4件中2件修正確認、2件は未修正のまま継続。新規バグ2件。**

会計精算フォームのお釣り欄・レジ締めフォームの実際のレジ現金欄における負値入力時の無音無反応パターン（旧BUG-010, BUG-011）は、いずれも明確なインラインエラー表示とボタン無効化に改善され**修正確認**。

## BUG-006: 予約編集モーダルが、メモ等の非日程系フィールドのみの変更でも「当日担当医不在」チェックでブロックされる（既知バグ・未修正）
- **シナリオ**: V02 / **重大度**: High
- APIレベルでの再検証により再発を確認。`PATCH`で`notes`のみ送信すれば200で成功するが、フロントエンドが実際に送信するであろう全フィールド（日付・時間帯含む）を含んだペイロードでは、担当医不在チェックが不要に再評価され409で拒否される。既存予約編集フォーム自体へのUI導線が本ラウンドでは発見できず、完全なUI経由の再現はできていないが、バックエンド動作としては明確に問題が残っている。

## BUG-007: 予約詳細のステータスドロップダウンから「受付済→診療中」へ直接遷移でき、カルテ作成必須の業務ルールをバイパスできる（既知バグ・未修正）
- **シナリオ**: V02 / **重大度**: High
- カルテが0件であることを確認したクリーンな前提条件下で、`PATCH /reservations/:id`に`{status:"in_consultation"}`を送信すると200で遷移が成立してしまうことを確認。ステータス変更エンドポイントがカルテの存在有無を一切検証していない。

## BUG-008: 休診日を設定しても、予約作成APIへの直接リクエストではバックエンド側の検証がなく作成できてしまう
- **シナリオ**: V02（§11休診日設定、新規実施） / **重大度**: Medium
- フロントエンドの新規予約日付ピッカーは休診日を正しく無効化(`disabled:true`)するが、`POST /reservations`に休診日を含むペイロードを直接送信すると201で作成されてしまう。BUG-006/007と同様、「フロントエンドのみに業務ルールが実装され、バックエンドでの強制がない」という設計パターンの再発であり、他の予約作成経路（LINE予約連携等）から容易にバイパスされうる。

## BUG-009: 在庫登録フォームで必須項目未入力・負の在庫数のまま登録を試みても、エラーメッセージ等の視覚的フィードバックが一切ない
- **シナリオ**: V02（§12在庫登録、新規実施） / **重大度**: Medium
- 送信自体は正しくブロックされる（データ整合性は保たれる）が、赤枠・エラー文言・トースト等が一切表示されず、フォーカス移動のみでユーザーには「反応しない」ように見える。BUG-010/011修正で解消されたはずの「サイレント無反応」パターンが、このフォームには横展開されていない。

---

# V03（飼主・ペット・スタッフ・権限系フォーム）

**既知バグ2件修正確認（うち1件で関連する重大な新規バグを検出）。**

権限グループ名重複時のトースト空名表示問題は**修正確認**。自己剥奪ガード（BUG-140）は回帰なし。

スタッフ編集パネルの所属医院選択肢に非アクティブ・重複名医院が無差別表示される問題(旧BUG-014)は、フロントエンド側の「（無効）」ラベル表示ロジック自体は正しく実装され**修正確認**したが、検証の過程でこの修正を実質的に無効化する、より深刻な新規バグを発見した。

## BUG-010: スタッフ編集パネル保存時、非アクティブ化された医院への既存所属が無言で消失する
- **シナリオ**: V03 / **重大度**: High
- 原因はバックエンド`GetStaffClinicAssignments`が「現在アクティブな医院一覧」でGET結果をフィルタしていること。非アクティブな医院への既存割当はそもそもフロントエンドに返却されないため、旧BUG-014で実装された「（無効）ラベル付きで保持表示する」救済ロジックは実質的に到達不能（死んだコードパス）になっている。所属医院セクションに一切触れず、資格番号など無関係な項目のみ変更して保存しただけで、`PUT .../clinics`が無条件に発火し、非アクティブ医院への既存割当がDBから消去されることをAPI証跡（保存前GET→保存→医院再アクティブ化後GETで消失確認）で確認した。データ消失は不可逆でUIから元に戻す手段がない。多店舗運用で「一時的に医院を非アクティブ化→再アクティブ化」という運用を行うクリニックでは、その間にスタッフ情報を編集しただけで所属履歴が消える。一般スタッフ（担当医院が限定的）が編集した場合はより広範囲が同様に消える可能性もソースコード上示唆されるが、今回はsystem admin権限でのみ実機確認。

---

# V04（設定マスタ系フォーム）

**既知バグ3件すべて修正確認。新規バグなし。**

Lステップ連携設定のLIFF IDクリア機能、処置マスタの子処置削除500エラー、診療項目マスタの重複名エラートーストの実名表示は、いずれも**修正確認**。動物種類・保険・支払方法・薬剤マスタ・予約区分・シフトパターン・診断カテゴリの主要な検証観点も期待通り動作。§9（LINE予約ページ設定）は他エージェントのテストマーカーが複数ラウンドにわたり残存しているため非破壊確認のみに留めた（次ラウンドは単独セッションでの実施と、正しい初期値への復元を推奨）。§5予約可能枠・§6締め時間設定・§10法人情報は共有シングルトン設定への影響を避けるため見送り。

---

# V05（認証・LINE関連フォーム）

**既知バグ1件修正確認。新規バグ2件。V05-11・V05-5の一部を重点実施。**

パスワード変更ダイアログで新旧パスワード不一致時に「現在のパスワード」欄も消去される問題(旧BUG-018)は、`ChangePasswordDialog.tsx`の全エラー分岐で`currentPassword`を保持するよう修正され**修正確認**。LINEアカウント紐付け・解除の永続性（V05-11）も正常動作を確認。

## BUG-011: ローカル環境のLIFFアカウント連携がモック実装のため、連携済み重複(409)・トークン無効(400)の分岐が実機検証できない
- **シナリオ**: V05 / **重大度**: Medium
- `frontend/.env.local`の`VITE_LIFF_MOCK=true`設定により、`use-liff-link.ts`がバックエンドAPIを一切呼ばずタイムアウトのみで無条件に成功表示する実装になっている。有効な連携URLへの初回アクセスも、同一URLへの再アクセスも常に「連携が完了しました」と表示されるが、バックエンドの連携状態（LINE顧客の`updated_at`）は一切変化しない。V05-5 #2の「連携成功」表示自体が見せかけであり、機能バグではなくテスト環境設計上の制約だが、QA観点で重大な誤解を招くため記録する。テスト専用に`VITE_LIFF_MOCK=false`の検証環境を別途用意することを推奨。

## BUG-012: line-reserveアプリのクリニック名表示に、過去ラウンドのテストデータ残留文字列が付与されたままになっている
- **シナリオ**: V05 / **重大度**: Low
- ヘッダーに「ノア動物病院 八王子 QAR5-1787312315230」のように、ラウンド5のテスト時に投入されたと見られる文字列が付与されたまま。表示のみの問題で機能的支障はないが、複数ラウンドにわたるテストデータの累積・放置がシード運用上の課題になっている。

V05-6のバリデーション系（空白のみ、電話番号形式、ペット名必須）は正常動作を確認。フルの予約作成フローは、実スタッフのシフトデータ欠如により今回もブロックされたまま（S04手順8と同一の環境制約）。V05-7〜10・V05-12〜18は時間都合により未実施（次ラウンド持ち越し）。

---

## 未実施・要フォローアップ項目（次ラウンド以降で優先実施を推奨）

- S02手順7b・8、S08手順5、S13手順7・8: 権限系の負例テストに必要な専用テストアカウントでの実地検証。S08手順5は認証情報が用意されたが共有セッションの競合で完遂できなかったため、次回は専用の独立セッションでの実施を強く推奨。
- S09境界時刻の網羅テスト: `completed_at`のサーバー時刻固定という制約が複数ラウンドにわたり未解消。テスト用の時刻上書きパラメータの追加、またはシードデータへの複数時刻帯データの追加を検討されたい。
- BUG-001（S13オーナー検索の全角/半角スペース）の原因箇所のソースコードレベルでの特定。
- BUG-006（V02予約編集の担当医不在チェック誤爆）の完全なUI経由での再現。既存予約編集フォーム（メモ欄付き）への正しい導線の特定が必要。
- V01: §1残り・§4〜§10・§11項目2・§12項目6。
- V04: §5予約可能枠、§6締め時間設定、§10法人情報、§8Lステップのタグ設定等。§9 LINE予約ページ設定は放置されたテストデータの復元も含め単独セッションでの実施を推奨。
- V05: V05-7〜10・V05-12〜18（Lステップ関連）、マイ予約ペット名不一致の再調査（シフトデータ整備後）。
- S04手順8・V05-6のフル予約作成フロー: 実スタッフ（林文明・さくら・山﨑晶子・三井隆之・ノア）のシフトデータがシードに一件も投入されていないため、ラウンド2以降継続してブロック。シード投入スクリプトへのシフトデータ追加を推奨。

## 環境上の注意（バグではないが記録）

- **admin@noavet.jpのパスワードが変更されている**: 本ラウンドのテスト中、V05エージェントが検証過程で`admin@noavet.jp`のパスワードを変更し、複雑性ポリシーの制約により元の`password`には戻せなかった。現在のパスワードは **`password1`** である。次ラウンド以降のテストではこの点に留意されたい。
- 本ラウンドは複数QAエージェントの共有ブラウザセッション・共有`localStorage`・共有シードデータへの並行アクセスに起因する干渉が例年以上に激しく、数十秒〜数分間隔での全タブ強制ログアウト、クリニックコンテキストの意図しない切替、他エージェントの操作によるレコードの並行編集が多数観測された。これにより複数のシナリオ（S09、S11、S13、V01〜V05の一部）で完全なUI経由の検証を断念し、API直叩きでの検証に切り替えている。次ラウンドはエージェントごとに独立したブラウザセッション（別ウィンドウ・別プロファイル等）を用意することを改めて強く推奨する。
