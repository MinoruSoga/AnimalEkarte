# TASK-010 — Runtime browser batch3 (V03 + V04 residual)

> **Date**: 2026-08-01  
> **Unit**: `TASK-010-BATCH3-20260801`  
> **Claim**: `claim/TASK-010` was empty at start (USER released); not re-acquired  
> **Status**: **PARTIAL PASS** — env READY; V03 pack + V04 residual measured; **6** marks elevated; backlog remains  
> **Scope**: Browser/API measurement + report + scenario mark elevation + todo 状態. Product code / migrate / claim delete not touched.

---

## 1. Env health

| Check | Result |
|:---|:---|
| `docker compose ps` | backend / db / frontend **Up (healthy)** |
| `GET http://127.0.0.1:8080/health` | **200** |
| `GET http://127.0.0.1:3003/` | **200** |
| Browser | **Chrome DevTools MCP** |
| Login / seed | **OK** — demo システム管理者（identity present on login page; password not recorded）→ 八王子病院 |

---

## 2. 【要実測】 census

| Scope | Before | After | Δ |
|:---|---:|---:|---:|
| Scenario bodies exact `【要実測】` (`V*.md` + `S*.md`) | **59** | **53** | **−6** |
| Semantic prefix `【要実測` | **62** | **56** | **−6** |
| `V03-owner-pet-staff-forms.md` | **4** | **1** | **−3** |
| `V04-settings-master-forms.md` | **5** | **2** | **−3** |

```bash
rg -c '【要実測】' docs/ops/testing/scenarios/V*.md docs/ops/testing/scenarios/S*.md
# bodies total → 53
```

---

## 3. Pack results

### V03 owner / pet / staff (priority pack)

| ID | Step | Result | Observation |
|:---|:---|:---|:---|
| V03 L130 | permission-group duplicate name | **PASS** (elevated) | `/settings/permission-groups` 新規「執行」→ POST `/api/v1/masters/permission-groups` **409**。トースト `permission_group '' already exists`。**白画面・500 なし**。メッセージ品質は既知 **BUG-023** |
| V03 L144 | clinic tax rate format / out of range | **PASS** (elevated) | FE: % spinbutton min0 max100。101 → HTML「値は 100 以下にする必要があります。」API 未到達。10% → payload `standard_tax_rate:0.1`；100% → `1.0`（BE 0–1 小数） |
| V03 L145 | clinic postal / email invalid | **PASS** (elevated) | **素通しではない**。`type=email` ネイティブ検証 + PATCH 400 `postal_code の値が正しくありません; email の値が正しくありません`。郵便 `12-3456` も BE 拒否。医院 ID4 で検証後、email を空へ復元済み |
| V03 L88 | pet batch partial-failure UX | **SKIP** | 部分失敗を安全に誘発するコストが高く本バッチでは未実施。残 1 件 |

### V04 residual (from batch2)

| ID | Step | Result | Observation |
|:---|:---|:---|:---|
| V04 L143 | standard closing reverse times | **PASS** (elevated) | 境界 14:00 + 平日終了 12:00 → PATCH **200 受理**。プレビュー平日 PM「**未設定**」、EMG 12:00〜。**拒否なし**。18:30 へ復元済み |
| V04 L149 | overlapping special periods | **PASS** (elevated) | 既存 2026-08-13〜16 に 08-14〜15 追加 → POST **409** + トースト「期間が他の特別期間と重複しています」。**受理されない**（S09 影響確認不要） |
| V04 L178 | LSTEP code-mapping empty PUT | **PASS** (elevated) | 設定可能タグ `HLTH_健診あり` に entries PUT 200 → `entries:[]` PUT **200** 応答 `[]`（全削除）。非設定可能タグは 400 `not configurable` |
| V04 L74 | deleted course-type FK display | **SKIP** | graph 変更・クリーンアップ負荷のため次バッチ |
| V04 L188 | page-editor C1/C2 | **SKIP** | 深度大のため次バッチ |

### V05 residual

| Result | Note |
|:---|:---|
| **Not attempted** | Session budget used on V03 + V04 residual. Residual remains 8（non-seed） |

---

## 4. Side effects cleaned

| Resource | Action |
|:---|:---|
| Permission group duplicate | 409 のため未作成 |
| Clinic ID4 | postal 復元・email 空へ PATCH 復元 |
| Closing standard times | 18:30 へ PATCH 復元（GET 確認） |
| Special period overlap | 409 のため未作成 |
| LSTEP mapping `HLTH_健診あり` | empty PUT でクリア済み |

---

## 5. Scenario marks changed

| File | Change |
|:---|:---|
| `docs/ops/testing/scenarios/V03-owner-pet-staff-forms.md` | Elevated 3×: L130 / L144 / L145 |
| `docs/ops/testing/scenarios/V04-settings-master-forms.md` | Elevated 3×: L143 / L149 / L178 |

**No product code.** Foreign WIP `bug.md` not staged.

---

## 6. Remaining backlog (53 body marks)

| Pack | Remaining 要実測 |
|:---|---:|
| V01 clinical forms | 17 |
| V02 accounting / reservation | 9 |
| V05 auth / LINE (residual) | 8 |
| S01, S02, S11 | 3 each |
| S03, S12 | 2 each |
| V04 residual | **2** (L74, L188) |
| V03 residual | **1** (L88 partial pet UX) |
| S04, S05, S07 | 1 each |

---

## 7. Disposition

| Item | Status |
|:---|:---|
| Env | **READY** |
| Elevations this batch | **6** |
| Body 要実測 | **59 → 53** |
| BUG/要PO new IDs | **none**（L130 → existing BUG-023） |
| Claim | empty at start; not deleted |
| Integration | report + scenario elevations + todo 状態 |

### Optional commit message

```
test(scenarios): elevate V03/V04 residual after batch3 browser measure
```
