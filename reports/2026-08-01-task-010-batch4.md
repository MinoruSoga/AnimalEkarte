# TASK-010 — Runtime browser batch4（primary residual 11）

> **Date**: 2026-08-01  
> **Unit**: `TASK-010-BATCH4-20260801`  
> **Claim**: `claim/TASK-010` empty at start（再取得なし）  
> **Status**: **PASS (primary pack complete)** — env READY; primary 11 件すべて disposition; **11** marks elevated（body 53→42）  
> **Scope**: Browser/API measurement + scenario elevation + report + todo 状態. Product fix / migrate / claim delete なし.

---

## 1. Env health

| Check | Result |
|:---|:---|
| `docker compose ps` | backend / db / frontend **Up (healthy)** |
| `GET http://127.0.0.1:8080/health` | **200** |
| `GET http://127.0.0.1:3003/` | **200** |
| Browser | **Playwright MCP**（Chrome DevTools MCP profile が既に占有されていたためフォールバック。browser-test skill 手順に沿い認証後測定） |
| Login | demo システム管理者（identity present; password not recorded）→ clinic_id=1 |

---

## 2. 【要実測】 census

| Scope | Before | After | Δ |
|:---|---:|---:|---:|
| Scenario bodies exact `【要実測】` (`V*.md` + `S*.md`) | **53** | **42** | **−11** |
| Semantic prefix `【要実測` | **56** | **45** | **−11** |
| `V03-owner-pet-staff-forms.md` | **1** | **0** | **−1** |
| `V04-settings-master-forms.md` | **2** | **0** | **−2** |
| `V05-auth-line-forms.md` | **8** | **0** | **−8** |

```bash
rg -c '【要実測】' docs/ops/testing/scenarios/V*.md docs/ops/testing/scenarios/S*.md
# bodies total → 42
```

Primary residual after batch4: **NONE** on V03/V04/V05.

---

## 3. Primary 11 disposition

| ID | Result | Observation (evidence) |
|:---|:---|:---|
| **V03 L88** pet batch partial UX | **PASS** elevate | 現行は nested `POST /api/v1/owners` + `pets[]` の原子作成。`Promise.allSettled` 個別 POST は不在。BE `CreateWithPets` 同一 TX。部分成功 UX なし。成功/失敗は全体トースト 1 本 |
| **V04 L74** deleted course-type FK | **PASS** elevate | type `V04B4種別` + course 作成後 `DELETE type` → **409** `この種別は使用中のため削除できません`。削除済み FK 編集 UI は通常到達不能。cleanup: course 204 → type 204 |
| **V04 L188** page-editor C1/C2 | **PASS** elevate | C1: 5 テキスト空 PUT **200**。C2: marker PUT 200 → 再 GET 永続。上限実測: header 1万字 **400** `header_text は 2000 以下…` / notice 1万字 **200**。元文言復元済み |
| **V05 L93** phone `abc` | **PASS** elevate | FE `phone.trim()` 非空のみ。BE customer_fields string ≤500・phone 形式なし。形式拒否なし |
| **V05 L106** same-day cancel | **PASS** elevate | `CancelReservation`/`CancelByID` に期限・当日ガードなし。当日直前もキャンセルし得る |
| **V05 L116** 受付停止 owner UX | **PASS** elevate（**BUG-141** 併記） | PUT `status=stopped` 200 + public LIFF settings `stopped`。コード意図は `MaintenancePage`「メンテナンス中」。**runtime SPA は Top「新規予約」のまま**（`App.tsx` Step2 `setPage('top')` が maintenance を上書きし得る）。`running` 復元済み |
| **V05 L130** 長文 1 万字 | **PASS** elevate | header max=2000 / notice max=10000 / privacy max=100000（binding + API 実測）。FE maxLength なし |
| **V05 L174** mapping entry 形式 | **PASS** elevate | empty codes → 400; empty string code → 400; invalid code_type → 400。`entries:[]` clear 200 |
| **V05 L175** 疾患コード重複 | **PASS** elevate | 新規 POST 201 → 同一 condition_code 再 POST **409**。seed コードも 409。テスト行 DELETE 204 |
| **V05 L176** CSV 列不正 | **PASS** elevate | `POST .../friend-attributes` `foo,bar` → **400** `required column not found: line_user_id (expected one of: LINE ID, line_user_id, userId)` |
| **V05 L177** bulk remove 一覧観測 | **PASS** elevate | 手動タグ `優良顧客` owner_count=2。`DELETE .../lstep/tags/:tag` **204** でも直後の tag-summary / owners 件数 **不変**の場合あり。観測点= summary + drawer owners + toast/invalidate。LSTEP 実タグは対象外 |

**Elevated: 11 / left 要実測: 0 / BUG: BUG-141（L116 挙動差分） / BLOCKED: 0**

---

## 4. Stretch

| Pack | Attempted | Note |
|:---|:---|:---|
| V02 (9) | **no** | primary 完了後の session budget 優先で未実施 |
| S* non-seed | **no** | 同上 |
| V01 (17) | deferred | TASK-009 seed apply 待ち |

---

## 5. Side effects cleaned

| Resource | Action |
|:---|:---|
| Trimming type/course V04B4* | course DELETE 204 → type DELETE 204 |
| Page-editor texts | original header 等へ PUT 復元 |
| LINE reservation status | `stopped` 測定後 **running** 復元（LIFF settings 確認） |
| Condition mapping test row | POST 後 DELETE 204 |
| Tag mapping HLTH_健診あり | invalid PUT のみ（clear 200） |
| 優良顧客 tag | DELETE 試行したが件数不変（データ実質維持） |

---

## 6. Scenario marks changed

| File | Δ exact |
|:---|---:|
| `V03-owner-pet-staff-forms.md` | −1 |
| `V04-settings-master-forms.md` | −2 |
| `V05-auth-line-forms.md` | −8 |

---

## 7. BUG filed (report only; bug.md is foreign WIP — not staged)

### BUG-141: LINE 予約受付「停止中」でも飼い主 SPA が Top（新規予約）を表示する

- **症状**: hospital で `status=stopped` 保存後、`GET /api/liff/:id/settings` は `stopped` を返すのに、`/line-reserve/1/` は「メンテナンス中」ではなく Top（新規予約・予約確認）を表示する。
- **再現**: clinic 1 で settings PUT stopped → owner SPA hard reload → メンテナンス見出しなし。
- **調査起点**: `frontend/line-reserve/src/App.tsx` — Step1 で `setPage('maintenance')` するが、LIFF ready 後 Step2 の `finally { setPage('top') }` が上書きし得る。unit test は mock で maintenance を期待。
- **期待**: 停止中は予約開始不可（MaintenancePage）。
- **安全**: 測定後 status は running に戻した。

---

## 8. Orchestration

| Wave | Mode | Agents |
|:---|:---|:---|
| Wave 0 RO | parallel subagents | RO-V03, RO-V04, RO-V05 explore + coordinator census/env/claim |
| Wave 1 measure | coordinator browser/API | Playwright + authenticated fetch |
| Wave 2 write | coordinator serialize | V03/V04/V05 elevates + report + todo |

Native Workflow tool was available but subagent fan-out + coordinator measure satisfied dynamic-workflow contract（user authorized both）.

---

## 9. USER next

1. Optional path-scoped commit（allowlist paths only; never `bug.md`）
2. BUG-141 を `bug.md` / 実装レーンへ正式起票するか判断
3. TASK-009 seed apply 後 V01 batch
4. Stretch V02 / S* は batch5 候補
5. 不要なら `claim/TASK-010` は空のまま
