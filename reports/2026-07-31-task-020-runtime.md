# TASK-020 — ui-design-compliance Playwright runtime

> **Date**: 2026-07-31  
> **Claim**: `claim/TASK-020` (not deleted)  
> **Worker**: W-020  
> **Status**: **FAIL / env-blocked** (not a product-green result)  
> **Scope**: `e2e/ui-design-compliance-readonly.spec.ts` only — no product source changes; no full-suite intent.

---

## 1. Commands used

### Attempt A — prefer `docker compose exec frontend` (failed env)

```bash
cd /Users/minoru/Dev/Case/AnimalHospital/AnimalEkarte
docker compose exec frontend pnpm test:e2e -- e2e/ui-design-compliance-readonly.spec.ts --workers=1
```

| Field | Value |
|:---|:---|
| **Exit code** | **1** (`ELIFECYCLE` / `Command failed with exit code 1`) |
| **Playwright selection** | **Full e2e suite** — `Running 257 tests using 1 worker` (file filter did **not** isolate the target spec; observed as `playwright test -- e2e/ui-design-compliance-readonly.spec.ts --workers=1`) |
| **Pass** | **9** |
| **Fail** | **47** |
| **Did not run** | **201** |
| **Skip** | **0** (not reported) |
| **Duration** | 16.4s |

**Root cause (env / tooling, not product assertion):**

```text
Error: browserType.launch: Executable doesn't exist at
/root/.cache/ms-playwright/chromium_headless_shell-1228/chrome-linux/headless_shell
║ Looks like Playwright was just installed or updated.
║ Please run the following command to download new browsers:
║     pnpm exec playwright install
```

Container check after run: `/root/.cache/ms-playwright` **does not exist**. `@playwright/test` version in frontend container: **1.61.1**.

This matches project documentation in `frontend/scripts/run-e2e.sh`:

```text
# Why Docker: the project frontend container is Alpine-based and cannot run
# Playwright's Chromium. The official playwright jammy image has Chromium 1223.
```

**Disposition of Attempt A**: invalid as TASK-020 evidence for the 84-page / 93-test compliance suite. Counts above are **full-suite env failures**, not ui-design-compliance product results. Do not treat 9 passed as green product signal.

---

### Attempt B — documented alternate `./scripts/run-e2e.sh` (correct file scope; auth env blocked)

```bash
cd /Users/minoru/Dev/Case/AnimalHospital/AnimalEkarte/frontend
./scripts/run-e2e.sh e2e/ui-design-compliance-readonly.spec.ts --workers=1
```

Underlying runner (from script): `mcr.microsoft.com/playwright:v1.60.0-jammy`, fresh `npm install @playwright/test@1.60.0`, base URL `http://host.docker.internal:3003`.

| Field | Value |
|:---|:---|
| **Effective Playwright result** | **FAIL** (3 failed) |
| **Shell pipeline exit** | **0** observed on host pipeline (`… \| tee …` without `pipefail`; `tee` succeeded). Playwright itself reported failures — treat run as **failed**, not green. |
| **Tests selected** | **93** using **1** worker — matches expected ui-design-compliance inventory (84 product pages + public/RBAC/clinical extras) |
| **Pass** | **4** |
| **Fail** | **3** |
| **Did not run** | **86** (serial describes aborted after first failure in group) |
| **Skip** | **0** (not reported as skipped; remaining tests listed as `-` / did not run) |
| **Duration** | 13.1s |

#### Verbatim summary lines (Attempt B)

```text
Running 93 tests using 1 worker
  3 failed
    [chromium] › e2e/ui-design-compliance-readonly.spec.ts:484:9 › protected routes: read-only 4 viewport audit › 飼主カルテレポート 
    [chromium] › e2e/ui-design-compliance-readonly.spec.ts:535:7 › known RBAC regressions: synthetic read-only /me › 会計詳細はレジ締め閲覧権限がない場合にfail-closedとなる 
    [chromium] › e2e/ui-design-compliance-readonly.spec.ts:646:9 › clinical P10: view-only /me cannot mutate clinical records › カルテ作成 
  86 did not run
  4 passed (13.1s)
```

#### Passed (4) — only unauthenticated / static checks completed

| # | Result | Title |
|:---|:---|:---|
| 1 | ✓ | `route matrix inventory is exactly 84 product pages` (4ms) |
| 2 | ✓ | `public auth routes: read-only 4 viewport audit › ログイン` (4.9s) |
| 3 | ✓ | `public auth routes: read-only 4 viewport audit › パスワードを忘れた方` (3.2s) |
| 4 | ✓ | `public auth routes: read-only 4 viewport audit › パスワード再設定` (3.1s) |

#### Failed (3) — identical env blocker

```text
Error: E2E_LOGIN_EMAIL and E2E_LOGIN_PASSWORD must be set (no in-repo credential fallback; see frontend/e2e/README.md)
   at helpers/auth.ts:12
   at requireE2ECredentials (/test/e2e/helpers/auth.ts:12:11)
   at loginAsDemoAdmin (/test/e2e/helpers/auth.ts:44:31)
```

Failed titles:

1. `protected routes: read-only 4 viewport audit › 飼主カルテレポート`
2. `known RBAC regressions: synthetic read-only /me › 会計詳細はレジ締め閲覧権限がない場合にfail-closedとなる`
3. `clinical P10: view-only /me cannot mutate clinical records › カルテ作成`

Remaining protected routes (80) and remaining RBAC/clinical cases (6) **did not run** after those first failures in each serial group.

---

## 2. Exact env blockers

| Blocker | Where | Detail |
|:---|:---|:---|
| **B1. No Playwright browser in frontend app container** | Attempt A | Alpine frontend image; `/root/.cache/ms-playwright` missing; launch of `chromium_headless_shell-1228` fails. Documented: use `frontend/scripts/run-e2e.sh` instead. |
| **B2. File isolation broken on Attempt A CLI** | Attempt A | `pnpm test:e2e -- e2e/ui-design-compliance-readonly.spec.ts --workers=1` expanded to full suite (**257** tests). Not used as compliance evidence. |
| **B3. Missing `E2E_LOGIN_*` credentials** | Attempt B (and host) | Host shell: `E2E_LOGIN_EMAIL` / `E2E_LOGIN_PASSWORD` **unset**. Frontend container: both **unset**. Required by `frontend/e2e/README.md` and `helpers/auth.ts` for any authenticated login path. **No credentials invented or read from secrets files.** |
| **B4. `run-e2e.sh` does not forward login env** | Attempt B script | Script only injects `-e PLAYWRIGHT_TEST_BASE_URL=…`. Even if host exports `E2E_LOGIN_EMAIL` / `E2E_LOGIN_PASSWORD`, the Playwright jammy container would not receive them unless the script (or a one-shot `docker run -e …`) is extended. **Script was not modified** (writer scope: reports only). |

App reachability note (coordinator-measured; not re-verified in depth here): backend/frontend reported healthy at localhost; Attempt B public routes (login / forgot / reset) **did** complete browser work against `host.docker.internal:3003`, so browser + base URL path worked for unauthenticated pages.

---

## 3. Counts to record (canonical for TASK-020)

**Canonical run = Attempt B** (scoped to `ui-design-compliance-readonly.spec.ts`).

| Metric | Count |
|:---|---:|
| Selected | 93 |
| **Passed** | **4** |
| **Failed** | **3** |
| **Did not run** | **86** |
| Skipped (explicit) | 0 |
| **Exit / disposition** | **FAIL** — env-blocked on authenticated suites |

**Not product-green.** Static inventory assertion (“exactly 84 product pages”) passed; full 4-viewport audit of protected routes and RBAC/clinical P10 suites **not executed**.

---

## 4. USER unblock (for a real full runtime)

Do **not** commit credentials. Per `frontend/e2e/README.md`:

```bash
export E2E_LOGIN_EMAIL='…'      # local demo admin email present in target DB
export E2E_LOGIN_PASSWORD='…'   # never commit

# Option: extend run temporarily with env passthrough, e.g.:
# docker run … -e E2E_LOGIN_EMAIL -e E2E_LOGIN_PASSWORD … 
# or patch run-e2e.sh to forward those two vars (out of W-020 write scope).

cd frontend
./scripts/run-e2e.sh e2e/ui-design-compliance-readonly.spec.ts --workers=1
```

Avoid Attempt A for this suite until browsers are installed in a non-Alpine Playwright-capable image (project already documents jammy image as the path).

After a green or product-failing (non-env) run, update `docs/spec/ui-design-compliance.md` runtime date only if that remains the established pattern — **not done in this packet** (no successful full runtime; no optional doc touch).

---

## 5. Out of scope / hygiene

- No product source changes.
- No full e2e suite intended (Attempt A accidentally expanded; discarded as evidence).
- Claim branch `claim/TASK-020` left in place for USER release after integration.
- Foreign WIP on main tree (`backend/internal/inventory/*`) was not touched.

---

## 6. Log artifacts (session temp; not committed)

| Run | Host path |
|:---|:---|
| Attempt A | `/tmp/task-020-e2e-output.txt` |
| Attempt B | `/tmp/task-020-e2e-alt-output.txt` |
