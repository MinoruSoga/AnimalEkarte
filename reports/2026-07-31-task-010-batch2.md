# TASK-010 — Runtime browser batch2 (V04 settings master)

> **Date**: 2026-07-31  
> **Packet**: W-010-B2 / unit TODO-MD-NEXT-ORCH-WAVE-20260731  
> **Claim**: `claim/TASK-010` (not released by agent)  
> **Status**: **PARTIAL PASS** — env READY; Playwright batch executed; **6** marks elevated on V04; backlog remains  
> **Scope**: Browser measurement + report + scenario mark elevation only. Product code / inventory / foreign claims not touched.

---

## 1. Env health

| Check | Result |
|:---|:---|
| `GET http://127.0.0.1:8080/health` | **200** `{"status":"ok"}` |
| `GET http://127.0.0.1:3003/` | **200** |
| Browser | **Playwright MCP** (Chrome DevTools MCP profile busy → Playwright used) |
| Login / seed | **OK** — demo `admin@noavet.jp` / `password`（システム管理者）→ 八王子病院コンテキスト |

---

## 2. 【要実測】 census

| Scope | Before | After | Δ |
|:---|---:|---:|---:|
| Scenario bodies only (`V*.md` + `S*.md`) | **65** | **59** | **−6** |
| `V04-settings-master-forms.md` alone | **11** | **5** | **−6** |

Count command:

```bash
rg -c '【要実測】' docs/ops/testing/scenarios/V*.md docs/ops/testing/scenarios/S*.md
rg -c '【要実測】' docs/ops/testing/scenarios/V*.md docs/ops/testing/scenarios/S*.md | awk -F: '{s+=$2} END {print s}'
```

---

## 3. Pack attempted (V04 high-value from RO-010)

**Theme**: V04 settings master form validation edges  
**URLs exercised**:
- `/settings/campaigns`
- `/settings/medicine`
- `/settings/closing-time`
- `/settings/shift-templates`
- `/settings/clinic`
- `/settings/reservation-type`
- `/line-reserve/1`（smoke; LIFF courses API は 401 のため消費者側コース名一覧までは未到達）

### Results table

| ID | Step | Result | Observation |
|:---|:---|:---|:---|
| V04 L71 | campaign end &lt; start | **PASS** (elevated) | FE `CampaignSidePanel` で保存ブロック。メッセージ「終了日は開始日以降にしてください」。API 未到達。BE にも `validateCampaignPeriod` |
| V04 L100 | medicine strength `abc` | **PASS** (elevated) | 製品含量は `type=number`（体重換算時のみ表示）。`abc` は値に残らない。空+per_weight で POST `/v1/masters/medicines` **400** トースト「per_weight 計算には strength（製品含量）が必要です」。永続レコードなし |
| V04 L116 | reservation-type LINE name fallback | **PASS** (elevated) | パネル: LINE表示名空時 placeholder=区分名称。API: 主要区分は `reservation_display_name=""` かつ `show_short_name=false`。BE `toLiffCourseResponse` は空 RDN+show_short_name=false → **Name**。LIFF `/api/liff/1/courses` は本 env で 401（mock token 不足）のため消費者 UI のコース名までは未確認だが、FE 契約+マスタデータ+BE フォールバックは実測済み |
| V04 L161 | shift break outside work hours | **PASS** (elevated) | 勤務 09:00–18:00 + 休憩 19:00–20:00 → POST `/api/v1/shift-templates` **201** 受理。FE/BE に勤務時間内制約なし。一時テンプレ「V04シフト外休憩」(id=19) は UI 削除でクリーンアップ済み |
| V04 L148 | special-period invalid range error shape | **PASS** (elevated) | start&gt;end → POST 400 `{"error":"開始日は終了日以前に設定してください"}` + toast。boundary≥pm_end → POST 400 `PM締め終了時刻(12:00)は境界時刻(18:00)より後…` + toast。**500 / 白画面なし** |
| V04 L197 | company invoice validation | **PASS** (elevated) | 空 / `123…`(Tなし) / `T123` / 長文(T+9×100) / `T9999999999999` いずれも PATCH `/v1/company` **200** +「インボイス登録番号を更新しました」。形式・必須検証なし。元値（空）へ復元済み |
| V04 L143 | closing standard reverse times | **SKIP** | RO-010 defer（live mutate リスク） |
| V04 L149 | overlapping special periods | **SKIP** | RO-010 defer（状態残留） |
| V04 L74 | course type deleted FK display | **SKIP** | RO-010 defer（graph 変更） |
| V04 L188 | page-editor | **SKIP** | RO-010 defer |
| V04 L178 | LSTEP mapping empty PUT | **SKIP** | RO-010 defer |

### Side effects cleaned

- キャンペーン: 無効日付のため未作成
- 薬剤: 400 のため未作成
- 特別期間: 400 のため未作成
- シフトテンプレ id=19「V04シフト外休憩」: 削除確認ダイアログ経由で削除（一覧から消失）
- インボイス: 元値（空文字）へ PATCH 復元

### Scenario marks changed

| File | Change |
|:---|:---|
| `docs/ops/testing/scenarios/V04-settings-master-forms.md` | Elevated 6×【要実測】: L71 campaign period, L100 medicine strength, L116 LINE display fallback, L148 special-period error shape, L161 out-of-hours break accept, L197 invoice free-text |

**No other scenario files edited.** Product code not touched.

---

## 4. Remaining backlog (59 body marks)

| Pack | Remaining 要実測 |
|:---|---:|
| V01 clinical forms | 17 |
| V02 accounting / reservation | 9 |
| V05 auth / LINE (residual) | 8 |
| V04 settings master (residual) | **5** |
| V03 owner / pet / staff | 4 |
| S01, S02, S11 | 3 each |
| S03, S12 | 2 each |
| S04, S05, S07 | 1 each |

**V04 residual**:
1. L74 削除済みコース種別の表示
2. L143 標準締め時刻の境界逆転
3. L149 特別期間の重複（受理可否; S09 影響は別）
4. L178 LSTEP コードマッピング空置換
5. L188 page-editor C1/C2

---

## 5. Commands / evidence

```bash
# Health (session start)
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8080/health   # 200
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:3003/         # 200

# Census after
rg -c '【要実測】' docs/ops/testing/scenarios/V*.md docs/ops/testing/scenarios/S*.md
# bodies total → 59
```

Browser: Playwright MCP against `http://127.0.0.1:3003`（login demo admin@noavet.jp）.

Notable API captures:
- `POST /v1/closing-settings/special-periods` → 400 + Japanese error body (×2 cases)
- `POST /api/v1/shift-templates` → 201 with out-of-range breaks
- `PATCH /v1/company` → 200 for empty/invalid/long invoice strings
- `POST /v1/masters/medicines` → 400 when per_weight without strength

---

## 6. Disposition

| Item | Status |
|:---|:---|
| Env | **READY** |
| Elevations this batch | **6** |
| Body 要実測 | **65 → 59** |
| Claim `claim/TASK-010` | Held — **USER** releases after integrate/abandon |
| Integration ready for ledger | **YES** — report + V04 elevations; optional commit below |

### Optional commit message

```
test(scenarios): elevate V04 pack after batch2 browser measure
```
